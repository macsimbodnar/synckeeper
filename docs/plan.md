# Synckeeper — Implementation Plan

Master tracking document. The spec in [spec.md](spec.md) is the contract; this file tracks how and in what order the delta between spec and code gets built. Rewritten 2026-07-17 against the revised spec: the phase system (0–7) is retired as history; new work is organized in workstreams.

## Phase history (done, kept for reference)

| Phase | Doc | Outcome |
|---|---|---|
| 0 | [phase-0.md](history/phase-0.md) | Skeleton: auth, config, DB, Drive client interface — done 2026-07-07 |
| 1 | [phase-1.md](history/phase-1.md) | One-shot bidirectional sync, S1–S8 — done 2026-07-08 |
| 2 | [phase-2.md](history/phase-2.md) | Safety hardening, F1–F5 — done 2026-07-08 |
| 3 | [phase-3.md](history/phase-3.md) | Continuous mode + 2 h soak — done 2026-07-09 |
| 4 | [phase-4.md](history/phase-4.md) | `init --adopt` + matrix tests — done 2026-07-14 (real rollout pending, now W6) |
| 5 | [phase-5.md](history/phase-5.md) | Cross-platform hardening — superseded by W7/W8 |
| 6 | [phase-6.md](history/phase-6.md) | Daemon monitoring + control socket — stages 1–2 done 2026-07-14; tray → W9 |
| 7 | [phase-7.md](history/phase-7.md) | macOS quick wins — activity direction + case-collision done; Finder sidebar abandoned (no API); rest folded into W3/W9 |

## Workstreams

Statuses: `not started` → `in progress` → `blocked (reason)` → `done (date)`. Work strictly in order within a workstream; W1 blocks everything else (correctness first).

**Current order (Max, 2026-07-27): ~~W14~~ → ~~W13 close-out~~ → **W15** → W7 (L1–L5, L7, L8) → W6 → W10.** W13 is **closed** — code, the macOS compile/run (T13.10), and the on-hardware acceptance on both platforms (T13.17). **W15 is in progress and has no code items left** — U1–U5 done 2026-07-27/28, only U6's closing doc pass remains; **W7 follows**. W12 and **W9** are parked (W9 because W15 is the UI now).

**Execution order on the primary platform (updated 2026-07-18): W1.8 → W1.9 → W4 → W3 → W5.** W1.8, W1.9, W4, W3, and **W5 are done** (W4 closed 2026-07-23 finding R25; W3 closed 2026-07-24 — adversarial check + 2 h FSEvents soak passed; **W5 closed 2026-07-24** — init offers `service install`, `account` shows the Google email, pause kept in-memory). **Next is W6** (real multi-machine rollout, blocked on a second physical machine) or the platform ports W7/W8. W3 and W4 kept their identifiers and swapped execution order — correctness never depends on the watcher (spec §8.1), so the fuzzer earned more than FSEvents did; see decisions.md 2026-07-18 "Roadmap". W1.9 (adversarial round 3, over the code) preceded W4 for the same reason: the fuzzer simulates machines on the real, fold-happy local filesystem, and two of round 3's reproduced defects (C2, C3) would turn its oracle red from day one. W6+ follow the spec roadmap.

### W1 — Correctness fixes (from the 2026-07-17 review) — `done (2026-07-17)` · reopened by adversarial analysis (W1.7, W1.8, W1.9 closed 2026-07-19; W1.9.6 from a round-4 review closed 2026-07-21)

Reproduced bugs first; each fix lands with its regression test (testing.md R1/R2, plus rows below). **A later adversarial re-analysis found a critical data-loss bug this ordering-focused pass missed — see W1.7 below; that too must be green before W2.**

1. **[R1] Conflict-backup vs move ordering — data loss.** — **done 2026-07-17.** Remote rename to a lexicographically smaller path + remote edit + local edit: `ConflictBackup` sorted before the `MoveLocal` that fed it, backup failed, download overwrote the moved local edit; cycle 2 planned nothing. Fixed per spec §4.5: a true conflict never routes the local content through a move — the backup acts on the file's current location (conflict copy named after it) — and actions carry `ProtectedBy`, refused by the executor when their backup failed. Tests: see testing.md R1 (engine + reconcile + executor levels).
2. **[R2] MkdirLocal-before-MoveLocal — permanent livelock.** — **done 2026-07-17.** Remote same-id dir rename + new remote subdir inside it: the mkdir stage `MkdirAll`ed the move destination; `MoveLocal` then failed `file exists` every cycle. Fixed with the ordering fix per spec §4.5: plan stage order is now dir-moves → mkdirs → file-moves/backups → transfers → deletes. Dir moves depend on nothing the plan creates; file moves stay after mkdirs because a `MoveRemote` may target a folder a `MkdirRemote` creates. Tests: see testing.md R2.
3. **`init --force` leaves a stale remote mirror** — **done 2026-07-17.** It reset the page token without rebuilding the cache → silently missed remote changes. Fixed per spec §12: `initialize` now calls `remotedelta.ForceFullWalk` (fresh token + mirror rebuild in one step); a fresh `init` gets a pre-warmed mirror as a side benefit. Test: testing.md R3.
4. **Download overwrite window** — **done 2026-07-17.** Downloads now carry the local state the plan assumed at the target (scanned size/mtime, or "absent" for conflict-vacated and edit-beats-delete targets); the executor re-stats immediately before the atomic rename and refuses on any drift — the racing local write wins the cycle and the next cycle resolves it as an ordinary conflict. Tests: testing.md R4.
5. **Read-only commands can migrate the DB without the lock** — **done 2026-07-17.** `statedb.OpenRead` accepts only an exact schema-version match (older → "run `sync` once with this binary"; newer → "binary too old"; missing DB never created); `openReadEnv` uses it, treating a missing DB as not-initialized. Migrations now run only under the instance lock (spec §14). Test: testing.md R5.
6. **Swap-rename sweep** — **done 2026-07-17, and the review's "self-heals" analysis was wrong.** The verification test exposed *silent permanent divergence*: after the swap's moves half-failed (FS renamed, DB rolled back), an unprotected `Record` stamped the planned md5 onto whichever file sat at the path — a poisoned baseline the scanner trusted forever. Fixed by extending `ProtectedBy` to local moves (records/uploads of a moved file are refused when its move failed) and by `Record` verifying the scanned stat before overwriting the baseline (invariant 7). The transient unique-constraint move failures remain as accepted noise; convergence to the *correct* state within ~4 cycles is now pinned by the test. Tests: testing.md R6.

### W1.7 — Adversarial-analysis correctness fixes — `done (2026-07-17)`

Reopened W1 after an adversarial re-analysis found a confirmed critical data-loss bug the ordering-focused W1 review missed. Same discipline (red-first regression tests, suite + `-race` green). Blocks W2. See decisions.md 2026-07-17 "W1.7 (R7/R8/G3)".

1. **[R7] `MoveLocal` clobbers an untracked local file at the move destination — silent data loss.** — **done 2026-07-17.** A remote rename/move onto a name that already exists locally `os.Rename`d over the local file (no conflict copy, no quarantine, reported as success). Fixed with the R4 pattern generalized to moves: reconcile preserves an untracked occupant as a conflict copy (backed up before the move, uploaded, move `ProtectedBy` the backup) and stat-pins a tracked occupant (safe to clobber, e.g. the R6 swap); the executor re-stats the destination and refuses an unexpected occupant. Tests: testing.md R7 (reconcile + engine + executor).
2. **[R8] `ConflictBackup` overwrites an existing destination.** — **done 2026-07-17.** Same unguarded-rename class; the executor now refuses a rename onto an existing conflict-copy path (crash leftover), replanning with a fresh timestamp. Tests: testing.md R8.
3. **[G3] Mass-delete guard aborted the whole cycle (spec §6/§8.1 deviation).** — **done 2026-07-17, C1.** The daemon now defers only the delete-class actions and keeps syncing everything else, surfacing the block in `status`; the interactive one-shot still aborts with the `--confirm-deletes` hint. New `engine.Options.DeferMassDelete` + `Result.GuardBlocked/GuardReason`. Tests: testing.md G3.

### W1.8 — Adversarial-analysis correctness fixes, round 2 — `done (2026-07-19)`

Reopens W1 again after a second adversarial analysis (2026-07-18, run against the goal/plan/spec once W2 closed) found nine defects, each reproduced with a throwaway test before being reported. Same discipline as W1.7: **red-first regression test, then the fix, then the testing.md row, suite + `-race` green at every commit.** Blocks everything else. See decisions.md 2026-07-18 "W1.8".

Work strictly in this order — item 1 is the choke point everything after it is written against, and items 2–3 are the highest user-visible impact.

1. **[GATE] The local-write gate — one choke point, mechanically enforced.** — **done (2026-07-18).** New `internal/executor/localwrite.go` becomes the only file in the package permitted to mutate a local path: `guardedRename` / `guardedRemove` / `guardedStat` over a single `expectation{exists, size, mtimeNS}`. Port all five primitives onto it — `download` (`executor.go:507`), `moveLocal` (`:323`), `conflictBackup` (`:380`), `record` (`:559`), and **`quarantineLocal` (`:581`), which had no guard at all (A7)**. Expectations map straight across: backup = zero value (absent), quarantine/record = pinned scanned stat, download/move = what the plan assumed, so `Action.LocalExists/LocalSize/LocalMtimeNS` is already the wire format and no new plan surface is needed. Closing A7 also means reconcile stat-pins `QuarantineLocal`, exactly as R4 stat-pinned downloads. Companion test walks the package AST and fails on any `os.Rename`/`os.Remove`/`os.RemoveAll`/`os.Create`/`os.OpenFile`/`os.WriteFile`/`os.Truncate`/`os.Chtimes`/`os.Link`/`os.Symlink` outside the gate file *(ban list extended 2026-07-18, plan review — `os.WriteFile` was the obvious one-call bypass)*. One recorded exemption outside the package: `doctor --repair`'s prefix-matched orphan-temp removal (`doctor.go:233`) stays outside the gate — spec §7 names it. Amends spec §7. Tests: R13 + the gate's structural test. *Implementation notes (2026-07-18):* the expectation stays the three-field wire format as decided; absence-when-expected is a per-primitive policy, preserving the two decided behaviors (moves proceed — R7's sibling-vacated case; downloads/records/quarantines refuse — R4's one-rule) rather than inventing a unified one. `moveFile` moved into the gate (its copy fallback uses banned calls), temp cleanup routes through `removeOwnTemp` (refuses non-`.synckeeper.tmp.*` names), and the empty-dir remove keeps `os.Remove`'s non-empty refusal as its guard. The gate test was verified to catch a planted violation.
2. **[A1] Local directory renames are moves, not delete + recreate.** — **done (2026-07-18), together with C1/R18.** `reconcile.Plan` pass 2 `continue`s on `loc.IsDir` before the move-pairing block (`plan.go:256-265`), so directories are excluded from local-driven pairing; pass 4 then trashes the orphaned baseline dir (`plan.go:374-377`). Reproduced with no concurrency: renaming `Docs/` → `Papers/` plans `mkdir_remote` + per-file `move_remote` + `trash_remote`, and the Drive folder id changes.

   **Approach is decided — implement it, don't re-derive it** (decisions.md 2026-07-18 "W1.8.2"). *Share the representation, not the detection.* Keep two detectors feeding one `dirMove` list and one `rewrite()`:
   - *Remote-driven* stays as-is (`plan.go:52-82`): same file id at a different remote path.
   - *Local-driven* is **collapsed out of the file pairing that already happens.** The file pass already computes `Docs/one.txt → Papers/one.txt` for every child and discards the pattern — that is the entire bug. Pair baseline dir D → new location N when: D is absent locally; **D's remote is unchanged** (same precondition file pairing already uses); at least one tracked descendant paired as a move; and **every** surviving paired descendant landed under N preserving its subpath relative to D. Deleted descendants don't block (their own `TrashRemote` covers them); a descendant paired *elsewhere* does block — that's a scatter, not a rename. No surviving children → no evidence → delete + create, unchanged and intended.
   - Emit **one** `MoveRemote{IsDir:true}` and **drop** the per-file moves it explains: a Drive reparent carries the whole subtree.
   - *Why conservative:* a missed pairing costs a churned folder id (today's behaviour, and harmless once item 3 lands); a wrong pairing reparents an entire remote subtree including remote-only files the plan never reasoned about.

   **Two traps, both verified against current code:**
   - **The moves-stage split must stop keying on `IsDir`.** `plan.go:398-405` hoists every `IsDir` move ahead of the mkdirs (that hoist is R2's fix — a `MkdirLocal` scaffolding a rename destination is a permanent livelock). But a local-driven dir move is a `MoveRemote`, touches no local path, and must run *after* the mkdirs like every other `MoveRemote`, since its destination parent may be a folder `MkdirRemote` has yet to create (`plan.go:396-397` already says so for files). Predicate becomes `Type == MoveLocal && IsDir`. Within the post-mkdir stage, dir `MoveRemote`s order **before** file `MoveRemote`s, top-down — a file moving into the renamed folder resolves its parent via `pathIDs`, which only learns the new path when `renamePathIDs` runs (`executor.go:357`).
   - **`moveRemote`'s commit drops `IsDir` and would corrupt the baseline.** `executor.go:358-373` calls `RenameItemPath` (which already renames descendant rows — `statedb/items.go:85-116`) and then `UpsertItem` with no `IsDir`; `UpsertItem` deletes-then-inserts (`items.go:61-75`), so the folder row returns as `is_dir = 0` and pass 1 then treats it as a file. Set `IsDir`, or skip the upsert and let `RenameItemPath` stand alone — and don't `os.Stat` for size/mtime/md5, which mean nothing for a directory. **Round 3 (2026-07-18) proved the *file* case of this same commit is live data corruption (C1, test R18):** the upsert records the destination's *current* stat with the *scanned* md5, so an edit landing between scan and commit poisons the baseline — local and Drive diverge silently and permanently, reported as success (reproduced; the R6 class at a sixth commit site). The fix is trap 2's, made general for both files and dirs: **the commit never restates local truth** — `RenameItemPath` keeps the row's scanned stat + md5, only the Drive-side fields (`drive_md5`, `drive_version`) are refreshed, and there is no `os.Stat` at all. Red test R18 lands with this item.

   **The §4.4 interaction is decided (2026-07-18, plan review): remote-side rows resolve under N.** Remote children of D the pairing didn't explain — added or edited concurrently on another machine — are planned at their post-reparent location under N (the reparent carries them there): remote-only creations download into N, edit-beats-delete downloads land in N, and the plan never scaffolds a zombie D/ after deciding D moved. Mechanically this is the shared `rewrite()` applied to **the side that hasn't moved yet**: remote-driven dir moves rewrite local/baseline paths (as today), local-driven dir moves rewrite remote paths — sharing the list without honoring that direction plans downloads into the dead D/. R9 gains both concurrent-change cases. Amends spec §4.3. Test: R9. *Implementation notes (2026-07-18):* the "drop the per-file moves" fell out structurally — once a dir collapses, pass 1 judges its children at their post-rename paths (§4.2's post-move rule applied to the side the user moved), so the per-file moves are never emitted rather than emitted-and-removed. Detection gained two conservative guards in the decided rule's spirit: N must be a brand-new local dir (a tracked destination is a merge, not a rename) and N's name must be free on the remote side (a reparent onto an occupied name would mint a duplicate — the C2 class). The reparent slots into the stage order after the mkdirs and before the file moves, with remote mkdirs that target a just-reparented dir deferred behind it (spec §4.5 note). C1/R18 landed in the same change: `moveRemote`'s commit is `RenameItemPath` + a Drive-fields-only update — no stat, no restated local truth, `is_dir` preserved (both traps closed by the same shape).
3. **[A2] The mass-delete guard counts content, not containers.** — **done (2026-07-18).** Falls out of A1 but fix it independently as belt-and-braces: `guards.CheckMassDelete` counts only file-class deletions against tracked non-directory items. Reproduced: renaming one folder with 20 subfolders = 21 delete-class actions / 41 tracked items → the one-shot aborts demanding `--confirm-deletes`, and the daemon defers the deletes without changing the state that produced them, so it replans the same 21 every cycle and stays **permanently guard-blocked**. Verify both halves: one-shot no longer aborts, daemon no longer wedges. Amends spec §6, adds acceptance G4. Test: R10.
4. **[A4] Decision-table rows resolve at post-move paths.** — **done (2026-07-18).** Pass 2 looks up `newRemoteAt(p)` at the *pre-move* path (`plan.go:266`) while emitting the action at `target`, the post-move path (`plan.go:255`, `:295`) — the directory branch three lines above already uses `target` (`plan.go:261`), which is what makes this an oversight rather than a decision. Consequences: spec §4.2's `absent | new | new, diff md5` → conflict never fires (no conflict copy for the local content), and the plan emits `upload B/x` **and** `download B/x` into the same parallel stage. Amends spec §4.2. Test: R11.
5. **[A5] Enforce spec §4.5's concurrency rule instead of assuming it.** — **done (2026-07-19).** `reconcile.ValidateTransferStage`, called by `executor.Apply` before anything journals; the reconcile test harness validates every planned scenario as a property net. One precise exemption: a **directory Record** commits a DB row only and cannot race a child's file I/O, so it is exempt from the ancestor/descendant rule (not the same-path one) — a blanket check would refuse the real dir-adopt + child-download plan. No code anywhere implements "same rel_path, or an ancestor/descendant pair, never run concurrently"; `executor.Apply` fans the transfer stage into 4 workers with no overlap check (`executor.go:139-159`). Add a pre-execution assertion so a planner mistake is a refused plan, not a race — this catches A4's class mechanically. **Scope (corrected 2026-07-18, plan review): the assertion covers the transfer stage only** — the only concurrently executed stage. Serial stages sequence overlapping paths *by design* (nested mkdirs, bottom-up deletes, R7's backup-then-move on one rel_path); a blanket same-stage check refuses those correct plans and goes red on S8. Amends spec §4.5. Test: R12.
6. **[A3] `reload` must publish hot config safely.** — **done (2026-07-19).** The loop publishes an atomic ignore-glob snapshot (`publishIgnore`) that the pump and `watchSubtree` load; `applyReload` republishes on swap. One narrowing verified at implementation (decisions.md "W1.8.6"): `w.Poll` has **no off-loop reader** — every read is on the sync loop — so it stays a plain loop-owned write; the race was the ignore globs alone. Red-first: R14 generates a file-write storm during 25 reloads and the pre-fix code races exactly as predicted. Confirmed under `-race`: `applyReload` writes `w.Eng.Cfg.Engine.Ignore` (`control.go:123`) while the fsnotify event pump reads it per event (`watch.go:234`, and via `watchSubtree` at `:279`); `w.Poll` (`control.go:127`) is the same shape. The method's comment ("runs on the sync loop, so no cycle is concurrently reading the config") is true for cycles and false for the watcher goroutine. The suite is race-clean today only because no test reloads while events flow — so the regression test must generate event load. Amends spec §8.3. Test: R14.
7. **[A6] The daemon never exits on a watcher-rebuild failure.** — **done (2026-07-19).** The rebuild branch now degrades exactly like the `pollingOnly` branch (nil watcher, polling-only, cadence retries); **scope extended one site** (decisions.md "W1.8.7"): the startup `startNotifier` call had the identical `return err` shape, so a daemon that cannot create a watcher at launch now starts polling-only per §10's universal fallback instead of refusing to run. Red-first via two test seams (`newNotifyWatcher` injection + a test-only rebuild cadence); R15 pins degrade → still-syncs → watching restored, plus the startup case. Original finding: `watch.go:171-177` returned the error from `startNotifier`, killing the daemon — against `Run`'s own doc comment, spec §8.1 ("without exiting") and §10 ("falls back to pure polling"). The `pollingOnly` retry branch ten lines above (`:162-167`) handles the identical error by degrading, which is the tell. Trigger is `fsnotify.NewWatcher()` failing under fd pressure — precisely what the surrounding latch machinery exists for. Code fix only; spec is already correct. Test: R15.
8. **[A9 + A8] Two low-severity hardening items.** — **done (2026-07-19).** (a)'s red test corrected the analysis (decisions.md "W1.8.8"): the crash shape did **not** converge benignly — the plan was `MoveLocal` (refused by the §7 gate every cycle, destination occupied) **plus** the `TrashRemote`, so "convergence" happened only by destroying the moved folder. Two-part fix: pass 4 skips the trash when a local **dir** sits at the id's current remote path (a dir absent from both local paths stays a real deletion — trash still propagates, pinned by a companion case), and the executor completes a dir `MoveLocal` whose disk half already happened as a commit-only replay (§4.6's self-healing made real; no local mutation, so the write gate is untouched). (b) `Snapshot` gained a visited set mirroring `prune`'s; red test pins termination on the one root-reachable cycle the single-parent schema allows. Original findings: (a) a crashed directory `MoveLocal` — renamed on disk, DB not yet committed — plans `TrashRemote` for the dir when it is empty (`plan.go:374-377`: `hasCreateUnder` and `remoteAliveUnder` both false); a crash produced a remote delete, the shape invariant 6 exists to rule out. (b) `remotedelta.Snapshot`'s BFS has no visited set (`remotedelta.go:240-285`) — unlike `prune`'s directly above (`:168-179`) — so a parent cycle in the cache is an infinite loop in the daemon. Tests: R16, R17.
9. **[DOCS] Reconcile the testing.md N-row drift** — **done (2026-07-18; remainder 2026-07-19)**, same session the plan was written: N1 and N3 have rows and IDs, the duplicate is deleted. Re-scoped remainder (2026-07-18, plan review): N3 passes on the dedup suite but has no named case — `TestSnapshotDuplicateNameCollision` (`internal/remotedelta`) **landed 2026-07-19** with the item-8 commit.

### W1.9 — Adversarial round-3 fixes (code analysis, 2026-07-18) — `done (2026-07-19; item 6 added by round-4 review, 2026-07-21)`

A third adversarial code pass, run after the round-2 plan was frozen. Five defects reproduced with throwaway probes (probes deleted, tree left clean, suite green), two more found by inspection. The worst — C1, the `moveRemote` commit poisoning the baseline — **folds into W1.8.2**, which already rewrites that exact commit (see the amendment there; red test R18). The rest is this workstream: **after W1.8, before W4**. Same discipline: red-first regression test, then the fix, then the testing.md row, suite + `-race` green at every commit. See decisions.md 2026-07-18 "W1.9".

1. **[C2] Cross-tree fold collisions get the conflict they were owed.** — **done for files (2026-07-19); the directory arm is a recorded follow-up** (decisions.md "W1.9.1": a new local folder fold-equal to a remote folder still mints a duplicate folder — closing it needs the case-only-rename arm of the §7 gate for dirs, the same family as W1.8.8's R7-dir-arm note; MANUAL.md entry narrowed accordingly, not retired). Implementation: fold-keyed sibling index consulted by pass 2 after the exact match misses; the adopt lands via a **case-only `MoveLocal` to the remote byte form** with the §7 expectation pinned to the file's own scanned stat (the destination "occupant" is the source itself under the fold); shadowed ids (fold **and** duplicate-name skips, both carry `FileID` now) held harmless in passes 1 and 4 via `Input.ShadowedRemote`. Reproduced on APFS: local `Readme.txt` + remote `README.txt`, both new, different content → cycle 1 blind-uploads a **case-duplicate onto Drive** (permanent; on real Drive "first by id" makes the winner nondeterministic across machines), cycle 2 fold-collapses the siblings, our own upload loses, the baseline row reads "remote-deleted" → the user's file is **quarantined**; the §4.2 both-new conflict never fires. Spec §5's "names are compared under fold wherever the local FS folds them" was implemented only for remote siblings. Decided approach (targeted — a full fold-keyed re-key of reconcile was rejected as a wide refactor of the most load-bearing code): (a) new-local vs new-remote matching consults a **fold-keyed sibling index** when the FS folds, so fold-equal names fire the ordinary §4.2 adopt/conflict, remote winning the canonical byte-form; (b) a baseline row whose file id was **fold-skipped from the snapshot is held harmless** — surfaced as a skip, never treated as remote-deleted (no quarantine, no trash). Amends spec §5. Test R19.
2. **[C3] Directory quarantine sweeps invisible leftovers.** — **done (2026-07-19).** `sweepInvisibleLeftovers` (in the gate file, per the §7 exclusivity rule) moves ignored-or-temp entries into the dated quarantine destination before `guardedRemoveEmptyDir`; temp files sweep even when no ignore pattern covers them, and a cross-volume rename fails the action and replans (accepted — the leftover may be a subtree; decisions.md "W1.9.2"). Executor gained the `Ignore` list from engine config. Reproduced: remote deletes `docs/` while the local copy holds a `.DS_Store` (default-ignored; Finder drops one into every browsed folder) → `quarantine_local docs: directory not empty` **every cycle, forever** — A2's wedge shape with a different trigger. Fix: before removing the dir, move remaining entries that are **ignored-or-temp** into the quarantine destination (preserving subpath); anything else still refuses, unchanged — an unexpected survivor means the plan is wrong. Amends spec §3 (invariant 3 note). Test R20.
3. **[C4] Quarantine never overwrites a rescue copy.** — **done (2026-07-19).** `uniqueRescueDest` numbers the suffix before the extension (`f (2).txt`); beneath it `guardedMoveFile` refuses any occupied destination, so a race between the name pick and the rename replans instead of clobbering. Reproduced: two same-day quarantines of one rel_path — the second `os.Rename`s over the first, silently destroying the earlier rescue copy (Drive trash still held it, which caps the severity). Fix: the quarantine destination **uniquifies on collision** (numbered suffix), and `moveFile` refuses an existing destination instead of clobbering. Amends spec §3 (invariant 3 note). Test R21.
4. **[C5] Type clashes are skips in every direction.** — **done (2026-07-19).** Pass 2 skips-and-claims both directions (new local file vs remote folder, new local dir vs remote file); pass 3 checks the local occupant's type before MkdirLocal/Download. One convergence note (decisions.md "W1.9.4"): a *tracked* file remote-replaced by a folder still quarantines first and materializes the folder the next cycle — one cycle slower than before, never an error loop. Reproduced: remote folder `x` + untracked local file `x` → `mkdir_local x` fails every cycle **and** pass 2 blind-uploads the file, minting a same-name file-beside-folder pair on Drive; the mirror direction (remote file vs local dir) error-loops on the refused download. Pass 1 already has the rule — "type clash; report and leave both sides alone" — passes 2 and 3 get the same: on a type mismatch at one rel_path, emit a skip, no transfer, no delete, no upload beside it. Amends spec §4.2. Test R22.
5. **[C6] PKCE on the OAuth loopback flow.** — **done (2026-07-19).** `oauth2.GenerateVerifier` + `S256ChallengeOption` on the URL, `VerifierOption` on the exchange; two test seams (swappable endpoint + browser-opener) let R23 drive the whole loopback flow against a fake token endpoint and prove S256(verifier) equals the promised challenge. Original finding: the client secret is public by design (spec §9), so an intercepted authorization code is exchangeable by anyone; the loopback listener and crypto-random state are right but incomplete. Amends spec §9. Test R23.
6. **[R24] A shadowed folder's tracked subtree is held harmless.** — **done (2026-07-21).** Found by adversarial review round 4: W1.9.1's "never a quarantine" bound held only for the directly-colliding row. `Snapshot` skips a folder that lost "first by id" without walking its subtree, so the engine's shadowed set (built from skip `FileID`s alone) missed the folder's tracked descendants, and reconcile quarantined them off disk. Reproduced end to end (fold-equal sibling wins first-by-id → child quarantined). Fix: `engine.expandShadowed` drags every baseline row under a shadowed skip's rel_path into the harmless set — reconcile already holds a shadowed id harmless. Not fold-specific (the exact-duplicate-name folder case, N3, shadows identically on any FS). The full directory arm stays deferred; this only closes the descendant-quarantine hole in its stated safety envelope. Amends spec §5. Test R24.

Resolved without a work item: **C7** — spec §5 promised ignore-pattern matches are "always reported" while both the scanner and `Snapshot` silently drop them; resolved **spec-side** the same day (ignored matches are silent by design — the user chose the patterns; everything else stays always-reported). The round's clean-verification list (path traversal, symlinks, token/socket/DB perms, SQL parameterization, retry/backoff, fsync protocol, add-only repair, and work-matches-plan for R1–R8/G3/W2) is recorded in decisions.md so the next round doesn't re-derive it.

### W2 — Spec alignment & hygiene — `done (2026-07-18)`

1. Remove `full_rescan_interval_secs` (config, validation, reload, docs) — spec §13. — **done 2026-07-17.** Field dropped from `EngineConfig`, its default, validation, and the `reload` hot-copy; the reload test TOML no longer sets it. (Unknown keys are already rejected, so a stale key in a user config surfaces as a named error.)
2. Unicode normalization folding: extend the fold in `remotedelta.Snapshot` + `names.CaseInsensitiveFS`-style probe to NFC/NFD (spec §5); tests N2. — **done 2026-07-18.** Added `names.NormalizationInsensitiveFS` (probe by writing an NFC name and statting its NFD form) and `names.FoldKey` (NFC → lower → NFC, so a combined case+normalization collision collapses correctly); `Snapshot` now takes `caseFold, normFold` and reports the collision's cause (case / normalization / both). `golang.org/x/text/unicode/norm` promoted to a direct dep. Probe validated on real APFS.
3. Stale text: `cmd/synckeeper/config.go` still says live reload is future. (The phase-7 exit-criterion staleness was fixed 2026-07-17 with the docs restructure.) — **done 2026-07-18.** `config` now points at `synckeeper reload` for hot fields and names the restart-only fields; the parked gofmt drift in `cmd/synckeeper/status.go`, `internal/driveclient/driveclient.go`, `internal/service/status_test.go` is cleaned (whitespace-only; `internal/config/config.go` was already reformatted in W2.1).
4. Credentials (decided 2026-07-17, spec §9 — rclone model): (a) keep the author's client id/secret embedded via `internal/auth/credentials.go` as the shipping default; (b) add the BYO override — load `credentials.json` from the config dir (or config keys) with precedence over embedded; (c) `account` reports which credentials are in use; (d) README gains an rclone-style "use your own client id for dedicated quota" page and, at publication time, a donation note (funds future Google verification if the ~100-user unverified cap is ever reached); (e) hygiene: drop the stray `client_secret_*.json` from the repo root — `credentials.go` is the single embedded source. Optional (owner: Max): rotate the client in the console anyway, since publishing makes the historical secret live forever. — **done 2026-07-18.** `resolveClient(configDir)` does `credentials.json` → embedded (config-keys tier dropped — it would leak the secret via `synckeeper config`; see decisions.md); `oauthConfig` threads the config dir into `TokenSource`/`Login`; `account` shows the active client + id; stray file removed, `.gitignore` blocks BYO files; README "Credentials" section added. Donation note stays deferred to publication. Client rotation remains Max's optional call. *(2026-07-18, later: the credentials walkthrough relocated README → MANUAL.md §5 in the README/MANUAL consolidation, and the publication-time donation note will land there too — see decisions.md "README/MANUAL".)*
5. README: update build instructions for the native-build policy (no cross-compile matrix, cgo allowed). — **done 2026-07-18.** README build section already stated the native policy; corrected the stale Go version (1.22+ → 1.26+ per `go.mod`), sharpened the `build`/`build-all` (supported vs pure-Go-only-legacy) captions, and added matching comments to the Makefile (CGO_ENABLED note + legacy marker).

### W4 — Confidence: randomized sync testing (spec §16.7) — `done (2026-07-23)` · found R25

Implemented as `internal/engine/fuzz_test.go` (`TestFuzzConvergence`), reusing the S-series multi-machine harness. Default bounded for CI (8 seeds × 70 steps, `-short` shrinks it); `SYNCKEEPER_FUZZ_{RUNS,STEPS,SEED,MACHINES,CRASH}` widen it for long manual runs (item 4). First run found **R25** (a coincident/crashed-move wedge — reconcile planned a download+upload on one rel_path, §4.5 refused it every cycle); minimized red-first at the reconcile level, fixed in `plan.go` pass 1, spec §4.6 amended. Suite + `-race` green. See decisions.md 2026-07-23 "W4".

1. **[FZ1]** — **done.** Seeded fuzzer over the fake backend: N simulated machines, random op streams, random crash points at executor checkpoints, interleaved syncs. Invariants realized (decisions.md "W4"): eventual convergence + idempotence (zero-action/zero-failure fixed point), no silent divergence (byte-identical machine + Drive trees at the fixed point). "No content loss" is that no-silent-divergence plus the §4.5 net — a raw content-set check is unsound (edits/deletes drop content by design); the sound content-supersession model is a recorded deferral. Deterministic replay from seed (pinned clock + monotonic mtimes; concrete Drive ids vary under the concurrent transfer stage but the op scenario and all oracles are id-agnostic).
2. **Oracle additions decided 2026-07-18 — not optional.** — **done.** (a) **Identity stability, scoped to renames the pairing evidence supports** *(scoping corrected 2026-07-18, plan review)*: a rename of an **unmodified** file, or of a directory with surviving paired children, preserves the id and plans zero delete-class actions. Unscoped, the oracle flags decided-correct behavior as failure — the empty-dir rename (delete + create by design, R9) and §4.3's unpaired edit-and-rename remainders; the fuzzer generates the ops, so it knows which renames qualify. **Realized as the quiet-rename probe** — the clean single-writer file rename (quiesce → one machine renames one unmodified file → assert MoveRemote + id preserved + zero delete-class); the fuzzer's content oracle is id-agnostic, so it never false-flags the non-qualifying renames. (b) **§4.5 structural invariant:** no two actions in the **concurrent transfer stage** on one rel_path or an ancestor/descendant pair (shares W1.8.5's assertion, same scoping — serial stages sequence overlapping paths by design). **Realized** as `ValidateTransferStage` on every plan. Rationale: W1.8's A1 converges and loses no content, so the originally specced oracle would have passed it clean.
3. **Op menu must include** the classes that have actually shipped bugs, not just the tidy ones: remote move onto an existing local name (W1.7's R7), same-cycle cross-rename swaps (R6), a new local file under a remotely-moved directory (W1.8's A4), directory renames on both sides (W1.8's A1), fold-colliding names created across machines (W1.9's C2), type clashes at one rel_path (W1.9's C5), and delete/recreate churn against the quarantine (W1.9's C4). — **done with two recorded scope calls** (decisions.md "W4"): **C5 type clashes** and the **deferred directory-arm gaps** (fold-equal *folders*, tracked case-only renames) are **excluded from the convergence oracle** — their correct outcome is a by-design standoff / a known deferral, not clean convergence, so each is covered by its own dedicated regression test (R22; recorded W1.9.1 follow-up) rather than the fuzzer. The status pointer's warning that the dir-arm gap must not leak into W4's oracle is honored: dir names use distinct fold keys and fold variants are files-only.
4. Wire into CI as a bounded-time run; long runs manual. — **done.** The default `go test ./...` run is bounded (8 seeds × 70 steps, ~10s; `-short` → 3 × 35); `SYNCKEEPER_FUZZ_{RUNS,STEPS,SEED,MACHINES,CRASH}` widen it for long manual runs and single-seed replay. The 2 h soak stays coupled to W3 (re-run once the watcher backend changes, spec §16.9).

### W3 — Watcher modularization + FSEvents (spec §10) — `done (2026-07-24)` · ran after W4

Items 1–5 landed and committed. The adversarial check ran first (2026-07-23, two fixes: soak-harness shutdown serialization + FSEvents component-wise ignore filter — decisions.md), then the **2 h FSEvents soak gate PASSED (2026-07-24, code `aefe4fe`):** `--- PASS: TestSoak (7216.68s)`, converged on 16,344 files, clean `doctor` on both machines, no divergence. **W3 is closed.** (Process note: the passing run had to be launched *detached* from the harness task manager — the machine idle-sleeps after 1 min and harness background tasks are torn down when it sleeps; a launchd-reparented process is suspended-and-resumed across sleep instead. Three prior harness-task attempts were killed mid-run by sleep, none a test failure. See decisions.md 2026-07-24.)

1. Extract the `fswatch` module interface from `internal/watch`; current fsnotify backend becomes one implementation; poll-only stays the universal fallback; keep the latch/rebuild belt-and-braces. — **done (2026-07-23).** `fsWatcher` interface in `internal/watch/fswatch.go` (`refresh(root) → failed-count`, `close`); the pump, per-directory `Add`, new-dir watching, and event filtering moved into `fsnotifyBackend`, which wakes the loop through an injected `wake func()` and reads ignore globs through an injected `ignore func() []string`. The loop holds the interface; `startNotifier` is now a thin factory call, `newBackend`/`newNotifyWatcher` are the seams (R15 preserved), the `failureLatch` and rebuild cadence are unchanged. Pure refactor — no behavior change; existing watch tests (incl. R14 reload race, R15 rebuild failure) green under `-race`. Spec §10 dated. See decisions.md 2026-07-23 "W3.1".
2. FSEvents implementation for the primary platform (cgo; directory-tree stream, no per-file descriptors). Native build only — first cgo in the repo, Makefile/CI adjusted. — **done (2026-07-23).** `internal/watch/fsevents_darwin.go` (`//go:build darwin && cgo`): one recursive **file-level** stream on a private serial dispatch queue, delivered to Go by a handle registry (a Go pointer can't ride in the C stream context); `refresh` is a no-op returning 0 (the tree stream auto-covers new dirs, zero per-file fds); ignored paths are filtered (`shouldWake`) before waking, so Finder's constant `.DS_Store` rewrites don't spin the daemon. Selected on darwin+cgo by an `init` override of `newBackend`; the pure-Go fsnotify backend stays the fallback when cgo is off (the file's build constraint drops it) and everywhere else. Makefile: the global `CGO_ENABLED=0` dropped — `build`/`test`/`vet` native (cgo on → FSEvents), `build-all` stays pure-Go cross-compile (fsnotify fallback). Existing watch tests pinned to fsnotify via `TestMain` (R14/R15 stay meaningful); a darwin+cgo test covers the FSEvents wake-up + the filter. Verified: native build+test+`-race` green; `CGO_ENABLED=0` build/test green; all-platform pure-Go cross-compile green. Spec §10 + build policy dated; decisions.md "W3.2".
3. **[W1-scale]** acceptance test: ≥50k-file tree under the daemon, no fd exhaustion; watcher kill → polling degradation → recovery. — **done (2026-07-23).** `internal/watch/scale_test.go` (`TestScale`) + `TestFSEventsScaleNoFDExhaustion`, gated by `SYNCKEEPER_SCALE_FILES` (acceptance = 50000). Measured at 50k: **FSEvents 0/500 dirs unwatchable** (no per-file fds — the point of the backend); **fsnotify 403/500 unwatchable** at the 10240 fd soft limit → the failure latch trips → polling (graceful, not fatal; the full degrade→sync→recover loop is R15's, scale-independent); a full engine sync of **50 500 actions** converges and the second cycle is idle (scan + reconcile handle 50k). The self-inflicted-events question (below) was sized and recorded, not a defect. testing.md W1-scale row now passing; spec §16.8 verified; decisions.md "W3.3".
4. Retire what the fd-exhaustion era no longer needs (rlimit raise stays; per-cycle rebuild cadence re-evaluated per backend). — **done (2026-07-23).** The periodic watcher rebuild is now a per-backend decision: `fsWatcher.needsRebuild()` — fsnotify returns true (kqueue's per-file fd leak is the reason the rebuild exists), FSEvents returns false (a directory-tree stream holds no per-file descriptors, so tearing it down and recreating it every ~6 h was pure waste). The loop's rebuild branch is gated on `fw.needsRebuild()`; when false it falls through to the normal per-cycle `refresh` (a no-op for FSEvents), leaving the stream running. The `rlimit` raise and the polling latch stay (belt-and-braces, spec §10). Tests: `TestRebuildIsPerBackend` (a fake backend via the `newBackend` seam proves a no-rebuild backend is created exactly once — never recreated at cadence — while a rebuild backend is recreated); R15 still exercises the real fsnotify rebuild+degrade+recover path unchanged; the FSEvents wake test asserts the real backend reports `needsRebuild=false`. spec §10 dated; decisions.md "W3.4".
5. **Re-run the 2 h soak on the FSEvents backend (spec §16.9) — THE LAST W3 STEP.** — **done (2026-07-24): PASSED.** `SYNCKEEPER_SOAK_SECONDS=7200 go test ./internal/watch/ -run TestSoak -timeout 10h -v` against code `aefe4fe`: `--- PASS: TestSoak (7216.68s)`, **converged on 16,344 files** (both machines byte-identical), clean `doctor` on both, no divergence, no panic; the 3,488 transient `lstat … no such file` retries (chaos racing the scanner) converged as designed. `TestSoak` runs against the production watcher backend (FSEvents on darwin+cgo, fsnotify elsewhere): `TestMain` captures the platform default before pinning the suite to fsnotify (R14/R15 seams), and `TestSoak` restores it (`newBackend = productionBackend`) so no build-constraint reference to the FSEvents symbol leaks into the cross-platform soak. Two live FSEvents streams under 2 h of create/edit/mkdir/rename/delete chaos on both sides; then chaos stops, watchers cancel, and it settles with explicit syncs until both machines plan nothing twice in a row.

   **Execution note (the gate needed a detached run).** The macOS daily driver idle-sleeps after **1 minute**, and a Claude Code *harness* background task is torn down when the machine sleeps (on battery `caffeinate -s` is also void). Three harness-task attempts were killed mid-run by sleep — **none a test failure** (each clean, `failed=0`, to the sleep instant). The passing run was launched **detached** (`nohup … &`, reparented to launchd) so machine sleep suspends-and-resumes it instead of killing it, plus `caffeinate` while on AC; `-timeout 10h` gives margin since wall-clock can exceed the 2 h of awake-time. For future platform-port soaks, run detached (or keep the machine awake on AC for the whole window). See decisions.md 2026-07-24.

   - **The goal (met):** the FSEvents watcher path is durable under sustained real-world churn — no lost or corrupted files, no wedged daemon, clean convergence. The spec §16.9 release-ritual gate, re-run because the watcher backend changed. **W3 is closed.**

*Carried in from the 2026-07-18 analysis, to size while designing the backend:* every cycle that writes files fires its own fsnotify events, which reset the debounce and trigger another cycle. It converges (the next cycle finds nothing) and is harmless at personal scale, but at ≥50k files it means a full rescan chasing every sync. Worth measuring under the W1-scale test before deciding whether a backend needs to suppress self-inflicted events.

### W5 — Daemon-first polish (spec §1, §8) — `done (2026-07-24)`

1. **`init` offers `service install` at the end (daemon-first onboarding).** — **done (2026-07-24).** After a successful `init`/`init --adopt`, an interactive Y/n prompt (`Start Synckeeper automatically at login?`) installs the login service; when stdin/stdout aren't a terminal it prints a hint instead. `--service`/`--no-service` force the choice (`--no-service` wins if both given). The offer never fails init — a decline, non-interactive run, or install error only prints how to install later. Logic is a pure, injected helper (`offerServiceInstall`, `promptYesNo`, `isTerminal` in `cmd/synckeeper/serviceoffer.go`) so it's unit-tested without touching the OS service manager; the daemon the install starts acquires the instance lock once init exits. Spec §8.4 + §15 dated; MANUAL §1/§2/§3. Test W5.1.
2. **`account` gains the Google email via one `about.get` call (graceful offline).** — **done (2026-07-24).** `driveclient` gains `About(ctx) (About{Email, DisplayName}, error)` — real backend calls `about.get` with a `user(emailAddress,displayName)` fields mask (required by the API), the fake returns a settable `AboutInfo`. `account` builds the client from the stored token and prints the signed-in account under a 15 s timeout; **best-effort** — no token, offline, or an API error prints a short "unavailable" note, never failing the command. Rendering is a pure helper (`printAccountIdentity`) covered by W5.2. Spec §15 dated; MANUAL §3 account row.
3. **Persist-pause-across-restart: decided — kept in-memory.** — **done (2026-07-24; decision, no code).** A daemon restart is rare and explicit, and resuming automatic syncing on restart is the safe default (syncing is the product; a pause that silently survives a reboot is the more surprising failure). No annoyance reported, so the status quo stands. Spec §8.3 dated note; decisions.md 2026-07-24 "W5". Revisit only if it annoys.

### W5.5 — Security & privacy hardening (secrets, sharing, PII) — `done (2026-07-24)`

A standalone pre-rollout security gate (Max's call, 2026-07-24): before real personal files go on real Drive across machines (W6), be sure nothing leaks a key/secret or exposes personal files. **Review done 2026-07-24** (decisions.md "Security review"); the posture is already strong — loopback-only OAuth (`127.0.0.1`) with CSRF `state` + S256 PKCE; `token.json` `0600`, atomic, refused if group/world-readable; control socket a `0600` unix socket (no TCP); **no Drive sharing/permissions code exists — uploads default to owner-private, nothing is ever made public**; no secret/content in any log or stdout; only outbound host is `googleapis` over HTTPS; `.gitignore` covers `token.json`/`credentials.json`/`client_secret_*.json`/`*.db`. Four residual items, each a Max decision:

1. **Embedded OAuth client secret — reviewed & accepted (Max, 2026-07-24). `done`.** Max runs on debug credentials and **considers them safe to publish**, so no rotation/revocation is required and the old "rotate before publication" concern is dropped. The remaining ask — *review how the secret is distributed and loaded* — is done: **distribution** = embedded `var ClientID`/`ClientSecret` (`internal/auth/credentials.go`; compiled into the binary, overridable via `-ldflags -X`) + optional BYO `credentials.json` in the config dir + the ldflags path; **loading** = a single `resolveClient` resolver (BYO wins over embedded) is the only load point; the secret is consumed only by `oauthConfig` to build the OAuth config and reaches only Google's token endpoint over HTTPS during code-exchange/refresh; it is never logged, never printed (`account` shows the client *id* only, via `CredentialInfo` which drops the secret), and never persisted to disk. Conclusion: no leak path beyond the intended binary/source embedding. decisions.md 2026-07-24 "W5.5 item calls". **REVERSED 2026-07-25:** GitHub push protection blocked the embedded secret on the first `git push`. Decision reversed — embedded creds removed (empty `var`s), `credentials.json` now **required** (`ErrNoCredentials` otherwise), and the id/secret scrubbed from **all** git history via `git filter-repo` (Max force-pushes). spec §9 + MANUAL §1/§5/§9 + CLAUDE.md credential rule updated; audit allowlist no longer exempts `credentials.go`. decisions.md 2026-07-25 "REVERSED W5.5-S1".
2. **Folder-scoped token vs full `drive` — investigated 2026-07-24; decision open for Max** (decisions.md "W5.5-S2 investigation"). **No folder/path-scoped Drive scope exists** — scopes are account-wide or per-file. The only narrowing is **`drive.file`** (non-sensitive; per-file access to app-created or Picker-opened files). *Wins:* leaked-token blast radius shrinks to Synckeeper-created files, and it escapes the Restricted-scope verification + security assessment (CASA) full `drive` requires. *Cost:* files added to the Drive folder **outside Synckeeper** (Drive web UI, mobile, other apps) become invisible — they won't download and `changes.list` won't report them, **silently breaking §4's remote-created-file download**; `--adopt` of pre-existing content and plain `init`'s find-existing-folder also break; multi-machine of *app-created* content still works. *Agent recommendation:* keep full `drive` (narrowing contradicts the §4 bidirectional-sync contract and the adopt-based W6 rollout). **Decided (Max, 2026-07-24): keep full `drive`** — `driveScope` unchanged; the broad-scope trade-off is logged as a MANUAL §9 known limitation ("may be revisited in a future version"). `drive.file` stays on record as the future option, gated on an empirical scope-downgrade **retroactivity test** + a spec §4/§9 amendment. **S2 closed.**
3. **Log hygiene — decided & `done` (Max, 2026-07-24): tighten the log to owner-only.** The launchd log (`~/Library/Logs/synckeeper.log`) records synced file *names* (never contents) and launchd creates it `0644` (world-readable on a shared Mac). Fix: the daemon chmods it to `0600` at `watch` startup — `service.RestrictLogToOwner` (best-effort/idempotent; a missing file on a foreground run or non-file-log platform is a no-op). Log *content* is unchanged — paths stay for the owner's own debugging. Existing installs tighten on the next daemon restart. Test W5.5-S3 (`TestRestrictToOwner`, `TestLogPathDarwin`). **S3 closed.**
4. **Pre-publication secret-scan gate — `done` (2026-07-24).** `internal/audit` + `make audit`: a self-contained Go gate (no external tool) asserting (A) no sensitive runtime file (`token.json`/`credentials.json`/`client_secret_*.json`/`*.db`) is git-tracked, (B) `.gitignore` still covers them, (C) no secret pattern (Google OAuth `GOCSPX-`, Google API key, AWS access-key id, Slack token, PEM private-key block) appears in tracked files — only the audit package's own pattern-holding source is allowlisted (the `credentials.go` exemption was dropped 2026-07-25 when the embedded secret was removed; decisions.md "REVERSED W5.5-S1"). It runs inside `go test ./...` too, so drift is caught continuously, not only at release. Test W5.5-S4.

**Acceptance — all met (2026-07-24):** S1 reviewed & accepted; S2 investigated → keep full `drive`, logged as a MANUAL §9 limitation; S3 log tightened to `0600` (owner-only); S4 secret-scan gate green (`make audit`) and wired into the suite. MANUAL §3/§9 updated. **W5.5 closed** — no known secret-or-sharing exposure left before W6.

### W6 — Real multi-machine rollout — `unblocked (2026-07-25) — Max performs it`

Spec §16.10: adopt on a second real machine, a day under `watch`, clean `doctor` on both. Then service reboot check. **The second machine is the Linux box (`pop-os`), so W6 and W7 converge there.** **Max runs the rollout and its validation himself** (decision 2026-07-25) — no agent task under W6; agents own W7's code items and gates.

Rollout sequence (agreed 2026-07-25): binary → `~/.local/bin/synckeeper` (the path is baked into the systemd unit's `ExecStart`) · `credentials.json` in the config dir, `0600` in a `0700` dir (W7-L6) · machine name (W7-L1) · `synckeeper login` · `synckeeper init --adopt` (plain `init` refuses a non-empty folder) · **`service install` last** — installing before signing in is the crash-loop/lock trap already fixed in messaging (worklog 2026-07-25).

### W7 — Second platform (Linux, spec roadmap) — `in progress (2026-07-25)`

**State of play:** the offline suite is **already green on linux/amd64** (`go build && go vet && go test ./...`, verified 2026-07-25 on `pop-os`, Pop!_OS 24.04 LTS, go 1.26.5) — the port is a hardware-validation job plus the short list of platform items below, found by reading the port-relevant code *on* that machine. Facts measured there: **ext4** (case-sensitive, no normalization folding → both probes return `false`, the safe default); `fs.inotify.max_user_watches=127469`, `max_user_instances=1024`; `ulimit -n` 1048576 (fd pressure is a non-issue); systemd 255 user manager, `Linger=no`; COSMIC/Wayland with `xdg-open` (the OAuth loopback flow's browser opener works).

Order: **L6 → L1 → L2 → L5 → L3 → L4 → gates (L8) → docs (L7).** L1/L2/L5/L6 change what `init` asks and what `service install` writes, so they land before Max reinstalls the service; L7 is last because doc claims are claims about *validated* code.

1. **[L1] Machine name: always prompt, OS hostname as the default.** — `not started`. Today `config.Default()` silently takes `os.Hostname()` → `sanitizeMachineName` → `unknown_machine` on failure, so a machine with no usable hostname syncs conflict copies as `unknown_machine` and the user never sees the field. **Decision (Max, 2026-07-25): prompt during `init` on every machine**, pre-filled with the sanitized OS name (`Machine name [pop-os]:`; empty answer accepts the default). Non-interactive runs (no terminal) take the default without prompting — same `isTerminal` seam as W5.1's service offer; `--machine-name <name>` skips the prompt and is the scriptable path. No usable OS name (hostname error, empty, sanitizes to empty) → the prompt has no default, and a non-interactive run falls back to `unknown_machine` **plus a hint naming `engine.machine_name`**. `init --force` over an existing config defaults to the existing name, not the hostname. Answers are sanitized + validated by the existing rules before use. Logic is a pure injected helper (`promptLine` beside `promptYesNo`) so it unit-tests without a terminal. **Adding the flag trips the DOC1 surface guard** → manifest + MANUAL §3 + spec §15 in the same commit. Test W7-L1.
2. **[L2] Daemon log → an owner-only file on Linux (W5.5-S3 parity).** — `not started`. `service.LogPath()` returns `""` on Linux, so `RestrictLogToOwner` is a no-op and daemon output lands in the user journal — which records the same synced file *names* that justified chmod'ing the launchd log to `0600`, readable by root and `adm`/`systemd-journal` members. **Decision (Max, 2026-07-25): redirect to a `0600` file.** `LogPath()` → `$XDG_STATE_HOME/synckeeper/synckeeper.log` (default `~/.local/state/synckeeper/synckeeper.log`); the unit gains `StandardOutput=append:<abs>` + `StandardError=append:<abs>` (systemd ≥ 240; the path is baked at install time exactly as launchd's plist does); the installer `MkdirAll`s the dir `0700`; the existing `RestrictLogToOwner` call at `watch` startup then tightens the file. Consequences: `info`/`status`/MANUAL show a real log path on Linux, and `journalctl --user -u synckeeper` keeps only unit lifecycle. **L2b (flagged, settle at build time): journald's rotation goes away, so log growth becomes ours** — on both platforms (the launchd log is already unbounded). Proposal: rotate at ~10 MB keeping one previous file, checked at daemon start and per cycle. Test W7-L2.
3. **[L3] `needsRebuild()` is per platform, not per backend name.** — `not started`. `fsnotifyBackend.needsRebuild()` hardcodes `true` for kqueue's per-file fd leak (W3.4). inotify holds one fd per *instance* and one watch descriptor per *directory* and leaks nothing per file, so the ~500-cycle teardown/recreate is pure waste plus a small blind window on Linux. Make the answer platform-conditional (Linux `false`; darwin/BSD `true`, where kqueue is the engine and FSEvents only steps aside under `CGO_ENABLED=0`). Test W7-L3, mirroring `TestRebuildIsPerBackend`.
4. **[L4] Name the inotify limit when watches fail, and wake on queue overflow.** — `not started`. **The watching model is settled (Max, 2026-07-25): stay conventional — one inotify watch per directory for the whole tree, exactly as Dropbox and Syncthing do; no watch budget, no partial-watch mode, no config knob** (see decisions.md "Linux file watching stays conventional"). What that leaves:
   - *(L4a)* A failed `fw.Add` on Linux is `ENOSPC` against `fs.inotify.max_user_watches` (a **per-user** budget shared with every other app), not fd exhaustion; the failure latch → polling path already handles it correctly (spec §10), but `status` says "polling" with no cause and the message vocabulary was written for descriptors. Log the cause once when the latch trips — naming `fs.inotify.max_user_watches`, its current value, and the `sysctl` remedy (Dropbox's advice) — and add a MANUAL §7 recovery row. Note our posture is already *better* than Dropbox/Syncthing here: they warn and lose real-time detection, we degrade to polling, keep syncing, and recover automatically.
   - *(L4b)* `ErrEventOverflow` (inotify `IN_Q_OVERFLOW`, a 16384-event queue) is only logged today (`fswatch.go:131`) — an overflow means events were **dropped**, so a big `git checkout` or untar in the sync dir can leave local changes waiting a full poll interval. Treat it as a wake, and count it in status.
   Test W7-L4.
5. **[L5] systemd unit corrections.** — `not started`. Drop `After=network-online.target`: it names a *system* target from a **user** unit, where it is a no-op (and the engine already tolerates offline and retries). Keep `Restart=on-failure` + `RestartSec=10`, and verify the no-credentials crash-loop surfaces through `reportServiceStartup` (`is-active` → `failed` → "not running" + the actionable credentials message) — the launchd `KeepAlive` asymmetry is accepted, not fixed. **Linger: decided (Max, 2026-07-25) — no linger, no offer; on Linux Synckeeper syncs while you are logged in**, stated in MANUAL (§3 service row, §9) with `loginctl enable-linger` named for anyone who wants otherwise. Test W7-L5.
6. **[L6] Config-dir and `credentials.json` permission hardening.** — **done (2026-07-25).** Found 2026-07-25 preparing this rollout: `config.Dir()` applies `0700` only when **it** creates the directory, and MANUAL §5 tells users to place `credentials.json` there *before* first run — so a hand-created config dir keeps the user's umask (observed: `0775` dir, `0664` credentials, i.e. a world-readable client secret), and unlike `token.json` (`internal/auth/token.go:26` refuses group/world-readable) `credentials.json` is never perm-checked. Fixed as planned: `config.tightenDir` chmods an existing directory back to `0700` on every `Dir()` (best-effort — a chmod we may not make is a warning, never a failed command; no-op on Windows), and `auth.loosePerms` makes credential loading **warn** with the mode and the `chmod 600` fix when the file is group/world-readable — a warning, not a refusal, since it is the user's own file and refusing would block onboarding at exactly the wrong moment (deliberate contrast with `token.json`, which `LoadToken` still refuses). Scope addition: **`info` now reports the `credentials.json` mode** beside `token.json`'s and flags a readable file, human + `--json` (`credentials_mode`/`credentials_loose`). Not Linux-specific; surfaced here. Dated notes spec §9/§13; MANUAL §5 `chmod` line + §3 `info` row. Test W7-L6 (red-first: tightening disabled → dir stayed `0755`; warn disabled → empty log).
7. **[L7] Docs, after the gates pass.** — `not started`. MANUAL §1 ("macOS primary today"), §3 service row (per-platform log path + `journalctl` scope), §9's "Linux and Windows planned (code written portably, not yet validated there)", README status line. Also a MANUAL §9 multi-machine line for the newly-real **mixed case-sensitivity** hazard: a case-sensitive machine (ext4) can create `Readme.txt` + `README.txt` in one folder, which a case-folding machine (APFS) collapses — first by file id wins, the rest are skipped and reported (spec §5). Update only once L8 has actually validated the claims.
8. **[L8] Hardware gates on Linux** (spec §16, re-run per platform port). — `go build && go vet && go test ./...` **green (2026-07-25)**; still to run: `-race`; the 50k scale acceptance (`SYNCKEEPER_SCALE_FILES=50000` — expect ≈0 unwatchable directories, ~500 dirs against 127469 watches, making the fd-exhaustion arm a non-event here); and the **2 h soak** (§16.9) against the production backend, which on Linux is fsnotify/inotify. Run the soak **detached** and inhibit suspend (`systemd-inhibit --what=sleep`) — the Linux analogue of the macOS idle-sleep lesson that killed three soak attempts (decisions.md 2026-07-24).

### W8 — Third platform (Windows) — `not started`

Names hardening (reserved names, illegal chars, trailing dots/spaces, long paths), fswatch/RDCW, rename-replace semantics under the journal (F3 on-platform), Task Scheduler, native build, full suite + soak.

### W9 — UI (after CLI is solid, spec roadmap) — `parked (2026-07-27) — W15 is the UI now; reopen if the menu-bar presence is missed`

Tray/menu-bar app as a separate binary on the control socket (mode icon, sync now, pause/resume, open folder/logs); then file-manager badges where the OS has an API. Strictly a client; no sync logic.

**Parked (Max, 2026-07-27)** when W15 was planned: a tray icon and a TUI want the same data for different ergonomics, and W15 delivers the interactive front end — *"like the GUI application but just from command line"*. The tray becomes optional polish, not capability. Reviving it stays cheap by design: **W15-D5's `stat` control payload is specified as the shared client read model**, so a tray would consume it rather than invent a second one. File-manager badges are unaffected by this parking (no API on the primary platform anyway — phase-7 history).

### W10 — Daemon surfaces skips in `activity` (from the 2026-07-24 doc audit) — `not started`

Skipped files (Google-native docs, symlinks, non-regular files, invalid names, Drive same-name duplicates) are reported **only** by a one-shot `synckeeper sync` / `init --adopt`; under the daemon they're invisible. This is the **D1** finding of the 2026-07-24 doc audit (decisions.md), which MANUAL §6 was corrected to state honestly in the meantime. **Decision (Max, 2026-07-24, interactive): schedule it, surfaced as per-skip `activity`-ring entries** — a new direction-neutral `skip` activity kind shown in both `status` recent-activity and `synckeeper activity` — chosen over a `status`-only count.

- **Scope:** the engine already returns `Result.Skips`; the daemon's `cycleDone` (`internal/watch/status.go`) records action counts, guard block, and per-*executed-action* activity but **drops skips**. Thread skips into the activity recorder, add a `skip` kind + its `directionLabel`/render, and the testing row. No sync-behavior change — only a new observability surface (dated note spec §5 / §8.2).
- **Open design detail to settle at build time (flagged, not pre-decided):** the `activity` ring is **capped**, and a permanent skip (e.g. a Google-native file that lives in the folder forever) recurs identically every cycle — re-logging it each cycle would evict real sync history from the ring, the same per-cycle noise spec §5 deliberately made ignore-matches silent to avoid. Likely resolution: emit a skip entry only when a rel_path's skip **first appears or its reason changes** (dedupe unchanged repeats) so the display stays per-skip without flooding the ring. Confirm the dedupe rule with Max when built.
- **Priority:** self-contained, needs no hardware — doable on the primary platform anytime; below the platform ports (W7/W8) in importance.
- **Acceptance:** a daemon cycle with a skipped Google-native file (or symlink) records a `skip` entry visible in `status` and `activity`; recurring identical skips don't flood the capped ring (per the settled dedupe rule). testing.md row lands with the code.

### W15 — `status` becomes a live TUI dashboard (the CLI's UI) — `in progress (2026-07-28)` · U1–U5 done; only U6's final doc pass remains

Max's call, 2026-07-27, planned interactively over three iterations (decisions.md "W15 — `status` becomes the dashboard"): *"I want a new tool. Some sort of cli application that show me in real time what is happening… Like a btop but specific for synckeeper"* → *"I want to change the name top. Let's put it under status. because status will become obsolete with this command"* → *"I want in the new status to also have a section with static information. It should replace at long run the old status and activities. It's like the gui application but just from command line."*

**No new command.** `status` gains the dashboard; the DOC1 manifest **changes a row rather than adding one** (`{"json","watch"}` → `{"interval","json","plain"}`).

| Invocation | Behavior |
|---|---|
| `status`, stdout a TTY | the dashboard |
| `status`, piped/redirected/cron | today's plain dump, one shot — auto-detected, so `status \| grep`, bug-report pastes and scripts keep working |
| `status --plain` | that dump, forced on a TTY |
| `status --json` | **unchanged** — the scripting contract |
| `status --interval <dur>` | refresh rate (default 1 s) |
| ~~`status --watch`~~ | **removed**: a naive first draft of this; two things claiming one job |

**Three views**, persistent header (state · mode · pid · uptime) + footer (keys) + an always-visible identity strip (`machine · sync dir · drive folder`); switch with `1`/`2`/`3`/Tab, `?` for a key overlay:

| View | Content | Replaces (as the *human* surface) |
|---|---|---|
| **[1] Overview** | CYCLE (next poll, last cycle, in-flight), TOTALS (tracked, pending ops, quarantine, bin), ATTENTION — rendered only when non-empty (guard block + reason, last error, no system bin, stale heartbeat, autostart missing, loose `credentials.json`), ACTIVITY tail (~8), WAITING (U5) | `status`'s live half |
| **[2] Activity** | the full 500-entry ring: scroll, filter by kind and direction, search | reading `activity` by hand |
| **[3] Info** | version; every path (`config.toml`, `state.db`, `token.json` + mode, `credentials.json` + mode, `control.sock`, quarantine, system bin, log); sync target + root id; machine name + id; OAuth client; token status; effective config | `status`'s static half; `info` as a thing you *read* |

**The one-shot commands survive as the machine surface** (Max, 2026-07-27): a TUI cannot be piped, so `status --plain`, `status --json` and `activity -n` stay for scripts, cron, bug reports and any future client, with MANUAL §3 marking each row human or scriptable. Retiring `activity` later is a one-line manifest change, not a rewrite.

**Three tiers of data, and only two are cheap** (established by reading the code, 2026-07-27):

1. **Free — DB-backed, cycle-granular.** `statedb.DaemonStatus` + `RecentActivity` + `service.Status()` already carry state/mode/pid/uptime/paused, last sync + `CycleSummary`, next-poll estimate, guard block + reason, last error, tracked items, pending journal ops, quarantine usage, bin destination, autostart, and the activity ring. **Release 1 needs no daemon change at all** and runs against the binaries already installed on both machines.
2. **Needs one new read-only control command (U5).** What the cycle is doing *now* — stage, elapsed, actions done/total, watcher backend name, debounce-armed, the **true** next-tick deadline, a bounded watcher-event ring — lives only in daemon memory. `internal/control`'s doc comment states the current split (*"read-only monitoring goes through the state DB instead"*); U5 amends it deliberately rather than writing per-file rows into SQLite at transfer rate.
3. **Deferred (U7).** Bytes/sec and per-file progress need `googleapi.ProgressUpdater` in `driveclient` plus a progress sink threaded through the executor — the durability-critical path — for a cosmetic gain. Not in this workstream.

**Two honesty findings the design must respect:**
- **`NextPollAt` is an estimate, not a deadline.** It is written as `time.Now().Add(w.Poll)` at cycle *end* (`watch.go:249`) while the real ticker (`watch.go:145`) runs independently and is never reset, and any watcher wake pre-empts it. Tier 1 renders `next poll ≈`; only U5 may render a precise countdown. (`status` prints the same estimate today with the same imprecision — the dashboard must not amplify it into a big ticking clock.)
- **Today's `status` renderer has no automated test.** `gatherStatus`/`printStatusHuman` are referenced only by `status.go`; testing.md's *"CLI render smoke: `status` never-run / running / stale"* row (2026-07-14) names no test, unlike its neighbours and unlike `info`'s renderer (W11). So U1 **characterizes before it changes**.

**Dependency (Max, 2026-07-27): bubbletea + lipgloss** — the repo's first UI dependency, ~8 pure-Go modules, all Windows-capable. Chosen over a zero-dep hand-rolled renderer and over tview/tcell because `Update`/`View` are **pure functions**: every panel state becomes a golden-string test with no TTY, which is how this repo already tests renderers (`printInfoHuman`, `printAccountIdentity`, `systemBinLine`). decisions.md carries the rationale.

**Release 1 = U1–U4 + U6. Release 2 = U5.**

1. **[U1] One read model per fact, and characterization tests first** — **done (2026-07-27)**. Extract `gatherStatus`/`statusView` (`cmd/synckeeper/status.go:81`) into a shared, non-printing gatherer used by `status --plain`, `status --json` and the dashboard, and **reuse `info`'s existing gatherer + pure renderer** (`cmd/synckeeper/info.go`, W11, already tested) for view [3] — so `status`, `info` and the dashboard can never disagree about a path, a default, or what "stale" means. **Lands with golden tests for the plain renderer across never-run / stopped / stale / running / polling-only / paused / guard-blocked / last-error / empty-activity**, written *before* the behavior around it changes, and testing.md's manual smoke row upgraded to a named test. **Guarantee: `--plain` output stays byte-identical to today's** — the new tests pass before and after. New `internal/tui` package: `Snapshot` in, pure `Model`/`Update`/`View`.

   *As built (2026-07-27):* new package **`internal/status`** — `Snapshot` (carrying `Now`, so one frame is internally consistent and reproducible without a clock), `Input` + `Gather` with every machine-dependent read behind a seam (`Now`, `DaemonAlive`, `ServiceStatus`, `SystemBin`, `TokenPresent`, `Quarantine`), `PrintHuman(w, Snapshot)`, `WriteJSON`/`JSONView`, and the shared formatters. The **staleness policy stays in `cmd`** (`3 × watch.HeartbeatInterval`, passed as `Input.StalenessWindow`) so the view package never imports the daemon — `Gather` owns the logic, the caller owns the number. `cmd/synckeeper/format.go` is **deleted**, not wrapped: `Ago`/`Until`/`Dur`/`DirectionLabel`/`SystemBinLine`/`CycleSuffix`/`AutostartText`/`DaemonState`/`QuarantineUsage` now have exactly one definition, with `account`/`activity`/`doctor`/`info`/`sync` calling it (the formatters now take `now` explicitly instead of reading the clock). `TestSystemBinLine` moved with them, unchanged, so testing.md M14.4 keeps its named test. **Byte-identity proven twice:** golden files captured from the *pre-refactor* renderer via a throwaway stdout-capturing generator (deleted in the same commit), and a live end-to-end diff — pre- and post-refactor binaries produce identical `status` **and** `status --json` bytes against the running Mac daemon. `internal/tui` is **not** in this item: U1 is a behavior-free refactor, so `--watch`/`--json` are untouched here and the flag surface changes with U2/U6. testing.md U1.1–U1.4.
2. **[U2] Views, layout, degradation** — **done (2026-07-27)**. The three views, header/footer/identity strip, ATTENTION suppressed when empty. Narrow terminal (<70 col) collapses each view to one column; the identity strip truncates, never wraps. **No TTY → the plain dump, not a refusal** (that is the auto-detect above); an explicit `--interval` on a non-TTY is a named error rather than a silent no-op. TTY detection reuses the existing `isTerminal` seam (`cmd/synckeeper/serviceoffer.go:70`, used this way by `init.go:115`) so it is injectable and testable.

   *As built (2026-07-27):* `internal/tui` (bubbletea v1.3.10 + lipgloss v1.1.0, +14 indirect, all pure Go — `CGO_ENABLED=0` darwin/linux/windows builds green). **Re-slice, recorded:** U2 absorbed U3's basic refresh tick and U6's *surface* half, because the DOC1 guard fails the build the moment a flag moves — so the flag change, the manifest, MANUAL §3 + the new §10, and spec §15 had to land in one commit. U3 keeps the countdown interpolation and the activity scroll/filter/search. **Three real layout bugs the width test caught** (the header didn't truncate; activity rows had a `max(8, …)` path floor that broke the width contract; an info note was appended *after* the value was truncated) — fixed by **dropping columns rather than squeezing them**: an activity row loses the direction label, then the kind; the header drops the clock, then trailing segments, and it composes from *plain* segments so a cut can never land inside an escape sequence. **View [3]** renders `[]tui.InfoRow` built by `cmd`'s `infoRowsForDashboard()` from `gatherInfo()` — `info`'s own gatherer, so no path or default is resolved twice; the TUI resolves nothing itself. `--plain` output is unchanged (U1's goldens still pass) and verified live on the Mac. Read-only by construction is now **mechanical**: an AST guard bans `statedb.Open`, every `os` path mutator, and `trash.MoveToTrash` inside the package, with a planted-violation test proving the guard fails. testing.md U2.1–U2.8.
3. **[U3] The live loop** — **done (2026-07-27)**. 1 Hz DB read (WAL + `busy_timeout(5000)` already make concurrent reads safe against the writing daemon); the countdown interpolated locally at ~8 Hz off an **injected clock** (the `nowFunc` idiom) so it ticks smoothly without hammering SQLite. Activity view: scroll (`j`/`k`/arrows), filter by kind and direction, search. **Unknown activity kinds must render, not vanish** — W10 will add a `skip` kind later, so the kind→glyph map needs a default case.

   *As built (2026-07-27):* **two tickers, not one** — `tickMsg` re-reads the database at `--interval`, `frameMsg` redraws every 250 ms (`FrameInterval`), and `View` measures every relative time from `m.now()` rather than from the snapshot's own `Now`. That is what keeps the countdown and the "2m ago" column moving at `--interval 30s` without querying SQLite four times a second, and it is only sound because U1 made `Snapshot.Now` the single clock a frame reads. Activity view: `j`/`k`/arrows, `PgUp`/`PgDn`/space, `g`/`G`, `f` cycling all → local→drive → drive→local → conflict → **errors** (a kind check, not a `Source` check — a failed cycle's row carries no source), `/` incremental search over path + detail + kind, `c` clears both. The search line **swallows keys** so a query may contain `q`; `Ctrl-C` still quits. Filter and search **intersect**, the heading reports `N of M` and names both, and `f`/`/` from another view jump to the activity view so the result is visible. Offsets are clamped on scroll, on resize, and on every refresh. testing.md U3.1–U3.6.
4. **[U4] Actions over the existing control socket** — **done (2026-07-27)**. `s` sync now, `p` pause, `P` resume, `R` reload — the already-shipped `CmdSync`/`CmdPause`/`CmdResume`/`CmdReload`, so release 1 adds **no** daemon capability. Inline per-action feedback and failure text; a fake control-client seam for tests. **`s` never auto-confirms deletes:** a guard-blocked state shows the reason and the `sync --confirm-deletes` hint as text, and releasing it stays an explicit CLI act — the daemon never self-confirms (spec §8.1) and neither may a keystroke.

   *As built (2026-07-27):* `tui.Actions` is a struct of four `func() error` seams (`cmd`'s `dashboardActions()` fills them with the same `callDaemon` requests `sync`/`pause`/`resume`/`reload` already send — no new daemon capability, no new protocol command). Each key returns a `tea.Cmd` so the call runs **off the event loop**: the control timeout is 30 s and the UI must not freeze for it. One action at a time (the protocol is one request per connection); a second key reports "already asking…". Results land in a self-expiring `notice` line under the header — painted by outcome, 6 s TTL, and an in-flight notice never expires — and the result triggers an immediate refresh so the panels do not wait out the interval. **The safety property is structural:** `Actions` carries no arguments at all, so `--confirm-deletes` is simply unreachable from the dashboard. testing.md U4.1–U4.6.
5. **[U5] Tier 2: a read-only `stat` control command** — **done (2026-07-28)**; the WAITING panel shipped narrowed to a count, and per-stage progress to a stage + planned total (no executor counter). Publishes the in-memory snapshot: cycle stage + elapsed + actions done/total, watcher backend name, debounce-armed flag, true next-tick deadline, bounded (~50) watcher-event ring. **Additive: `ProtocolVersion` stays 1** and an unknown-command reply degrades the dashboard to tier 1, so a new CLI against an older daemon still works (rather than a version bump that makes every mismatch fatal). This is what lights up the **WAITING** panel — *seen on disk, not yet synced* — which is **hidden, not empty, in release 1**: a box that can never fill would read as "nothing changed" when it means "your daemon cannot tell me yet". Amends `internal/control`'s "commands only" doc comment and spec §8.2.

   *As built (2026-07-27) — and what is **not** built:* `internal/watch/live.go` holds a mutex-guarded `liveState` the loop writes at its boundaries (tick fired, wake armed, cycle begin/end, backend changed, poll reloaded) and `LiveSnapshot` is the **exported** wire shape, so the client decodes the daemon's own type and the contract has one definition. `stat` is answered **off the sync loop** — a cycle in flight is exactly when it is worth asking. `ProtocolVersion` stays 1; a daemon that does not know the command answers `ok:false`, which `cmd`'s `fetchLive` maps to "no live detail, daemon still running" — **observed live**: the pre-U5 daemon on the Mac answered `unknown command: stat` and the fallback held. `fetchLive` also doubles as the aliveness probe, so a refresh costs **one** socket round trip instead of two. **Delivered:** the real poll deadline (the `≈` is dropped), `due now` for an elapsed deadline, `local change — syncing` when a wake is pending (it outranks the poll figure), a running cycle on the panel's top line with elapsed time, and the backend named in the header (`watching (FSEvents)` / `polling only`). **The WAITING panel was then narrowed to a count and shipped (2026-07-28, decisions.md "The pending display is a count, not a file list").** Max asked whether the paths version would make file watching heavier or more complex; measured answer: runtime **no** (the path already exists, is already filtered, and `noteWake` already takes a mutex per event, so a ring write would be free), complexity **moderate** (~6 files, but they are `fswatch.go`/`fsevents_darwin.go` and their seams — R14/R15/W3-adv-2/W3-adv-3 territory). Since a **count** needs no interface change at all, that is what shipped: `pendingChanges` increments inside the lock `noteWake` already holds and resets at `cycleBegin`, so it means "changes seen since the running cycle began"; the view reads `local change — syncing` for one and `3 local changes — syncing` for several. **The fswatch interface is unchanged and no per-event work was added.** Documented as a *bound*, not a file count: one save can fire several events and the scanner may find nothing to do for some. **Per-stage progress then landed too (2026-07-28, decisions.md "Per-stage progress: a reporter the engine owns"), narrowed the same way.** `engine.Sync` had **no** per-stage logging at all — not even at `-v` — so this is a new capability rather than a prettier log. Built as `engine.StageReporter`, a type the **engine owns** (`Options.Stage`, nil-safe): a caller-supplied callback would run foreign code inside the sync path, where a reporting panic or block could stop the thing it observes. Stages: `starting → checking Drive → scanning files → planning → transferring → finishing`, worded to name what is being *waited on* — "checking Drive" points at the network, the shape of the `connection reset by peer` cycles in the field. The **planned action total is free** (`len(plan)`); the **done-of-total counter was deliberately not built**, because it would mean instrumenting `executor.Apply`'s worker fan-out — the local-write/journal path — for a display. `Sync` resets the reporter on return, so a finished cycle never renders as a running one. testing.md U5.9–U5.11. **Also fixed on the way:** panel values were painted *before* truncation, so a long value could overflow its column and be cut mid-escape-sequence; `field` now takes the column budget and truncates plain text first (testing.md U5.6). testing.md U5.1–U5.6.
6. **[U6] Docs, manifest, tests — same commit** — `surface half done (2026-07-27, with U2 — the DOC1 guard forces it); each item since has carried its own MANUAL/spec/testing updates. Remaining: one closing reconcile pass over MANUAL §3/§10, spec §8.2/§8.3/§15 and the testing ledger, against the finished U1–U5 surface`. Surface manifest (`status` row rewritten, `--watch` removed); MANUAL §3 rows for `status` / `activity` / `info` marked human vs scriptable + a dashboard section with the key map; spec §15 (CLI surface) and §8.2 (what the daemon publishes); README layout (`internal/tui`) + the dep note; testing.md rows. **`--watch` is a user-visible removal → MANUAL in the same commit**, and the DOC1 guard fails until manifest + MANUAL + spec move together.
7. **[U7] Deferred: throughput** — `not started`, explicitly not in this workstream. Bytes/sec, current file, per-file bars. Revisit only as its own item with its own risk review, since it instruments the transfer path.

**Acceptance:** on a TTY, `synckeeper status` renders all three views live against a running daemon, updates without flicker, and drives sync/pause/resume/reload; piped, it prints today's dump byte-identically; `--json` is unchanged; every view state has a golden test that runs with no terminal; the dashboard holds no instance lock and never writes to or migrates the DB (spec §14 / R5) — pinned by a structural test that `internal/tui` cannot reach a writable `statedb.Open`; a daemon that is absent, stale, dying mid-session, or on a mismatched schema renders as a state rather than an error; `go build ./... && go vet ./... && go test ./... && make audit` green.

**Open details to settle at build time** (flagged, not pre-decided): the plain-flag name (`--plain` vs `--once`); whether the Activity view's filter is modal or incremental; the exact key for the static view if `3` proves awkward next to Tab; and the ATTENTION panel's ordering when several conditions hold at once.

### W14 — The mass-delete guard becomes a *recoverability* guard — `done (2026-07-26)`

Max's call, 2026-07-26 (decisions.md "The mass-delete guard becomes a recoverability guard"), after the guard blocked an intentional folder deletion on both machines: *"I don't like this rule. I think we should remove it and keep it only in case the BIN is not available so the deletion is permanent and not just a move into the bin. In that case this needs to be notified."* The guard's premise was **volume** — deleting a lot is suspicious. The new premise is **recoverability** — a deletion the user can undo from a bin they can see is not an emergency, however large; a deletion that lands somewhere they cannot see is, however small.

**What the guard costs today.** The trigger is `>10` delete-class files **and** `> mass_delete_threshold` (0.25) of tracked files, counted in both directions. With a sync folder holding one big folder and little else, *any* deletion of that folder crosses it, on every machine, every time — friction with no information content, and W13 already made the local side a single restorable bin entry.

**The one thing it uniquely covered — stated, and accepted by Max.** The guard is the last line of defense against a deletion **the user never asked for**: an engine bug, a half-finished `init --adopt`, a corrupt baseline, a subtree that vanished because a mount went away under it (W12's shape). After this change such a plan executes. Recovery is then bounded by *someone else's* clock — Drive's bin auto-purges at ~30 days, and both GNOME and macOS can be set to empty the trash after 30 days — where the guard's window was unlimited. Two design items below exist to buy that back cheaply: **M3** (a large deletion is *reported* loudly instead of blocked, so it is noticed inside the window) and **M4** (a locally-deleted folder becomes **one** entry in Drive's bin, so undoing it is one gesture rather than 1145). `CheckSyncDir` — sync dir missing / unreadable / empty-with-tracked-rows — is a **different guard and stays exactly as it is**: an unmounted disk is never a deletion.

1. **[M1] The guard fires on destination, not on volume** — **done (2026-07-26)**. `guards.CheckMassDelete` takes the destination facts and counts only deletions that would land somewhere the user cannot restore from:
   - **`TrashRemote` (local delete → Drive): never counted.** Drive's bin is user-visible and restorable; this direction stops being guarded at all.
   - **`QuarantineLocal` (remote delete → this machine): counted only when the system bin is unavailable** (`trash.Available() == false` — an unsupported platform, a build without it, a trash directory that cannot be created). Then the content goes to the private dated quarantine, which the user does not see and which self-purges after `quarantine_retention_days`, so the old ratio rule applies unchanged (`>10` absolute **and** `> mass_delete_threshold`, files only, `SubtreeFiles` counted for a collapsed folder — W13-T2).
   - Everything downstream keeps working as it does today for the case that remains: the interactive one-shot aborts with the reason and the `--confirm-deletes` hint; the daemon strips only the delete-class actions, syncs the rest, and surfaces the block in `status` (spec §8.1).
   - Signature: the engine passes the availability it already computes for the collapse (`Result.TrashAvailable`), so plan-time and execution-time agree on one fact from one source.
2. **[M2] An absent bin is a standing, visible state — not a log line** — **done (2026-07-26)**. This is the half of the deal that makes M1 safe:
   - **`status`** gains a `system bin:` line — the destination when present, and when absent a prominent `UNAVAILABLE — deletions are rescued to <quarantine path>; mass-delete confirmation required` (it is the reason the user will be asked to confirm, so it belongs beside the guard line).
   - **`doctor`** reports it as a checked condition; **`info`** already prints `system bin` (W13-T5).
   - The daemon **logs it once at startup**, not per cycle, and records a distinct `activity` entry the first time a cycle actually falls back to the quarantine (per-item trash failures included — those are invisible to `Available()`).
3. **[M3] A large deletion is reported, not blocked** — **done (2026-07-26)** *(agent-proposed; the compensating control for the accepted risk — drop it and the change is still coherent, just quieter)*. When a cycle deletes more than `mass_delete_threshold` of tracked files **and** the bin is available: execute it, and make it impossible to miss afterwards — a `WARN` log with the count and destination, a distinct `activity` entry (`deleted 1117 files → system bin`), and a `status` field that survives a few cycles (`last large deletion: 1117 files → system bin, 2026-07-26 13:05 — restore from your bin`). The user still finds out inside the bin's retention window; they simply are not asked for permission.
4. **[M4] Remote deletions collapse too — the mirror image of W13-T2** — **done (2026-07-26)** *(agent-proposed; recommended, and it also pays for itself in API calls)*. A locally-deleted folder currently emits one `TrashRemote` per file **plus** one for the folder: 1145 Drive API calls, and 1145 loose entries in Drive's bin. Trashing the **folder** alone is one call, one restorable bin entry, and exactly the semantics the other machines already handle correctly (a trashed folder prunes its subtree from the mirror — R26, W12).
   - Same rule as the local collapse: a directory's `TrashRemote` absorbs the subtree when **every** planned action under it is delete-class, and additionally when the **remote mirror** holds nothing under that folder the plan did not account for (the remote analogue of `verifySubtree`, read from `remote_nodes` rather than from a walk — no extra API calls).
   - Carries `SubtreeFiles` for the same counting honesty, and one `activity` entry with the count.
   - Ordering: the folder's trash must be the *last* action of the subtree, as it is today (deletes are sorted deepest-first — the collapse drops the children, so the folder action simply stays where it is).
5. **[M5] Compatibility, deliberately unbroken** — **done (2026-07-26)** (no code change needed: the key and the flag both stayed). `mass_delete_threshold` **stays a config key** — `config` rejects unknown keys, so removing it would break every existing `config.toml` on disk — with its meaning narrowed and documented: it governs the no-bin guard (M1) and the report threshold (M3). `--confirm-deletes` stays on `sync`, needed for the no-bin case. **No command or flag is added, renamed, or removed, so the DOC1 surface manifest is untouched.**
6. **[M6] Docs, same commit** — **done (2026-07-26)**. spec **§3 invariant 4 amended** — first sentence becomes recoverability-based, the "sync dir missing/unreadable/empty" sentence stays **verbatim**; §6 (guards) rewritten; §8.1 (daemon defer) narrowed to the no-bin case; §15/§16 rows. MANUAL §6 mass-delete bullet rewritten (it was extended 2026-07-26 to describe the *old* rule), §7 recovery row, §4 config comment. README unaffected (no layout/build change) — check and say so.
7. **[M7] Tests — the existing guard rows change meaning, so they are rewritten, not deleted** — **done (2026-07-26)**, testing.md M14.1–M14.8. `G1`, `G3`, `R10`, `T13.5`, and W12-F2's `TestMassDeleteGuardBlocksWholeTreeDeletion` all pin "a big deletion is blocked"; each is re-targeted to the no-bin case and joined by its opposite. New rows:
   - **M14.1** bin available + a whole-tree remote deletion → **executes**, one bin entry, no guard block, `status`/`activity` report it (M3).
   - **M14.2** bin unavailable + the same deletion → blocked with the reason, nothing executed, local files untouched; `sync --confirm-deletes` then clears it and the content lands in the quarantine.
   - **M14.3** a whole-tree *local* deletion is never blocked (Drive's bin is the destination), whatever the ratio.
   - **M14.4** `status`/`doctor` name an unavailable bin (M2), and a per-item fallback to the quarantine records its activity entry.
   - **M14.5** M4: a locally-deleted folder produces **one** `trash_remote` for the folder, no per-child calls, `SubtreeFiles` set; a subtree holding a remote item the plan did not account for refuses the collapse and trashes item by item.
   - **M14.6** the counting stays honest through both collapses (the M1 guard still counts `SubtreeFiles` in the no-bin case).
   - `CheckSyncDir`'s rows (G2, F5) must keep passing untouched — that is the regression that proves the *other* guard survived.

**Acceptance — met (2026-07-26):** deleting a folder in the Drive web UI removes it from every machine **without confirmation**, leaving one restorable entry in each machine's bin, with the deletion reported in `activity`; deleting a folder locally leaves **one** restorable entry in Drive's bin (**one** API call, asserted); on a machine with no system bin the same remote deletion is held back with its reason, `status`/`doctor` say why, and `sync --confirm-deletes` clears it into the quarantine; `CheckSyncDir` untouched; `go build ./... && go vet ./... && go test ./... && make audit` green.

*As built, two notes:* (a) **M3's `status` field** is the activity ring plus the cycle counters rather than a new `daemon_status` column — the ring is the durable record (500 entries) and adding a column would have meant a schema migration for a report; (b) the guard's **signature** gained `binAvailable bool`, fed from the same `Result.TrashAvailable` the collapse uses, so plan-time and execution-time can never disagree about where a deletion is going.

### W13 — Remote deletions go to the OS trash (replacing the local quarantine) — `done (2026-07-27)`

Max's call, 2026-07-25 (decisions.md "Remote deletions go to the OS trash"): a private dated quarantine inside the config dir is a copy the user did not make, in a place they never look — *"it's still on my file system and not marked in any way whatsoever."* A deletion arriving from Drive must move the local file/folder to the **operating system's trash**, which the desktop already shows and can restore from. **Both currently supported platforms ship together — macOS and Linux** (Max, 2026-07-25); Windows follows with W8. **Now the active workstream** — W12 is parked (Max, 2026-07-25) after its blast radius proved nil and every reproducible path came out correct.

**Scope is exactly the `QuarantineLocal` action.** It is emitted at two sites only — `plan.go:317` (a tracked file whose remote was deleted) and `plan.go:586` (the same for a directory) — and both are remote-initiated deletions, so the whole behavior change is confined to that action. The executor's *other* uses of the quarantine directory (conflict rescue copies, `uniqueRescueDest`, `sweepInvisibleLeftovers`) are a different code path and are **not** touched.

1. **[T1] New `internal/trash` package — the OS module** — **done (2026-07-25)** (spec §10 gains its `trash` row with T4, when the behaviour is wired).
   - API: `MoveToTrash(path string) error`, `Available() bool` (capability probe, cached), `Describe() string` (for `info`/`status`, e.g. "freedesktop trash at ~/.local/share/Trash"). Sentinel `ErrUnavailable` so the executor can fall back deliberately rather than on a string match.
   - **Linux** (`trash_linux.go`, pure Go — freedesktop.org trash spec): move into `$XDG_DATA_HOME/Trash/files/` (default `~/.local/share/Trash`), write the paired `info/<name>.trashinfo` (`[Trash Info]`, `Path=` percent-encoded absolute path, `DeletionDate=YYYY-MM-DDThh:mm:ss`). **Claim the name by creating the `.trashinfo` with `O_CREATE|O_EXCL`** — that is the spec's own atomic collision protocol — then uniquify (`name.2`, `name.3`) on `EEXIST`. **Cross-filesystem:** compare `Stat_t.Dev` of the file and the home trash; when they differ the move would be a copy, so use the file's own volume — `<mount>/.Trash-$uid/{files,info}`, created `0700` if absent (and the sticky-bit-checked `<mount>/.Trash/$uid` form when that exists), with `Path=` written relative to the volume root per the spec. Never fall back to a cross-device copy: that is a data move we do not control the atomicity of.
   - **macOS** (`trash_darwin.go`, `//go:build darwin && cgo`): `NSFileManager trashItemAtURL:resultingItemURL:error:` via `#cgo LDFLAGS: -framework Foundation`. Finder's own API, so "Put Back" works and per-volume `.Trashes` handling is the OS's problem, not ours. Second cgo file in the repo, same constraint shape as FSEvents.
   - **Fallback** (`trash_other.go`, `!linux && !(darwin && cgo)`): `Available() == false`, `MoveToTrash` returns `ErrUnavailable` — keeps `CGO_ENABLED=0` darwin builds and every future platform compiling and correct.
   - *As built:* all four files exist as specified. Two refinements: collision numbering is inserted **before the extension** (`notes.2.txt`) to match the desktop's own trasher rather than appended, and the cross-volume device comparison sits behind a `deviceOf` seam so that branch is testable without a second filesystem. **Verified on real hardware:** an item trashed through the module appears in the Linux desktop's trash (`gio trash --list`) with a working restore path. **The darwin path is verified (2026-07-26, macOS/arm64, go 1.26.4):** first compilation anywhere was clean — no cgo or linker change needed — `TestMoveToTrashDarwinMissing`/`TestAvailableDarwin` pass and the opt-in `SYNCKEEPER_TRASH_TEST=1 TestMoveToTrashDarwin` put a real file in `~/.Trash` with the source gone (T13.10). 7 Linux tests + 3 darwin; `CGO_ENABLED=0 GOOS=darwin` and `GOOS=windows` builds green.
2. **[T2] Granularity: trash the highest fully-deleted directory** — **done (2026-07-26)** *(rule agreed with Max, 2026-07-25)*. A 1145-file folder must produce **one** bin entry, not 1145.
   - Collapse pass over the delete stage: a directory action absorbs its descendants when **every** planned action under it is delete-class **and** the local subtree holds nothing the plan did not account for. The emitted action carries the subtree it stands for; the covered descendant actions are dropped.
   - **Execution-time re-check, inside the write gate:** before the move, walk the directory and refuse the collapse if anything unexpected lives there (an untracked file the user created, a non-ignored survivor) — falling back to per-item so nothing is trashed that the plan never reasoned about. Ignored/temp leftovers travel with the directory exactly as `sweepInvisibleLeftovers` does today (R20).
   - **Mass-delete guard interaction — the known trap** *(this is A2's mirror image, W1.8.3: counting containers instead of content wedged the daemon once already)*: `guards.CheckMassDelete` counts delete-class **files**, so collapsing 1145 file deletions into one directory action would make the guard count **0** and silence it exactly when it matters most. **Run the guard before the collapse pass**, and carry a `SubtreeFiles int` on the collapsed action so any later counting stays honest. A regression test pins this specifically.
   - *As built (2026-07-26):* `reconcile.CollapseDirDeletes` (pure, `internal/reconcile/collapse.go`), called by the engine **after** `guards.CheckMassDelete` and only when the bin is available (no bin → today's per-item quarantine, unchanged). The action carries `Subtree []SubtreeEntry` (rel_path, is_dir, file id, and the scan's size/mtime for each covered file) plus `SubtreeFiles`. Two additions to the plan: the guard **also** counts `SubtreeFiles` for a directory delete, so the ordering can never silently become load-bearing again (an uncollapsed directory still counts zero — R10 intact); and the execution-time re-check is exact rather than a count, since the carried stats let it enforce "edit beats delete" for every absorbed file. On any refusal the executor deletes the subtree item by item, as specified. T13.11/T13.12/T13.5/T13.6 passing.
3. **[T3] Executor + local-write gate** — **done (2026-07-26)**. `quarantineLocal` becomes `trashLocal`: a new primitive in `internal/executor/localwrite.go` (spec §7 exclusivity — the gate file is the only place allowed to mutate a local path), stating the same expectation as today (`expFromAction`) so an edit landing between scan and execution still wins the cycle. Order: journal the op → move to trash → drop the baseline rows → clear the journal, keeping invariant 6 (crash at any point leaves a state the next run repairs). **On `ErrUnavailable` or any trash failure, fall back to today's quarantine path** and record which happened — invariant 3 never degrades to a hard delete.
4. **[T4] Baseline rows for a collapsed subtree** — **done (2026-07-26)**. One action now retires many rows: ~~add `statedb.DeleteItemsUnder(relPath)`~~ → **`statedb.DeleteItemsByID(ids)`**, batched at 400 per statement. Rows are retired by **file id, not by rel_path prefix**: a delete's rel_path can be a post-move local path (`plan.go:586` emits the rewritten path) while its rows are still keyed at the baseline path, so a prefix delete would miss them — and could reach rows the action never covered. Ids are unambiguous both ways. Recorded in decisions.md.
5. **[T5] Visibility** — **done (2026-07-26)**. `status`/`activity` report one `trash` entry per subtree with its file count ("moved 1145 files to the system bin") instead of a wall of per-file lines, and `info` shows the trash destination via `Describe()`. Adding a field to the JSON views only — no new command, so the DOC1 surface guard is untouched.
6. **[T6] Docs + tests, landing with the code** — **done (2026-07-26)** (spec §3/§4.2/§10, MANUAL §6/§7/§3, README layout, testing.md T13.4–T13.15 + R13/R20/R21 restated). spec §3 invariant 3 amended (destination moves to the OS trash; "never permanent" unchanged), §10 `trash` row, §4.2 note on the collapse; MANUAL §6/§7 rewritten around "deleted in Drive → your system bin" with the quarantine described as the fallback and its remaining jobs; testing.md rows:
   - **T13.1** remote folder delete → **one** entry in the OS trash, the sync dir clean, second cycle idle (Linux: `.trashinfo` present, `Path=` decodes to the original absolute path; macOS: the item is in `~/.Trash`).
   - **T13.2** name collision in the trash uniquifies instead of overwriting (the `O_EXCL` claim).
   - **T13.3** cross-filesystem sync dir selects `<mount>/.Trash-$uid` — unit-tested through an injected device-id/mount seam, so it runs without a second filesystem.
   - **T13.4** unavailable trash falls back to the quarantine directory and reports it; nothing is hard-deleted.
   - **T13.5** the mass-delete guard still counts a collapsed subtree's files (the A2-mirror regression).
   - **T13.6** an unexpected survivor in the subtree refuses the collapse and falls back to per-item.
   - **T13.7** crash mid-trash (executor checkpoint) converges on the next run.

**Acceptance:** deleting a folder in the Drive web UI removes it from every machine and leaves exactly one restorable entry in each machine's system bin; the quarantine directory stays empty in normal operation; `make test` green on both platforms, plus the `CGO_ENABLED=0` build.

**Status — CLOSED (2026-07-27).** T1–T6 landed 2026-07-25/26; the suite, `make audit`, `-race` on every touched package, and the `CGO_ENABLED=0 GOOS=darwin` / `GOOS=windows` builds are green on both machines. The macOS compile/run (T13.10) passed on the Mac 2026-07-26 — the darwin cgo path compiled clean on its first compilation anywhere. **The on-hardware acceptance is met on both platforms (T13.17):** Linux 2026-07-26 (a 1117-file folder trashed in the Drive web UI removed in one cycle, one restorable desktop-bin entry) and macOS 2026-07-27 (**Max: "I tested the linux and mac machine and can confirm that all works as expected"**). Windows owes the same acceptance with W8's recycle-bin backend.

### W12 — Field incident: a Drive-bin delete came back as a re-upload (Linux, 2026-07-25) — `parked (2026-07-25) — reopen only if it recurs`

Found during Max's own W6 rollout on `pop-os`, hours after adoption. **Parked at Max's call (2026-07-25)** after the investigation established the blast radius was nil and every reproducible path came out correct: *"I don't want to discuss this bug anymore. We will discuss it eventually if it come back."* The MANUAL §8 entry stays so a user hitting it recognises it; **reopen if it recurs** — the unanswered question and the two experiments left to run are recorded below. Full evidence and reconstruction in decisions.md 2026-07-25 "Field incident".

**What happened.** A tracked folder (`kenney_ui-pack-space-expansion`, ~1145 files) was moved to the bin in the Drive web UI. The Linux daemon removed nothing locally; ~4 minutes later, after the user deleted the folder from the Linux filesystem by hand, one cycle planned **1145 actions including uploads** — 890 files were **recreated in Drive**, 255 failed with `stat …: no such file or directory` (the write gate correctly refusing files that had vanished), and the next cycle trashed the 890 again. The macOS machine propagated the same deletion correctly (`drive→local quarantine …` at 19:05:13). End state converged; copies survived in Drive's bin and the Mac's quarantine.

**Two distinct defects:**

1. **[F1] The real defect — a remote deletion that produced no plan, plus 255 upload attempts.** Two facts survive F2's analysis. **(a) Between the Drive-bin deletion (~18:50) and the user's own local delete (18:54), every cycle planned nothing** — 1145 tracked files whose remote had just been trashed should have planned 1145 `QuarantineLocal`s and hit the guard (exactly what `TestMassDeleteGuardBlocksWholeTreeDeletion/trashed_in_drive` now pins). Silence means the engine never saw those rows as remote-deleted: either the mirror did not reflect the folder trash for the subtree, or the rows were already gone. **(b) 255 files were planned as uploads**, i.e. judged untracked-new — their baseline rows were absent while the files were still on disk. **Leading suspect for both:** the `init --adopt` was still running when `service install` first started the daemon (journal 18:46:37 `another synckeeper instance is running (lock)`, restart 18:46:48); an adopt interrupted after downloading part of the tree but before recording it leaves exactly this shape. R26 proves the plain path is correct, so the trigger is state-specific. **Narrowed further 2026-07-25** by `TestRemoteTrashDuringPartialLocalDeletePlansNoUpload`: the incident's exact race (Drive bin entry 18:52, `rm -rf` still running when the 18:54:30 cycle scanned) is handled **correctly** — tracked rows split into `forget` (gone both sides) and `quarantine_local` (still on disk), never an upload. So the field's 255 upload-planned files **had no baseline rows**, while their 890 siblings did. The engine's behaviour is correct given that state; **the question is now only how 255 files came to be on disk untracked.** The journal shows no daemon cycle handling the tree at all before 18:54:32, so the tree was uploaded/downloaded by the foreground instance that held the lock at 18:46:37 — whose output went to Max's terminal, not journald. **Confirmed with Max:** the command was `init --adopt`. **Unanswered when parked:** whether the folder was already inside `~/Synckeeper` when adopt ran (adopt *uploaded* it) or came down from Drive (adopt *downloaded* it), and what that run printed — its output went to the terminal, not journald. **If it recurs, the two experiments to run are:** (1) an `init --adopt` interrupted between transfer and commit, then a Drive-bin delete against the resulting state; (2) the same with the login service starting mid-adopt (the journal's 18:46:37 lock rejection).

   *One piece of evidence only Max can supply:* whether the Drive bin holds **one** batch of the tree (his own deletion, ~18:50) or **two** (a second batch trashed ~18:55). The activity analysis says one.
2. **[F2] The mass-delete guard never fired — investigated 2026-07-25, `not a defect`.** The guard is correctly wired (`watch.go:163` sets `DeferMassDelete`) and fires at incident scale in **both** directions, now pinned by `TestMassDeleteGuardBlocksWholeTreeDeletion` (60 of 61 tracked files: `GuardBlocked` set, zero actions executed, local files untouched, a reason recorded for `status`). **Therefore the incident's cycles contained no delete-class actions at all** — and the `activityKind` mapping identifies what they were: `Record` and `Forget` are the only kinds that produce **no** activity entry, and the incident's 890+890 executed actions produced **zero** activity rows. **This corrects the first reading: nothing was re-uploaded to Drive.** The 890 executed in the 18:54:32 cycle were `Forget`s (rows for content gone on *both* sides — the correct action); the only upload attempts were the 255 that all **failed** at the write gate. Blast radius: nothing written to Drive, nothing lost locally.

**Method:** F2 first (offline, fast), then F1 (reconstruct the interrupted-adopt state). Red-first regression tests for both, then the fixes, then testing.md rows; MANUAL §8 carries the user-facing entry until they are fixed (added 2026-07-25).

### W11 — `info` command: one-shot static environment / paths dump — `done (2026-07-25)`

A read-only `synckeeper info` that prints everything static and useful in one place — every config-file path, the effective config, identity, and version — so a user (or a bug report) gets the whole picture without hunting across `status`/`config`/`account`. **Decision (Max, 2026-07-25): a separate `info` command**, not folded into `status` (which stays focused on live daemon state). The companion "print the manual" command is **deferred** (Max: skip for now).

**Shows** (offline — no network; point to `account` for the live Google email):
- **version** (binary `main.version`).
- **paths**: config dir; `config.toml`; `state.db`; `token.json` (+ present / perms); `credentials.json` (present?) and the active OAuth client (source + client id via `auth.CredentialInfo`); `control.sock`; `quarantine/`; log file (`service.LogPath`, darwin).
- **sync target**: sync dir (expanded); drive folder name + root folder id.
- **identity**: machine name + machine id.
- **effective config summary**: poll interval, mass-delete threshold, quarantine retention, ignore globs (the full TOML stays in `config`).
- **local state**: tracked items, pending ops, quarantine usage (files/bytes), token status (present / expiry); a one-line daemon-state pointer (live details stay in `status`).

**Behavior**: read-only (no instance lock, like `status`/`config`/`account`); works **before init** (prints the config dir + a "run `synckeeper init`" hint and whatever exists) and after; **offline** (no `about.get`); `--json` for scripting / a future UI (mirror `status`'s human + JSON split).

**Implementation**: new `cmd/synckeeper/info.go` (`newInfoCmd` with `--json`), registered in `newRootCmd` — which **trips the DOC1 surface guard** until the manifest + MANUAL §3 + spec §15 are updated in the same commit (the guard working as designed). Reuse `config.Dir`, `statedb.Path`, `auth.TokenPath`/`CredentialInfo`, `service.LogPath`, `cfg.SyncDir`, `db.GetMeta`/`ItemCount`/`PendingOpCount`, and `quarantineUsage` (factor out of `status.go` if shared). Gather into an `infoView`, render human + JSON like `status`.

**Acceptance — met (2026-07-25):** `synckeeper info` shows every path / identity / config / state field above, before and after init, offline, with a valid, complete `--json`. `cmd/synckeeper/info.go` (`newInfoCmd`, gather + human/JSON render); tests `TestPrintInfoHuman` / `TestPrintInfoHumanNotInitialized` / `TestPrintInfoJSON`; `info` added to the DOC1 surface manifest; MANUAL §3 row + spec §15 updated in the same commit. **W11 closed.**

**Deferred companion**: a `manual` command (embed MANUAL.md via a build-time copy + a drift test; the `manual` name avoids cobra's built-in `help`) — considered, **skipped for now** (Max, 2026-07-25); revisit if wanted.

## Working process

1. A workstream item is done when its code, tests, and doc updates land together; check items off here with a date **and rewrite [status.md](status.md)** (last/current/next) before ending the session.
2. Every scope change, spec deviation, or non-obvious choice gets a dated entry in [decisions.md](decisions.md) before or alongside the change.
3. [testing.md](testing.md) rows are the acceptance ledger — add the row with the feature, not after.
4. README build/run instructions stay correct at every commit.
5. The spec is kept current: behavior changes edit spec.md in the same change, marked with a dated note.
6. **`MANUAL.md` (project root) is the end-user manual and ships true at every commit** *(rule added 2026-07-18)*: any user-visible change — commands, flags, config, defaults, behavior — and any known-bug status change (found or fixed) updates it in the same commit. A fix that closes a Known-bugs entry removes that entry in the fix's own commit.

## Key risks

| Risk | Mitigation |
|---|---|
| FSEvents semantics (coalescing, event replay, volume boundaries) subtler than expected | Hints are wake-ups only (spec §8.1) — correctness never depends on the watcher; poll fallback; W3 scale test |
| First cgo in the repo complicates builds | Native-build policy (spec §10); cgo confined to the fswatch module; pure-Go fallback compiled everywhere |
| Fuzzer flakiness / nondeterminism | Seeded runs, deterministic replay, bounded CI time — **realized 2026-07-23**: a pinned `nowFunc` clock + monotonic mtimes remove the two nondeterminism sources (wall-clock conflict names, edit-detection); the atomic one-shot crash latch is `-race`-clean. Concrete Drive ids still vary under the concurrent transfer stage, but the op scenario and every oracle are id-agnostic, so failures reproduce from the seed |
| Ordering fixes regress existing scenarios | R1/R2 land as reconcile-level tests plus full S/F suite; dependency rules are spec-normative now (§4.5) |
| **Manual review keeps finding engine bugs the suite misses** (W1 → W1.7 → W1.8 → W1.9, four rounds — all now closed) | W4 moved ahead of W3 with a strengthened oracle (identity stability + §4.5 structural invariant); the local-write gate makes one bug class mechanically impossible rather than review-dependent. **W4 landed 2026-07-23 and its first run found R25** (a crashed/coincident-move wedge the four review rounds missed), validating the reorder — the fuzzer now guards this class continuously |
| **A1's fix touches the most load-bearing code in the repo** (pass 2 pairing + pass 4 resurrect rules interact) | Red-first reproduction at reconcile *and* engine level; full S1–S8 + R1–R8 suite must stay green; W4's fuzzer re-checks it with directory renames in the op menu |
| Drive API quota under fuzz/soak | Fake backend for both; live smoke stays small and env-gated |
