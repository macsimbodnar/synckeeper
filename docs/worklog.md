# Work log

Append-only, chronological record of **what agents actually did in this repo, keyed by the prompt that triggered it** — the console-transcript equivalent, kept as the project's permanent memory. The `## Baseline` recap stays pinned at the top as the starting point; every prompt after it appends one entry at the **bottom** of `## Log`. **Never rewrite, reorder, or delete past entries** — this file is the audit trail, not a summary.

## How to keep this file (the rule)

- **One entry per user prompt** (or per autonomous work session), appended at the end of `## Log`.
- Entry header: `### YYYY-MM-DD HH:MM — <one-line gist>`, then the prompt quoted verbatim (lightly trimmed only for length, marked with `…`).
- Under it, **one short sub-bullet per meaningful action or decision** — precise enough to reconstruct what happened, not a narration. Name the files, tests, and commits touched. Read-only/answer-only prompts get an entry too (note "no repo changes").
- Append as you work, and **commit it** — its own `docs: worklog …` commit, or folded into the work's commit.
- This log is granular history; it does **not** replace `status.md` (state pointer), `decisions.md` (the why), or `testing.md` (acceptance). Per-commit truth still lives in `git log`.

---

## Baseline — recap of work since project start (2026-07-06 → 2026-07-22)

*Written 2026-07-23 as the starting point. Summarized, not per-prompt; commit-level detail is in `git log --reverse`, the why is in `docs/decisions.md`.*

**Phase system 0–7 (retired to `docs/history/`).** Seeded project docs (spec, plan, phase checklists, test matrix). Phase 0: skeleton — config, OAuth loopback, SQLite state DB, Drive client, `init`/`status`; live OAuth verified, real credentials embedded. Phase 1: one-shot bidirectional sync. Phase 2: safety hardening. Phase 3: continuous mode (fsnotify) — a 2h soak found fd exhaustion; fixed, plus remote-cache prune and polling latch on watch failure. Phase 6: daemon monitoring via DB heartbeat (no IPC) + control socket (sync/pause/resume/reload). Phase 4: `init --adopt` multi-machine first-merge. Then: replan macOS-first, activity direction, case-collision safety on case-insensitive filesystems, abandoned the Finder-sidebar item (no macOS 14 API), added `login` re-auth.

**Doc/process restructure (mid-July).** Spec rewritten platform-agnostic; work re-organized as workstreams W1–W9; repo restructured as permanent inter-agent memory; retired phases archived. Durability invariant 7 (dependency-aware plan ordering) recorded after an ordering bug shipped.

**W1 correctness fixes (R1–R8/G3).** R1: conflicts never depend on moves; `ProtectedBy` propagation (data-loss fix). R2: plan stage order = dir-moves → mkdirs → file-moves (livelock fix). R3: `init --force` rebuilds the remote mirror, not just token reset. R4: downloads refuse a rename when the target drifted since scan. R5: read-only commands open the state DB without migrating. R6: protect-and-verify records (the swap test disproved "self-heals"). W1.7 (R7/R8/G3): move/backup overwrite guard + daemon defers mass delete.

**W2 cleanup & config.** Removed dead `full_rescan_interval_secs` knob; Unicode NFC/NFD normalization folding (spec §5); refreshed `config` reload hint + cleared gofmt drift; bring-your-own OAuth client via `credentials.json` (embedded default kept); README/Makefile build-policy consistency.

**Adversarial rounds 2–3 + docs.** Round-2/round-3 code-review fix plans; **MANUAL.md created** (end-user manual, new rule: kept true every commit); README/MANUAL consolidation (one owner per audience); roadmap reorder; W4 oracle scoped to the concurrent stage.

**W1.8 — the local-write gate + hardening (9 items).** 1: local-write gate, one mechanically-enforced choke point. 2: local dir renames = one remote move (A1) + commit never restates local truth. 3: mass-delete guard counts content, not containers. 4: decision-table rows resolve at post-move paths. 5: spec §4.5 transfer-stage overlap refused + reconcile property net. 6: reload publishes a hot config snapshot (pump never reads the loop's field). 7: watcher-creation failure degrades to polling (at rebuild and launch). 8: crashed dir-move replays + `Snapshot` terminates.

**W1.9 — fold / type / PKCE (6 items).** 1: fold collisions get their conflict (files resolve cross-tree; shadowed rows held harmless; **directory arm deferred, recorded**). 2: dir quarantine sweeps invisible leftovers instead of wedging. 3: quarantine never overwrites a rescue copy (uniquify above, refuse beneath). 4: type clashes are skips in every direction. 5: S256 PKCE on the OAuth loopback flow, proven end to end. 6: a shadowed folder's tracked subtree is held harmless, never quarantined.

**Round-4 review (doc-only).** MANUAL §8 surfaces the tracked case-only file-rename loop (workaround: edit contents too); code fix stays the deferred dir-arm follow-up.

**State at baseline.** W1 / W1.8 / W1.9 / W2 complete; every reproduced defect from four adversarial rounds fixed. One narrowed Known-bug: fold-equal *folders*, owned by the deferred gate-directory-arm follow-up. **Next: W4** (randomized/fuzzer convergence testing), then W3. Never pushed — Max pushes.

---

## Log

### 2026-07-23 12:30 — scan docs & git log for a recap of past runs
> "scan documenation and git logs and list me in a minimal way (as little as possible to read) of what was done in the last runs of past agents"
- Read `docs/status.md` and `git log`; returned a minimal summary of the last runs (W1.8/W1.9 close-out, next = W4). No repo changes.

### 2026-07-23 12:35 — add a work-log rule + this file
> "add a new rule to the project for the agents … a new file called work logs or similar … track every action done … the prompt, the time and date, and then under it … every action/decision … short but precise … enough for reconstruction … appended … committed … first entry a recap of work done since beginning … on the top as a starting point"
- Created `docs/worklog.md` (this file): append-only, prompt-keyed action log with the baseline recap pinned on top and the keeping-rule stated inline.
- Wired the rule into `CLAUDE.md` — added a doc-map row and a hard rule requiring per-prompt worklog entries.
- Recorded the new doc + agent rule in `docs/decisions.md`.
- Verified `go build ./... && go vet ./... && go test ./...` green, then committed (docs-only).

### 2026-07-23 12:45 — W4: build the randomized convergence fuzzer (next step in the plan)
> "let's move with the next step in the plan"
- Explored the harness: reused the S-series multi-machine `machine`/`newWorld`/`newMachine` helpers, `executor.FaultHook` checkpoints, the in-memory `driveclient.Fake`, `reconcile.ValidateTransferStage`.
- Wrote `internal/engine/fuzz_test.go` (`TestFuzzConvergence`): N machines, random op streams over a small shared namespace, interleaved syncs, one-shot crash injection at executor checkpoints, driven to a quiescent fixed point; oracle = §4.5 on every plan + convergence/idempotence + byte-identical machine/Drive trees + a scoped identity-stability "quiet-rename probe". Bounded default (8 seeds × 70 steps), `SYNCKEEPER_FUZZ_*` env to widen, `-short` to shrink.
- **First run found R25** (seed 3, then seed 39 no-crash): a baseline file whose remote moved to `Q` with the local file already at `Q` (same rename made locally, or a crashed MoveLocal) → reconcile planned `download Q` + `upload Q` → §4.5 refused the whole plan every cycle → permanent wedge. Diagnosed at the reconcile level (pass 1's `!locOK` "move beats delete" vs pass 2's blind upload; `newRemoteAt` returns false because the id is a baseline id).
- Minimized red-first: `internal/reconcile/coincident_move_test.go` (R25, clean + divergent-content cases); confirmed red without the fix (both trip the §4.5 property net), green with it.
- Fixed `internal/reconcile/plan.go` pass 1: when the baseline file is absent at its old path, the remote moved it to a **non-baseline** `Q`, and the local file already sits at `Q`, record it there (same content) or conflict (diverged) — the "non-baseline path" guard prevents mis-firing mid-swap (R6, which regressed on the first attempt and drove the guard).
- Made the fuzzer deterministic: pinned `nowFunc` (frozen during quiesce) removed wall-clock conflict-name churn that caused nondeterministic non-convergence; made the one-shot crash latch `atomic.Bool` after `-race` caught the closure race.
- Verified: full `go build/vet/test` green; `-race` on reconcile/executor/engine green; 60-seed crash sweep + 30-seed no-crash sweep green.
- Docs: testing.md (FZ1 + R25 rows), decisions.md (W4 + R25), spec.md §4.6 + §16.7 notes, plan.md (W4 done, risks), status.md (next = W3). MANUAL unchanged (R25 never a listed bug; W4 is internal).

### 2026-07-23 15:00 — W3.1: extract the fswatch backend interface (next step in the plan)
> "proceed with the next step in the plan. Let's stick to the plan"
- Read `internal/watch/{watch,fswatch?,control,status}.go` + tests; found the loop held a concrete `*fsnotify.Watcher` and did fsnotify-specific work (per-dir `Add`, new-dir registration, event filtering, the pump) inline. Test constraints: keep `newNotifyWatcher` (R15 seam) and `failureLatch` (latch_test).
- Created `internal/watch/fswatch.go`: unexported `fsWatcher` interface (`refresh(root) int` + `close`), `fsnotifyBackend` implementing it (owns its pump goroutine), `newBackend` factory seam, `newNotifyWatcher` moved here. Wake-ups + ignore globs injected (`wake func()`, `ignore func() []string`) so the loop keeps sole ownership of the debounce timer and config (R14).
- Rewired `watch.go`: `startNotifier` is now a thin `newBackend(...)` call; loop `fw` is the interface (`.close()`, `fw.refresh(...)`); `latchIfNeeded` takes `*fsWatcher`; removed `syncWatches`/`watchSubtree` and the fsnotify/`io/fs`/`filepath`/`names` imports; generalized two comments.
- Pure refactor, no behavior change: full `go build/vet/test` green; `internal/watch` tests (incl. R14 reload race + event storm, R15 rebuild/creation failure) green under `-race`; gofmt clean.
- Docs: spec.md §10 (interface-extracted note), decisions.md ("W3.1"), plan.md (W3.1 done, W3 in progress), status.md (next = W3.2 FSEvents). No testing.md row (refactor, no new criterion); MANUAL unchanged (nothing user-visible).

### 2026-07-23 16:00 — W3.2: the FSEvents backend (the repo's first cgo)
> "let's go with w3.2"
- Read the Makefile (`export CGO_ENABLED=0`, comment: "when cgo lands this drops") and confirmed no `TestMain` in the watch tests; matched the `newFSNotifyBackend` signature.
- Wrote `internal/watch/fsevents_darwin.go` (`//go:build darwin && cgo`, ~140 lines of hand-rolled cgo — no wrapper dep): one recursive **file-level** FSEvents stream on a serial dispatch queue; C callback → exported `goFSEventsCallback` via an integer handle registry (Go pointers can't ride in the C context); `refresh` a no-op returning 0; `shouldWake` filters ignored paths (`.DS_Store`) before waking; `close()` = Stop+Invalidate+Release then drop the handle (not idempotent by design). `init()` overrides `newBackend` on darwin+cgo.
- Wrote `internal/watch/main_test.go` (`TestMain` pins `newBackend = newFSNotifyBackend` so R14/R15 and the fsnotify wake tests stay meaningful) and `fsevents_darwin_test.go` (real-change wake integration test + deterministic `shouldWake` unit test; fixed an early double-close hazard).
- Makefile: dropped the global `CGO_ENABLED=0`; `build`/`test`/`vet` native (cgo on → FSEvents), `build-all` pure-Go cross-compile (`CGO_ENABLED=0` per target, fsnotify fallback).
- Verified: cgo compiles/vets clean (dispatch_release, `C.FSEventStreamRef` in a Go struct, the export all fine); FSEvents tests pass (wake in 0.01s); full watch suite green under `-race` with cgo; **fallback proven** — `CGO_ENABLED=0` full build + watch tests green (fsnotify), and darwin/linux/windows pure-Go cross-compile all green; `make build` → native Mach-O arm64, `make build-all` → all platforms.
- Docs: spec.md §10 + build policy (FSEvents implemented), plan.md (W3.2 done), decisions.md ("W3.2"), testing.md (W3.2 row), status.md (next = W3.3), README (`build-all` caption). MANUAL unchanged (backend is invisible to the user; "watches, falls back to polling" still accurate).

### 2026-07-23 18:00 — W3.3: the W1-scale acceptance (≥50k files)
> "let's go with the next step w3.3"
- Read `soak_test.go`, the watch test helpers (`newMachine`/`startWatcher`), and `fdlimit_unix.go`. Key realization: fsnotify's kqueue holds one fd **per watched file**, so 50k exhausts the 10240 soft limit and degrades to polling — while FSEvents holds none. So the acceptance is: FSEvents proves "no fd exhaustion" crisply; fsnotify demonstrates the real fd pressure that trips the latch (the kill→poll→recover *loop* is already R15, scale-independent); the engine must handle a 50k scan.
- Wrote `internal/watch/scale_test.go`: `buildScaleTree` (100 files/dir) + `TestScale`, gated by `SYNCKEEPER_SCALE_FILES` (skip unless set, like the soak). Asserts: full engine sync converges + idempotent second cycle; fsnotify backend creates without error, and when `failed>0` the `failureLatch` trips. Added `TestFSEventsScaleNoFDExhaustion` (darwin+cgo) asserting FSEvents `failed==0` + still wakes at scale.
- Smoke at N=2000 green; **acceptance at N=50000 green** — FSEvents 0/500 unwatchable, fsnotify **403/500** unwatchable (fd pressure, soft limit 10240), engine synced **50 500** actions, second cycle idle (~24 s total). Both cgo modes vet clean; gofmt clean; default `go test` skips the gated tests.
- Sized the carried-over **self-inflicted-events** question: after a cycle writes files, their watch events fire one **bounded, self-terminating** extra cycle (a full rescan — ~19 s at 50k, sub-second at personal scale), never a loop → suppression is an optimization, deferred (decisions.md "W3.3"). Closed the 2026-07-18 parked item.
- Docs: spec §16.8 (verified), plan.md (W3.3 done), decisions.md ("W3.3"), testing.md (W1-scale row passing), status.md (next = W3.4, with the TestMain-pins-fsnotify caveat for the W3.5 soak), CLAUDE.md (scale + deep-fuzz commands). MANUAL unchanged.

### 2026-07-23 18:35 — W3.4: the periodic watcher rebuild is per-backend
> "let's move with w3.4"
- Read the loop's rebuild machinery (`rebuildEvery = 500`, the `cycle%cadence == 0` branch that closes + recreates the watcher to bound a kqueue fd leak) and confirmed R15 relies on the fsnotify rebuild path.
- Added `needsRebuild() bool` to the `fsWatcher` interface; `fsnotifyBackend` returns true (kqueue per-file fd leak), `fseventsBackend` returns false (no per-file fds). Gated the loop's rebuild branch on `fw.needsRebuild()`; when false it falls through to the normal `refresh` (a no-op for FSEvents) and the stream keeps running. Updated the `rebuildEvery` comment; rlimit raise + polling latch untouched.
- Wrote `internal/watch/rebuild_test.go` (`TestRebuildIsPerBackend`): a fake `countingBackend` injected via the `newBackend` seam proves a `needsRebuild=false` backend is created exactly once (never recreated at cadence) and a `true` one is recreated. Added a real-backend assertion in the FSEvents wake test (`needsRebuild=false`). R15 (real fsnotify rebuild→degrade→recover) still green.
- Verified: build+vet both cgo modes; full watch suite green under `-race` (cgo on) and green (cgo off); gofmt clean.
- Docs: spec §10 build policy (rebuild now per-backend), plan.md (W3.4 done), decisions.md ("W3.4"), testing.md (W3.4 row), status.md (next = W3.5, only item left in W3). MANUAL unchanged.

### 2026-07-23 18:40 — W3.5: run the soak against the FSEvents backend
> "yes, let's proceed" (do the W3.5 code now + smoke; hand over the 2 h gate)
- Problem: `TestMain` pins the watch suite to fsnotify (R14/R15 seams), so `TestSoak` was exercising fsnotify; and the cross-platform `soak_test.go` can't name `newFSEventsBackend` (darwin+cgo-only symbol).
- Fix: `main_test.go` captures `productionBackend = newBackend` (the platform default — FSEvents on darwin+cgo, set by the darwin `init`) *before* pinning fsnotify; `TestSoak` sets `newBackend = productionBackend` for its run and restores the pin. No build-constraint symbol leaks into the cross-platform soak; the soak validates what production runs.
- Smoked: 30 s soak → converged on 89 files, clean `doctor`; 90 s soak → converged on 220 files, clean `doctor` (both against FSEvents; the transient lstat ERRORs are chaos racing the scanner — retried, converged, as designed).
- Docs: plan.md (W3.5 wired+smoked, 2 h gate = Max's), decisions.md ("W3.5"), spec §16.9, testing.md (soak row), status.md (W3 engineering done; next = W5). MANUAL unchanged.
- **Handoff: the full 2 h acceptance gate is Max's release-ritual run:** `SYNCKEEPER_SOAK_SECONDS=7200 go test ./internal/watch/ -run TestSoak -timeout 3h` (runs against FSEvents on macOS). This closes the W3 engineering.

### 2026-07-23 18:55 — defer the 2 h soak to be the last W3 step (after an adversarial check)
> "I changed my mind. I don't want to run this test now. Remove it and stop it. … I will run first an adversarial check and I want to run this long test after that. Annotate as next and last step in W3 the long test — what to run, how to run, what we expect, the goal."
- Confirmed nothing was running (the 2 h soak was never started; earlier background smokes all completed) — nothing to stop. Removed the "run now / check at 20:52" time annotation above.
- plan.md W3.5: re-annotated the 2 h soak as the **explicit final W3 step**, to run **after Max's adversarial check**, with a full run block — command, how-to, expected pass criteria, and the goal.
- status.md: Next is now the **adversarial check first, then the final 2 h soak** (W3 is not closed until it passes); W5 follows W3's close. No code change.

### 2026-07-23 19:09 — Adversarial analysis of W3 (the pre-soak check)
> "perform and adversarial analysis of the project with particular focus on work done in w3. Perform a general check if the code is aligned with the goals and the documented plan over all the project and then docus on the w3. Check if the implementation ir correct, is in line with goals and requirements, if the test coverage is complete. hunt for bugs, errors in logic or any other issue. Generate a report at the end with all the details and present a fix plan and the tests for it that will be executed before the end test for closing the w3."
- General alignment pass: status/plan/spec/decisions/testing/MANUAL/README/Makefile/CLAUDE.md cross-checked against the code — consistent; every W3.1–W3.5 claim verified to exist in the code as described. Verified green now: `go build ./... && go vet ./... && go test ./...`, `go test -race -count=1 ./internal/watch/`, `CGO_ENABLED=0 go test -count=1 ./internal/watch/`. No stray fsnotify references outside `internal/watch`.
- Deep W3 review of `fswatch.go`, `fsevents_darwin.go` (+cgo lifecycle: registry, Invalidate ordering, callback races — sound), `watch.go` loop integration, and all W3 tests. W3.1 confirmed a faithful pump transposition (diff-level).
- **Finding 1 (harness, gates the 2 h soak): `TestSoak`'s settle phase races in-flight daemon cycles** — `startWatcher`'s cancel doesn't wait for `Run` to return, so settle `Sync`s overlap the daemon's last cycle on the same engine (engine is documented cycle-serialized). Can mint a duplicate-name pair on the fake Drive → spurious red on the 2 h gate. Fix: cancel waits on done.
- **Finding 2 (backend divergence): FSEvents wakes on churn inside ignored directories** — `shouldWake` checks only the basename; fsnotify structurally never delivers events under ignored dirs (refresh SkipDirs them). A user-ignored `node_modules`-style dir busy-rescans the daemon under FSEvents only. Fix: component-wise ignore filtering relative to root.
- Finding 3 (low, recorded-not-fixed candidate): a mid-life dead FSEvents stream (external-volume remount) is never detected or rebuilt (`needsRebuild=false` forever); poll covers correctness, `status` overstates "watching". Max's call: accept via decisions.md or add belt-and-braces.
- Finding 4 (test hygiene): `rebuild_test.go` failure path leaks the `Run` goroutine (cancel/wait not in `t.Cleanup`). Finding 5 (nits): CLAUDE.md soak command lacks `-v` (pass criteria read a log line); `status.go` ModeWatching comment still says "fsnotify active".
- Report + sequenced fix plan (fixes red-first before the 2 h gate, per the standing discipline) delivered in-chat; awaiting Max's go-ahead on the fixes.

### 2026-07-23 19:24 — adversarial-check fixes 1+2, red-first, before the 2 h gate
> "next"
- **Fix 1 (F1+F4, harness serialization):** red test `TestStopWaitsForDaemonExit` written first and confirmed red (`stop() returned while the daemon reports running=true mode="watching"`). Fix: `startWatcher`'s stop is now `cancel(); <-done` (waits for `Run` to exit — `rec.stop()` runs before Run returns, so "stopped recorded when stop returns" is the observable proof); `rebuild_test.go` gains the same `t.Cleanup` against the failure-path daemon leak. The soak's settle phase can no longer race an in-flight daemon cycle on the same engine.
- **Fix 2 (F2, FSEvents ignored-dir filter):** red case `file under ignored dir` (`/sync/node_modules/pkg/index.js`, glob `node_modules`) confirmed red against the basename-only `shouldWake`. Fix: component-wise `ignoredPath` relative to the stream root (root always wakes; out-of-root falls back to basename); root `EvalSymlinks`-resolved once and the stream registered on it. Test extended with root/out-of-root/sibling cases.
- Nits folded in: CLAUDE.md soak command gains `-v`; `status.go` ModeWatching comment de-fsnotified.
- Verified: gofmt clean, `go vet` clean, `go test -race -count=1 ./internal/watch/` green, `CGO_ENABLED=0` watch tests green, full `go build && go vet && go test ./...` green.
- Docs: decisions.md two entries ("W3 adversarial check, fix 1/fix 2"), testing.md rows W3-adv-1 + W3-adv-2, spec §10 fswatch cell dated. F3 (dead-stream-never-rebuilt) left as Max's open call, noted in the fix-1 entry.

### 2026-07-23 23:04 — running the 2 h FSEvents soak gate (the W3 close-out test)
> "start the 2h test. Run it by yourself without me babysitting you. Start whatever you need and verify the result at the end of the test. document the result and the process"
- Ran `SYNCKEEPER_SOAK_SECONDS=7200 go test -count=1 ./internal/watch/ -run TestSoak -timeout 3h -v` in the background against commit `aefe4fe` (clean), cgo on → FSEvents. Conditions logged (go1.26.4, kern.maxfilesperproc 10240).
- **Run 1 (19:44 start): killed ~20:20:54, ~37 min in — NOT a test failure.** Log clean throughout (`failed=0`, only the expected transient `lstat … no such file` from chaos racing the scanner). Root cause via `pmset -g log`: system **idle-sleep**. Other apps' sleep-prevention assertions (coreaudiod, powerd "display on", a stray caffeinate) released at 20:20:48; with nothing holding one for `go test`, the machine slept 6 s later and the task died. Machine's `sleep` setting is an aggressive **1 minute**.
- **Run 2 (caffeinate -dimsu, 22:10 start): killed ~22:57, ~47 min in — again system sleep, not a failure.** The ~1.5 h gap between dispatch and the log's real start (22:10:07, at a scheduled `UserWake`) proved the machine had been asleep, suspending my own session too. It died on **battery**, where `caffeinate -s` is void (macOS honors it only on AC) — so the 1-min idle-sleep still won. Log clean to the kill.
- **Run 3 (23:04 start): under `caffeinate -dimsu` on AC power (100%, charged).** On AC, caffeinate genuinely prevents system sleep — confirmed `PreventUserIdleSystemSleep 1` held by the run's caffeinate (pid 95816). FSEvents backend confirmed in-log. This is the proven-effective condition; expected finish ~01:04 + settle. **Result pending — to be appended when the background task reports.**
- **Environmental caveat (documented, not fixable in software from here):** this machine idle-sleeps after 1 min and is intermittently on battery; an unattended 2 h run only survives while on AC with caffeinate holding. If run 3 is unplugged to battery mid-run it will sleep and die again — outside my control. The soak test itself was healthy in every attempt; the gate just needs an uninterrupted 2 h awake window.

### 2026-07-24 09:3x — 2 h soak: runs 3 killed too; switched to a detached (sleep-surviving) run
- **Run 3 (caffeinate on AC, 23:04 start): killed ~23:57, ~53 min in.** Same pattern (37/47/53 min — all end-of-day). Root constraint identified: a Claude Code *harness* background task cannot survive the Mac sleeping — on sleep the harness suspends and the tracked `go test` is torn down (log freezes at the sleep instant, process gone on wake), and the machine's idle-sleep is a 1-minute setting, intermittently on battery where `caffeinate -s` is void. Not a test failure in any run (`failed=0`, clean, convergence-bound throughout).
- **Run 4 (2026-07-24 morning): launched DETACHED** — `nohup caffeinate -dimsu env SYNCKEEPER_SOAK_SECONDS=7200 go test … -timeout 10h -v &`, reparented to launchd (verified PPID=1). A plain OS process is *suspended and resumed* across machine sleep instead of being torn down; Go's monotonic timers pause during sleep, so it accumulates a full 2 h of awake-time chaos and completes across sleep/wake cycles. FSEvents backend confirmed in-log. `-timeout 10h` gives margin since wall-clock may far exceed the 2 h of awake time.
- Tradeoff: a detached run yields no harness completion signal, so a harness "watcher" task (`b73br72tv`) polls the log and notifies on the terminal marker (PASS/FAIL/EXIT); if it dies on a sleep the detached run continues and the watcher is relaunched when next active. **Result still pending — appended when run 4 reports.**

### 2026-07-24 12:05 — 2 h FSEvents soak PASSED → W3 CLOSED
- **Run 4 (detached) PASSED the full 2 h.** `--- PASS: TestSoak (7216.68s)`, **converged on 16,344 files** (both machines byte-identical), clean `doctor` on both, 0 divergence, 0 panic/race. 3,488 transient `lstat … no such file` retries = chaos racing the scanner, converged as designed. Ran continuously 09:39→11:39 CEST (no sleep gaps this time — stayed on AC). Process exited cleanly; no strays left.
- Verified every pass criterion against the full log directly (not just the watcher tail): PASS marker, converged line, FSEvents-backend line, zero FAIL/diverged/never-settled/doctor-after-soak/panic.
- **The detached approach is what made it possible.** PID reparented to launchd (PPID 1) survived the watcher task's own death and any brief sleeps; three earlier harness-task runs (37/47/53 min) were all killed by the 1-min idle-sleep, none a test failure.
- Docs updated (all committed, not pushed): testing.md soak row → passing (2026-07-24, FSEvents); spec §16.9 gate PASSED; plan.md W3 item 5 done + W3 header `done (2026-07-24)` + execution-order line; decisions.md 2026-07-24 "W3 closed" (result + detached-run rationale + future-port guidance); status.md rewritten (W3 closed, next W5, W6/port soak lesson). MANUAL unchanged (backend + soak invisible to users).
- **W3 is closed.** Next workstream: W5 (daemon-first polish). One low FSEvents finding (dead-stream-never-rebuilt) left as Max's open call, recorded.

### 2026-07-24 12:39 — W3 follow-up: FSEvents remount detection (the deferred finding, fixed)
> "tlet's first fix the "a dead FSEvents stream after an external-volume remount ""
- Chose device-id detection reusing the existing latch/recovery path (over: accept-and-record; a blind FSEvents rebuild cadence — the waste W3.4 removed; cgo RootChanged/Unmount flag handling — more cgo, less robust than a dev-id compare).
- Red-first: added `TestFSEventsRefreshDetectsRemount` (via a new `fseventsRootDev` seam) + the struct fields with `refresh` still a no-op → confirmed red (`refresh after a device change = 0, want 1`). Then implemented: `fseventsBackend` captures the root's device id at creation; `refresh` (was a pure no-op) re-stats root and returns 1 on a device-id change (remount) or unstattable root (unmount race), else 0. The failure latch → polling-only (honest `status`) and the recovery path recreate the stream on the new volume. → green.
- Verified: gofmt/vet clean; `go test -race ./internal/watch/` green (cgo on); `CGO_ENABLED=0` build + watch tests green; full `go build && go vet && go test ./...` green.
- Docs: testing.md row W3-adv-3; decisions.md "W3 follow-up"; spec §10 fswatch cell; status.md (Updated line + Last completed + Next drop the open-finding item). No MANUAL change (MANUAL §9 already promises auto-restore + mode display; this makes it true for remount too). W3 stays closed; this is post-close hardening. Next: W5.

### 2026-07-24 12:57 — W5 (daemon-first polish): init offers service install, account shows email, pause kept in-memory
> "let's proceed with w5"
- Oriented: read status.md, plan.md (W5), spec §1/§8/§10/§15, the `init`/`account`/`service`/`watch` commands, `driveclient` interface + real/fake, auth `TokenSource`.
- **Asked Max the two judgment calls** (AskUserQuestion): (1) how `init` "offers" service install → **prompt when interactive** (+ `--service`/`--no-service`); (3) persist pause across restart → **keep in-memory**. (2) account email needed no decision.
- **Item 2 (driveclient):** added `About` type + `About(ctx)` to the `Client` interface; real backend calls `about.get` with the required `user(emailAddress,displayName)` fields mask (in `withRetry`); fake returns a settable `AboutInfo` field. Build/vet green — interface change type-checks everywhere (only `brokenClient` embeds the interface).
- **Item 2 (account cmd):** rewrote `account.go` to fetch the signed-in account under a 15 s timeout and print it via a pure `printAccountIdentity` helper; graceful on every failure (no token / offline / API error → "unavailable" note, never fails the command). Retired the "phase-6 follow-up" stub. Test `account_test.go` (W5.2): email+name, email-only, offline, no-email, name-without-email `<>` guard.
- **Item 1 (init offer):** new `cmd/synckeeper/serviceoffer.go` — pure `offerServiceInstall(in,out,force,skip,interactive,install)` (precedence skip>force>prompt>hint; never fails init), `promptYesNo` (empty/EOF/garbage → default), `isTerminal` (ModeCharDevice). Wired into `init.go` after a successful init/adopt with `--service`/`--no-service` flags and the real `service.Install` injected. Tests `serviceoffer_test.go` (W5.1): 7 precedence paths + soft-failure + promptYesNo table.
- **Item 3 (pause):** decision only, no code — kept in-memory (safe default; a pause surviving reboot is the worse surprise). spec §8.3 dated note.
- Verified: `go build ./... && go vet ./...` clean; `go test -race ./...` all green (cmd, driveclient, and the full suite incl. engine/watch).
- Docs: spec §8.3/§8.4/§15 dated; MANUAL §1/§2/§3 + date stamp; plan.md W5 items 1–3 done + header `done (2026-07-24)` + execution-order line; testing.md W5.1/W5.2 rows; decisions.md 2026-07-24 "W5"; status.md rewritten. **W5 closed — primary-platform planned work done; next W6 (needs a 2nd machine) / ports W7–W8.**

### 2026-07-24 13:34 — rename the login-service label macsimbodnar → mazerfaker
> "This needs to change "com.macsimbodnar.synckeeper" macsimbodnar should be replaced with mazerfaker"
- `internal/service/service.go`: `const label` → `com.mazerfaker.synckeeper` (the launchd Label + plist filename derive from it). The Go **module** path `github.com/macsimbodnar/synckeeper` is unrelated (repo path) and left unchanged.
- `internal/service/status_test.go`: both sample-`launchctl`-output fixtures updated to the new label (cosmetic — `launchdRunning` keys off the `PID` line, not the label). Build/vet/`go test ./internal/service/` green.
- **Migration flagged to Max (not auto-run):** an OLD-label agent is installed *and running* (`~/Library/LaunchAgents/com.macsimbodnar.synckeeper.plist`, PID 15862). The renamed binary won't manage it (`service status` will read "not installed"); installing the new-label service alongside it would have two daemons contend the instance lock. Migration = `launchctl unload` + remove the old plist, then `service install` the new one — left for Max to authorize (stops his live daemon).

### 2026-07-24 18:22 — adversarial doc audit of MANUAL/README vs code, then apply
> "run an adversarlial analysis of the MANUAL.md and README.md against the requirements and the code. Check they aligne and are consistent. They represent the real code situation. Generate a report at the end and prepare a fix plan for teh documentation and also a proposal if gurd needs to be added to the agent instructions so the documentation will eventually be more in line" … then: "apply"
- **Audited:** traced every command/flag/default/path/guard/behavior claim in MANUAL.md + README.md to the code (`cmd/synckeeper/*`, `internal/{config,guards,service,auth,watch,executor,conflicts}`) and cross-checked spec.md + status.md. Verdict: surface overwhelmingly accurate (all 13 commands+flags, config defaults, hot/restart split, mass-delete `>10 && >25%` dirs-excluded, conflict-copy naming, file paths, credential lookup+`account` strings, service log path, known-bugs/limitations all verified true). One real inaccuracy + 3 staleness nits.
- **Report delivered** in-chat with a findings table (D1–D4), a verified-accurate ledger, a fix plan, and a 3-level guard proposal (L1 prose rule / L2 ritual / L3 automated test; recommended L3+L1).
- **D1 (real, medium):** MANUAL §6 said skips are "shown in `status`" — false. Verified `DaemonStatus`/`CycleSummary` have no skip field, `cycleDone` records only counts/guard/executed-activity; skips print only via one-shot `sync` (`printResult`) + `init --adopt` (`printAdoptResult`). Fixed MANUAL §6 wording.
- **D2/D3 (README staleness):** `Use` still told users to run `service install` (pre-W5); `Status` said "correctness fixes first" (W1–W5 all closed). Both reworded.
- **D4 (nit):** MANUAL §3 `config` "and where it came from" → "and the file it's read from".
- **Guard applied (Max's "apply"):** (a) `CLAUDE.md` Hard rule "doc claims are claims about code, not intent" — verify user-facing where/what/default claims against code, not spec. (b) `cmd/synckeeper/main.go` refactored to expose `newRootCmd()`; new `cmd/synckeeper/surface_test.go` `TestCLISurfaceMatchesManifest` golden-pins the command set + per-command flags + global `-v`, so a CLI-surface change fails the build until the manifest + MANUAL §3/spec §15 are updated. `go mod tidy` promoted `spf13/pflag` to a direct dep.
- **Verified:** `go build ./... && go vet ./...` clean; `go test ./...` all green; `TestCLISurfaceMatchesManifest` PASS.
- **Docs:** decisions.md 2026-07-24 "Doc audit"; testing.md row DOC1; ideas.md (daemon-surface-skips idea); MANUAL §3/§6; README Use+Status; CLAUDE.md Hard rules; status.md updated. No spec/plan change (spec found accurate). Not committed/pushed yet unless Max says.

### 2026-07-24 18:22 — decide D1's deeper fix interactively, then plan it, then commit
> "Before commit let's decide how to handle \"D1's deeper fix is a product decision\". Let's do that in an interactive way. Once decided document the decision, add this to the plan and only then commit"
- **Asked Max two questions (AskUserQuestion):** (1) disposition of surfacing skips under the daemon → **schedule (add to plan)**, not build-now/decline; (2) surfacing shape → **per-skip `activity` entries** (over a low-noise `status` count or both). Note: the chosen preview explicitly showed skips repeating every cycle.
- **Recorded the noise tension honestly rather than overriding his shape choice:** the activity ring is capped, so a permanent skip (Google-native file) re-logged every cycle would evict real history — the same per-cycle noise spec §5 makes ignore-matches silent to avoid. Flagged as an **open build-time detail** (likely: emit only when a path's skip first appears/changes) in both the decision and the plan item; did not pre-decide it.
- **Documented:** decisions.md "Doc audit" entry amended — replaced the "idea filed" placeholder with the D1 deeper-fix decision (schedule as W10, per-skip activity entries, capped-ring caveat).
- **Planned:** plan.md gains **W10 — Daemon surfaces skips in `activity`** (`not started`; scope in `internal/watch/status.go` cycleDone; acceptance; priority below ports, no hardware needed). Reconciled the idea out of ideas.md (triaged → plan). status.md Next gains W10 and the Last-completed line points at W10 instead of the idea.
- Then committed (`afed980`).

### 2026-07-24 18:22 — security review before W6, add W5.5 gate
> "i want to add a new step before W6. I want to handle the security part and be sure that i don't leak any key or secret ar share personal file on the internet. Run an analysis and find potential issues in your implementation"
- **Reviewed** auth (loopback flow, token storage), credential handling, control socket, Drive client, all logging, egress, `.gitignore` — targeting two threats: secret leakage + personal-file exposure.
- **Structural win found:** grep of `internal/driveclient/` for `Permissions`/`anyone`/share/etc. returns **nothing** — no sharing API call exists, so uploads stay owner-private and nothing can be made public. Egress sweep: only `googleapis` (HTTPS); no pprof/extra listener/`0.0.0.0` bind — the sole listener is the loopback OAuth server (`127.0.0.1:0`, CSRF `state`, S256 PKCE, 5-min timeout).
- **Secret handling verified sound:** `token.json` `0600` atomic + `LoadToken` refuses group/world-readable; control socket `0600` unix (no TCP); config dir `0700`; `.gitignore` covers token/credentials/client_secret/db. Swept all 28 `slog` sites + every `fmt.Print` → no token/secret/credential/content logged or printed (`account` shows only presence/expiry).
- **Four residual items, all decisions (not bugs):** S1 embedded client secret committed in `internal/auth/credentials.go` (by design spec §9; publication exposes it + git history retains it → console rotate+revoke before publishing); S2 full `drive` scope (leaked-token blast radius = all of Drive); S3 launchd log records file paths (never contents), maybe `0644` on a shared Mac; S4 no enforced pre-publication secret scan.
- **Delivered:** the report in-chat (verified-good ledger + S1–S4). **Added plan.md W5.5 — Security & privacy hardening**, runs before W6, carrying S1–S4 + acceptance; needs no hardware so it's the actionable next step. Folded the parked "rotate OAuth client" item into W5.5-S1.
- **Docs:** decisions.md "Security review" (records the gate; the four calls left to Max, not pre-decided); status.md (Updated + new Last-completed + Next promotes W5.5 to #1 + parked rotate line → W5.5-S1); this worklog entry. No code/spec/behavior change. *(Committed `dc9c7b9` on Max's "commit the security fix plan".)*

### 2026-07-24 18:22 — Max's calls on W5.5 items S1–S4 + the S1 secret-distribution review
> "s1 is not an issue. I am running for now on debug credentials. I consider this one to be safe to publish. But i want to review how the secret is distributed and where and how it is loaded in the code. s2 let's keep this but change it to investiage if the access token can be granted only to the specific folder instead of full drive. s3 defer the decision on this topic to me when the time to process s3 will come. 34 this makes sense. keep it as is"
- **S1 → accepted, `done`.** Max runs on debug creds and accepts them as publishable → drop the rotate/revoke concern (and the parked item). Delivered the requested review of secret **distribution + loading**: traced every reference (`grep ClientSecret|resolveClient|CredentialInfo|oauthConfig`). Distribution = embedded `var ClientID`/`ClientSecret` (`internal/auth/credentials.go`, compiled into the binary, `-ldflags -X`-overridable) + optional BYO `credentials.json` (config dir, precedence) + ldflags. Loading = single `resolveClient` (BYO→embedded) → only consumer of the secret is `oauthConfig` → `oauth2.Config` → Google token endpoint over HTTPS; `account`/`CredentialInfo` drop the secret and show the id only; never logged/printed/persisted. No leak path beyond intended embedding.
- **S2 → reframed** (Max): keep the item but change it from "record blast radius" to **investigate whether the token can be scoped to only the Synckeeper folder** instead of full `drive`. Preliminary: Drive scopes are app-wide, not path-scoped; `drive.file` can't see/adopt UI-created folders → breaks `init`/`init --adopt`. To confirm + decide under the item.
- **S3 → deferred** to Max, decision to be made when S3 is processed. No change now.
- **S4 → kept** as specified (Max: "makes sense").
- **Docs:** plan.md W5.5 items 1–4 rewritten (S1 `done`/reviewed, S2 = scope investigation, S3 deferred, S4 kept) + acceptance; decisions.md "W5.5 item calls"; status.md (Next W5.5 summary + parked rotate line marked RESOLVED); this worklog entry. No code/spec/behavior change — decisions + the S1 review only.
- Committed `6d9d954`.

### 2026-07-24 18:22 — W5.5-S2 investigation (folder-scoped token vs full `drive`)
> "yes commit and then run the investigation on s2 and document the findings"
- Committed the item-call decisions (`6d9d954`), then ran S2 against Google's current docs (WebFetch the Drive API scopes page + WebSearch on `drive.file` cross-session behavior).
- **Findings:** (1) **No folder/path-scoped Drive scope exists** — account-wide or per-file only, so the literal S2 goal isn't achievable. (2) Only narrowing = **`drive.file`** (non-sensitive; app-created or Picker-opened files). (3) Blast-radius win: leaked token reaches only Synckeeper-created files. (4) Verification win: full `drive` is **Restricted** (needs restricted-scope verification + CASA security assessment for stored data); `drive.file` is non-sensitive (basic verification, no assessment; relaxes the §9 100-user cap). (5) **Cost/spec conflict:** under `drive.file`, files added to the folder *outside* Synckeeper (Drive web UI, mobile, other apps) are invisible → won't download, `changes.list` won't report → **silently breaks §4's remote-created download**; `--adopt` + plain `init` find-existing-folder break; multi-machine of app-created content still works. (6) **Migration risk:** scope-downgrade retroactivity (does an existing install keep access to already-created files after switching to `drive.file`?) is undocumented → needs a live test before any switch.
- **Recommendation (agent):** keep full `drive` (narrowing contradicts §4 + the adopt-based W6 rollout); record `drive.file` as a future option gated on the retroactivity test + a spec §4/§9 amendment. **Decision left to Max** — no scope change made (`driveScope` unchanged).
- **Docs:** decisions.md "W5.5-S2 investigation"; plan.md W5.5-S2 (findings + recommendation); status.md S2 line; this entry. Committed `e77c903`.

### 2026-07-24 18:22 — S2 decided: keep full `drive`, log as a known limitation
> "ok, for now let's keep scope for the whole drive. Put a note as a known limitation that will be investigated maybe in the future"
- **Decision (Max):** keep the full `drive` scope for now — no code change (`driveScope` unchanged).
- **MANUAL §9:** added a "Full-Drive access scope" known-limitation bullet — the app authorizes full `drive` (no folder-scoped scope exists; the narrower `drive.file` would silently stop syncing files added to Drive outside Synckeeper), the token can reach the whole Drive but lives at `token.json` `0600` and only goes to Google; "may be revisited in a future version".
- **Docs:** decisions.md "W5.5-S2 decided"; plan.md W5.5-S2 marked **decided / S2 closed**; status.md S2 line → `done`; this worklog entry. No code/spec/behavior change. `drive.file` stays on record as the future option (retroactivity test + spec §4/§9 amendment if ever taken).

### 2026-07-24 18:22 — S3 done: daemon tightens its log to 0600 (owner-only)
> "let's take care of the s3 now" … (clarified) … "i think the solution should be that it should be readable only by the owner user. Is this possible?"
- **Live-checked the exposure:** `~/Library/Logs/synckeeper.log` is really `-rw-r--r--` (0644, world-readable); at default level it logs synced file *names* (`rel_path` on failures, orphan-temp/quarantine paths, sync-dir), never contents. Config dir is `0700` but the log lives outside it.
- **Decision (Max):** the log should be readable only by the owner → chmod to `0600`.
- **Implemented:** `internal/service/service.go` gains `LogPath()` (darwin log path, "" elsewhere) + `RestrictLogToOwner()` + testable `restrictToOwner(path)` (chmod 0600; empty/missing = no-op). `cmd/synckeeper/watch.go` calls `service.RestrictLogToOwner()` best-effort at daemon startup (failure → Debug log, never blocks). Chose a targeted per-file chmod over a launchd `Umask` (which would also restrict synced downloads) and over demoting paths to Debug (which would strip the owner's own logs and leave the file 0644).
- **Tests:** `internal/service/logperms_test.go` — `TestRestrictToOwner` (0644→0600, empty + missing no-op), `TestLogPathDarwin` (path ends in Library/Logs/synckeeper.log). `go build && go vet && go test ./...` all green.
- **Note:** existing running daemon keeps its 0644 file until restarted; the chmod runs on next `watch` start (Max can `chmod 600` now for immediate effect).
- **Docs:** MANUAL §3 service row (owner-only log); decisions.md "W5.5-S3 decided & done"; plan.md S3 closed; status.md (Updated + Next S3 → done); testing.md W5.5-S3; this entry. **S3 closed; W5.5 has only S4 left.** Committed `7c4731a`.

### 2026-07-24 18:22 — S4 done: pre-publication secret-scan gate; W5.5 CLOSED
> "let's go adding s4 gate"
- **Surveyed** tracked files first (`git grep GOCSPX-`, `BEGIN…PRIVATE KEY`, `git ls-files` for sensitive names): only the one intentional embedded secret in `internal/auth/credentials.go`; nothing else → exact allowlist.
- **Built `internal/audit`** — a self-contained Go gate (no external tool): `Scan(root)` runs (A) no `token.json`/`credentials.json`/`client_secret_*.json`/`*.db` tracked, (B) `.gitignore` still covers those, (C) secret-pattern scan of tracked text files (Google OAuth `GOCSPX-`, Google API key, AWS `AKIA`, Slack `xox[baprs]-`, PEM private-key block), allowlisting `internal/auth/credentials.go` + the audit package's own source. `make audit` target added; also runs inside `go test ./...` (continuous enforcement).
- **Chose Go over gitleaks/trufflehog:** no external dep, allowlist-aware (gitleaks would false-fail on the intentional embedded secret), and runs in the suite. gitleaks left as an optional manual belt-and-braces (would need a config).
- **Tests** (`internal/audit/audit_test.go`): `TestSecretScanGate` (real tree clean), `TestScanBytesDetects` (planted fake secret caught via concatenation so the literal isn't in-source; clean text + binary skipped — proves the gate isn't vacuously green), `TestSensitiveTrackedClassifier`, `TestRequiredGitignorePresent`. `gofmt`/`build`/`vet` clean; `make audit` green; full `go test ./...` green.
- **Docs:** README + CLAUDE.md (`make audit`); decisions.md "W5.5-S4 done, W5.5 closed"; plan.md S4 `done` + **W5.5 header `done (2026-07-24)`** + acceptance all-met; testing.md W5.5-S4; status.md (Updated rewritten/trimmed, Next reordered — W10 is the actionable non-hardware item, W5.5 moved to Last completed); this entry. **W5.5 CLOSED — S1–S4 all done.** Committed `c3fec57`.

### 2026-07-25 — MANUAL missed cobra built-ins (help/completion/--version); documented + guard fixed
> "I see that help command is present but WHY IT IS NOT DOCUMENTED! Perform a full scan of the capabilities and compare it with the current manual. Check for missing commands like this one"
- **Scanned the real binary** (`go run ./cmd/synckeeper --help`, `completion --help`, `--version`): **15** invocable top-level commands, MANUAL documented **13**. Missing: cobra built-ins `help [command]` and `completion {bash|zsh|fish|powershell}`; global flags `--version` and `-h/--help`. The 13 custom commands were all accurate.
- **Root cause the DOC1 guard missed it:** `TestCLISurfaceMatchesManifest` walked `newRootCmd().Commands()` **before Execute()**, and cobra adds help/completion/--version lazily during Execute/InitDefault — so the guard was structurally blind to the built-ins.
- **Fixed both:** MANUAL §3 gains `help` + `completion` rows and lists `--version`/`-h`/`--help` in the global-flags line; `surface_test.go` now calls `root.InitDefaultHelpCmd()`/`InitDefaultCompletionCmd()`/`InitDefaultVersionFlag()`, requires `help`+`completion` in the manifest, and asserts the `--version` flag. Kept `completion` (standard, useful) — Max's issue was undocumented, not unwanted.
- **Verified:** cobra 1.10.2 Init signatures checked in the module cache; `TestCLISurfaceMatchesManifest` green; full `go build && go vet && go test ./...` green.
- **Docs:** MANUAL §3; decisions.md 2026-07-25; testing.md DOC1 row (now 15-command, materializes built-ins); this entry. No product-behavior change. **Note:** the W11 feature request (a `manual`/`help`-prints-the-manual command + an `info` command) is still un-recorded/paused — this was the undocumented-capability fix, not that feature.

### 2026-07-25 — record W11 (`info` command); manual command skipped
> "we can skip the manual command for now and only add a plan for the info command"
- **Decisions (Max):** (1) a **separate `info` command** (not folded into `status`); (2) the manual-printing command is **deferred** (skip for now).
- **Recorded plan.md W11 — `info` command** (`not started`, no hardware): read-only `synckeeper info` static dump — version; all config-file paths (config dir, config.toml, state.db, token.json, credentials.json + active OAuth client, control.sock, quarantine/, log path); sync dir + drive folder + root id; machine name + id; effective config summary; local state (tracked/pending/quarantine/token). Offline (no `about.get`; points to `account`), read-only (no lock), `--json`. Impl sketch (`cmd/synckeeper/info.go`, reuse existing helpers) + acceptance (render test, added to the DOC1 surface manifest, MANUAL §3 + spec §15 same commit). Deferred companion: a `manual` command (name avoids cobra `help`).
- **Docs:** plan.md W11; decisions.md 2026-07-25 "New W11"; status.md Next (W11 alongside W10; W6/W7/W8 renumbered). Planning only — no code. Committed `f203906`.

### 2026-07-25 — implement W11 (`info` command)
> "let's implement it now"
- **`cmd/synckeeper/info.go`** — `newInfoCmd` (`--json`); `gatherInfo()` builds an `infoView` (paths always; config loaded if written, else defaults; DB opened read-only if present) and pure renderers `printInfoHuman(w,v)` / `printInfoJSON(w,v)`. Read-only, no instance lock, no network (points to `account` for the live email). Reuses `config.Dir`, `statedb.Path`/`OpenRead`/`GetMeta`/`ItemCount`/`PendingOpCount`/`GetDaemonStatus`, `auth.TokenPath`/`LoadToken`/`CredentialInfo`, `service.LogPath`, `cfg.SyncDir`, `quarantineUsage`, `daemonState`/`daemonAlive`, `main.version`.
- Registered in `newRootCmd` → **DOC1 surface guard tripped** until `info` added to the manifest + MANUAL §3 + spec §15 (guard working as designed). Added `"info": {"json"}` to the manifest.
- **Verified on the real env**: full snapshot renders (all paths, folder+id, machine+id, token "expires in 14m; refresh present", effective config, state); `info --json` valid (keys incl. nested `paths`/`state`/`oauth`/`token`/`config`). gofmt clean; `go build && go vet && go test ./...` green.
- **Tests** `cmd/synckeeper/info_test.go`: `TestPrintInfoHuman` (every field), `TestPrintInfoHumanNotInitialized` (hint + no empty `(id )` + token absent), `TestPrintInfoJSON` (valid, nested).
- **Docs (same commit):** MANUAL §3 `info` row; spec §15 CLI surface + dated note; plan.md W11 `done` + acceptance met; decisions.md "W11 done"; testing.md W11; status.md (Updated + Last completed + Next drops W11). **W11 closed.** The `manual` command stays deferred.

### 2026-07-25 — compress MANUAL.md (caveman rules, all info retained)
> "now i want you to read the @MANUAL.md and compress it. IT's too verbose. use Caveman skils and compress the verbosity but retain the information. My goal is to minimize the amount of words to read and have easy to find information by short look but preserve all the information but condensed"
- Loaded `caveman:compress` skill; applied its rules directly (rather than its Python CLI) to keep tight control and avoid a redundant `MANUAL.original.md` (git history at `53debff` is the backup).
- Rewrote MANUAL.md: prose → terse fragments (dropped articles/filler/hedging); code blocks, commands, flags, paths, TOML preserved **verbatim**; all facts retained. **2104 → 1803 words (−14%)**; structure identical (10 headings, 21 table rows, 8 code fences; all 16 commands present — verified by script). Stamp → 2026-07-25. `make audit` + build green.

### 2026-07-25 — README gap-analysis + compress; dev-focused; minimal MANUAL overlap
> "analyze the @README.md file aswell. See if there are gaps, compress it in the same way you compressed the manual, add missing information. try to keep the overlap between the manual and the readme at minimal. Manual is for end users, readme is for developers. Put a link/reference to the manual in the readme."
- **Gaps found + fixed:** (1) no code/architecture map → added **Layout** table (`cmd/synckeeper` + all 17 `internal/` packages, one-liner each; verified matches `ls internal/` exactly); (2) incomplete make targets → listed `vet`/`clean`/`audit`; (3) no `-race` / env-gated suites → added (`SYNCKEEPER_LIVE_TEST`/`SOAK_SECONDS`/`SCALE_FILES`/`FUZZ_*`, pointer to CLAUDE.md); (4) end-user `synckeeper init` quick-start in a dev README → replaced with a **Usage → MANUAL.md** pointer (kills the MANUAL overlap); (5) added a durability-invariants pointer (spec §3).
- Prose compressed caveman-style, but README **grew 451 → 638 words** because the code map + missing dev info is net-new value (the point of "add missing information"). MANUAL linked from README in 3 places (doc map, Usage, footer).
- CLAUDE.md doc-map README row updated: "developer-facing … code layout … or the package layout". `make audit` + build green.

### 2026-07-25 — strengthen CLAUDE.md rule: check README + MANUAL every task; dense-prose house style
> "now add or modify if exists a rule for agents to make it more strong to after each task check and if necessary update the readme and the manual and to use the same minimalisitic but information dense rule i gived to you in the previous prompts."
- **Hard rules:** replaced the MANUAL-only same-commit rule with a stronger, mandatory **end-of-task check of BOTH `README.md` and `MANUAL.md`** (update in the same commit if the change touched their scope; keep them split with minimal overlap — usage only in MANUAL, layout/build/test only in README; README points to MANUAL). Added a new **"Doc prose is minimalistic and information-dense"** rule codifying the caveman-compress style Max taught this session: terse fragments, drop filler/hedging, scannable tables/bullets, **preserve every fact**, **copy code/commands/flags/paths/links/versions verbatim**, tighten docs that drift verbose.
- Doc-map table: README + MANUAL "Update when" cells now lead with "**checked every task**".
- **Applied the new rule to this task:** it changed only CLAUDE.md governance (no commands/flags/config/behavior/Makefile/layout), so README + MANUAL need no update — checked, confirmed. No code change; build/audit green.

### 2026-07-25 — REVERSE credential model: remove embedded creds, require credentials.json, scrub history
> "[GitHub push protection blocked the push — Google OAuth client id/secret flagged] … I want to reconsider my decision on this. I want you to remove the hardcoded google credentials from the code and remove also the credential files that was commited (remove it from git not from my file system). Remove it from ALL the git history … So the security decision is that the tool REQUIRE the credentials file. Update also the documentation about this decision."
- **Diagnosed:** secret in HEAD `credentials.go:22-23` + history commits `d7b485f`/`49c5f7f` (incl. committed `client_secret_*.json` at repo root); none in commit messages. `git filter-repo` not installed (will install for the scrub).
- **Phase 1 — code (committed before the rewrite):** `credentials.go` `ClientID`/`ClientSecret` → empty `var`s; `resolveClient` returns `ErrNoCredentials` when no `credentials.json` and no `-ldflags` creds; `CredentialEmbedded` label → "build-time embedded (-ldflags)"; `ErrNoCredentials` message points at MANUAL §5; audit allowlist drops `credentials.go`. Tests rewritten (`TestResolveClientRequiresCredentials`, `TestResolveClientEmbeddedViaLdflags`, save/restore the vars). R23 unaffected (writes its own creds). `go build && go vet && go test ./...` green.
- **Phase 1 — docs (same commit):** spec §9 credential model reversed (dated); MANUAL §1 (credentials required first), §5 rewritten "Google credentials (required)", §9 limitation reframed; CLAUDE.md Hard rule reversed ("no embedded credentials; never re-add"); README layout line; plan.md W5.5-S1 REVERSED note; testing.md S1-rev row + S4 row de-stale; decisions.md "REVERSED W5.5-S1"; status.md.
- **Phase 2 — history scrub (next):** `git filter-repo` to remove the id/secret strings + the `.json` from all commits, preserving history/metadata. Back up first; verify no secret remains in any commit; re-add the `origin` remote. **Do NOT push — Max force-pushes.** Rotation optional (push was rejected → secret never reached GitHub).
- **Phase 2 done:** backed up (`scratchpad/synckeeper-pre-scrub.bundle`), installed `git-filter-repo` (brew), scrubbed with `--replace-text` (id + secret → REDACTED) + `--path <json> --invert-paths`. 86 commits rewritten, metadata preserved. **Verified:** secret fragment `ttaCHBYYeK` and the client-id hash → zero commits; the `.json` gone from all history; remaining `GOCSPX-` are only the audit regex/test/doc-pattern mentions (not valid tokens → GitHub-safe). Re-added `origin`. HEAD builds/tests/audit green. Committed the code+docs as `c2cb0bf` (pre-rewrite `da8b307`); doc-consistency fix `d1080c8`.

### 2026-07-25 — meaningful "credentials required" error (path + how-to + docs link)
> "What is teh behaviour if you start the service without credentials? It should fail with a meaningfull error message saying that credentials are required and telling how to obtain them in a generic way and where to put them. Better if giving even a link to google docs on how to obtain them"
- `ErrNoCredentials` shortened to a sentinel; `resolveClient` now wraps it (`%w`) with `credentialsHelp(configDir)` — a multi-line, actionable message: what's needed, 4 how-to steps (Cloud console + enable Drive API + "Desktop app" client + download), a Google docs link (`developers.google.com/workspace/guides/create-credentials#oauth-client-id`), and the **exact save path** (`<config dir>/credentials.json`), then "run `synckeeper login`/`init`". Removed the now-dead empty-check in `oauthConfig`. Surfaces via `error: %v` on stderr for `init`/`login`/`sync`/`watch` (and the launchd service log).
- Test `TestResolveClientRequiresCredentials` extended to assert the message carries the path, `console.cloud.google.com`, the docs host, "Desktop app", "Drive API"; `t.Logf` prints the rendered error. MANUAL §5 step 2 gains the same guide link. `go build && go vet && go test ./... && make audit` green. Checked README/MANUAL (rule): MANUAL §5 updated; README unaffected.

### 2026-07-25 — post-install service feedback + fix stale `info` credentials text
> "[after `service install` + `info` + `service status` on a fresh no-creds build] … In this case it would be good to have a feedback after the install command! After the install the status should run and in case the service is not running because of credentials print the issue"
- **`info` bug (my regression):** the credentials path line hardcoded "(absent — using embedded default)" — false since there's no embedded default. Fixed → "(absent — required, see below)".
- **`service install` feedback:** after the install message, `service install` now `time.Sleep(2s)` (let launchd start/crash the daemon) then `reportServiceStartup(os.Stdout, service.Status, credCheck)`: prints "Service is running." when up; when down, prints "Installed, but the service is not running." + (if creds unresolved) the full actionable `ErrNoCredentials` help, else points at the service log; a `service.Status` error is surfaced. **Scoped to the standalone `service install`, NOT the `init` offer** — during `init` the lock is still held, so a status check there would falsely read "not running". Injected `status`/`credOK` funcs → OS-independent unit test `TestReportServiceStartup` (4 paths).
- MANUAL §3 service row notes the post-install check; testing.md `svc-fb` row. `go build && go vet && go test ./... && make audit` green. Checked README/MANUAL (rule): MANUAL updated; README unaffected.

### 2026-07-25 — onboarding-order UX fixes (lock error, service-first steering, docs)
> "[real-machine test: service install before auth → crash-loop; wrong credentials.json → parse error; then login/init blocked by 'another instance is running (lock)'] … There is some issues with your documentation. I tried what you suggest in the guide but it does not work as expected even after i put the credentials inside."
- **Diagnosed:** the user installed the service *before* authenticating with the new required-creds binary. The daemon crash-looped (no creds), they placed a wrong file (parse error), fixed it, launchd auto-started the daemon (valid creds + existing token) → it holds the instance lock → `login`/`init --adopt` fail with a bare, unhelpful "another instance is running (lock)". Setup was actually working; the path there was confusing.
- **Fixes:** (1) `guards.AcquireInstanceLock` error is now actionable — "stop the login service first with `synckeeper service uninstall` (or Ctrl-C a `watch` terminal), then retry" — so `init`/`sync`/`login` all guide the user; `login.go` drops its now-redundant wrap. (2) `credentialsHelp` no longer ends with "Then run `synckeeper login`/`init`" (misleading once the service is running) and adds a "not an API key or service-account key" hint for the parse-error case. (3) `reportServiceStartup` down-no-creds message now explains the service runs `watch` which can't sign in itself → stop it, `init`/`login`, reinstall (instead of the conflicting "run login"). (4) MANUAL: §3 init row notes it takes the lock; §5 "don't `service install` before signing in" + re-auth needs the daemon stopped; §7 two new recovery rows ("another instance is running", "service crash-loops with no credentials").
- No behavior/logic change beyond messages; existing tests still assert the content (`TestReportServiceStartup`, `TestResolveClientRequiresCredentials`). `go build && go vet && go test ./... && make audit` green. Checked README/MANUAL (rule): MANUAL §3/§5/§7 updated; README unaffected.

### 2026-07-25 — Linux plan: doc scan + real-hardware survey (discussion, no code)
> "Scan the project documentation and let's discuss the Linux plan. I am now on linux machine. The tool is already running on my other mac machine and is syncync"
- Read status.md / plan.md / spec.md (§5, §10, §13, §15, §16, roadmap) + MANUAL §1/§2/§9, README, testing.md tail, worklog tail. State: W1–W5.5 + W11 done; open non-hardware item W10; **W6 blocked on a second machine — this Linux box is it**, so W6 and W7 converge here.
- **Machine survey (pop-os, Pop!_OS 24.04, COSMIC/Wayland, go 1.26.5 linux/amd64):** `go build && go vet && go test ./...` **green on Linux**; ext4 (case-sensitive, no normalization folding → both probes return false = safe default); `inotify max_user_watches=127469`, `max_user_instances=1024`; `ulimit -n` 1048576 (fd pressure irrelevant); systemd user manager present, **`Linger=no`** (daemon dies at logout); `xdg-open` present. Clean slate: no `~/.config/synckeeper`, no `~/Synckeeper`, no systemd unit, no running daemon; `dist/synckeeper` is a fresh native ELF build (15:08).
- **W7 code items identified (from reading the port-relevant code, not yet built):** (1) `fsnotifyBackend.needsRebuild()` hardcodes `true` for kqueue's per-file fd leak — inotify has no such leak, so the 500-cycle teardown/recreate is pure waste + a blind window on Linux → make it per-platform (W3.4 shape). (2) Linux's watcher failure mode is `ENOSPC` on `max_user_watches`, not `EMFILE`; the latch→polling path covers it but the cause is never named → diagnostic naming `fs.inotify.max_user_watches`. (3) systemd unit: `After=network-online.target` is a no-op in a *user* unit; `Restart=on-failure` vs launchd's always-restart `KeepAlive`; no linger → decisions. (4) W5.5-S3 log-privacy equivalent: Linux logs to journald (`LogPath()` = "" → `RestrictLogToOwner` a no-op); the user journal records synced file *names* and is readable by root/`adm`/`systemd-journal`. (5) MANUAL/README platform claims ("macOS primary; Linux … not yet validated") + service row (journalctl, not the launchd log path) need updating once validated.
- **Proposed order (put to Max):** W6 rollout on this box first (binary install path → `credentials.json` copy → distinct `machine_name` → `login` → `init --adopt` → **service install last**), then the W7 code items informed by it, then the Linux gates (`-race`, 50k scale, 2 h soak detached). No code, spec, or plan change in this prompt.
- **Max's answers (same prompt):** (1) install path `~/.local/bin/synckeeper`; (2) `credentials.json` already copied — verified present, `installed`-type Desktop client; (3) machine name: take it from the OS, **prompt the user when that fails** — "always prompt, hostname pre-filled" chosen, added to the plan as W7-L1; (4) rollout step `login` → `init --adopt` ok; (6) **the day-under-`watch`/`doctor`/reboot validation is Max's own** — removed from agent scope. Linger: **no** (Linux syncs while logged in). Logs: **redirect to a `0600` file**, not journald (W5.5-S3 parity).
- **Found preparing the rollout (now W7-L6):** the hand-created config dir is `0775` with `credentials.json` `0664` (world-readable client secret) — `config.Dir()` only applies `0700` when *it* creates the dir, and `credentials.json` is never perm-checked (unlike `token.json`). Told Max to `chmod 700`/`600` now; planned fix is tighten-existing-dir + **warn** (not refuse) on group/world-readable credentials.
- **Docs written:** plan.md — W6 `unblocked … Max performs it` + the agreed rollout sequence; **W7 rewritten from a one-line stub to `in progress` with items L1–L8** (machine-name prompt; owner-only log file + the L2b log-growth flag; per-platform `needsRebuild` — inotify has no per-file fd leak, so the 500-cycle rebuild is waste; inotify-limit diagnostics; systemd unit corrections incl. no-linger; config/creds perms; docs-after-gates; the `-race`/50k-scale/2 h-soak gates) and the measured machine facts. decisions.md — "W7 opened on real Linux hardware; four platform calls; W6 is Max's to run". status.md — Updated line, Next (W7 active, order L6→L1→L2→L5→L3→L4→L8→L7), W6 unparked.
- Checked README/MANUAL (rule): **deliberately unchanged** — every platform claim they carry ("macOS primary", "Linux … not yet validated") is still true until the L8 gates run; L7 updates them in that commit. No code/spec/behavior change this prompt.

### 2026-07-25 — roadmap recap + Linux file-watching research (Dropbox) and a bounded-watcher proposal
> "what is the roadmap now. be short and precise" / "i want to discuss how the file monitoring work on linux. On mac we used specific to macos code for that, what about linux? is this part already implemented?" / "Search online how this is handled by dropbox on linux and prepare me a proposal. The goal is to be lightwaight and not have potential risk of growing inititly with open files or similar issues even if it will require more development. The engine should be stable and lean"
- Roadmap answered from plan.md (W7 L6→L1→L2→L5→L3→L4→L8→L7; W6 = Max; then W10 → W8 → W9; dir-arm still unscheduled).
- **Linux watching is already implemented** — `fsnotifyBackend` is it, and verified in the module cache that fsnotify v1.10.1 on Linux **is** inotify (`unix.InotifyInit1` + `unix.InotifyAddWatch`, pure Go over `x/sys`), i.e. the same syscalls a hand-written backend would issue. macOS needed cgo only because fsnotify's darwin backend is kqueue (one fd per *file* → 403/500 dirs unwatchable at 50k, W3.3); that failure mode cannot occur on inotify, which watches **directories** (1080 B kernel memory each) from a single instance fd.
- Also found while reading: `ErrEventOverflow` (inotify `IN_Q_OVERFLOW`, 16384-event queue) is only logged (`fswatch.go:131`) — an overflow means events were dropped, so we may wait a full poll interval; it should wake the loop.
- **Research (Dropbox + peers):** Dropbox on Linux = one inotify watch per folder, subfolders counted separately; over the limit it posts "Unable to monitor entire Dropbox folder hierarchy" and tells the user to raise `fs.inotify.max_user_watches` (support recommends 100000). Syncthing does the same (warn + raise the sysctl). Neither bounds its own usage. Limits are **per user, shared with every other app** (`ENOSPC` when exceeded). Unprivileged fanotify (Linux ≥ 5.13) cannot `FAN_MARK_MOUNT`/`FAN_MARK_FILESYSTEM` without `CAP_SYS_ADMIN` and gets no `FAN_UNLIMITED_MARKS` → per-inode marks only, no advantage over inotify.
- **Measured on `pop-os`:** `max_user_watches=127469` (kernel default — only `max_user_instances=1024` is set by Pop's sysctl.d), **1640 watches already held by other apps**, 83 of 1024 inotify instances in use.
- **Proposal put to Max (bounded watcher, W7-L9 if accepted):** P1 declared watch budget (`watch_max_dirs`, breadth-first coverage, honest partial-watching in `status`) — strictly better than Dropbox/Syncthing's "edit sysctl"; P2 incremental watch registry diffed from the directory set `scanner.Scan` already returns, replacing the per-cycle second walk + blind re-`Add`; P3 overflow → immediate wake; P4 `ENOSPC` self-diagnosis (= L4); P5 no rebuild on inotify (= L3). Rejected: fanotify, fsnotify's recursive `Add` (no ignore pruning), a cgo backend, per-file watches. No code/doc change yet — awaiting Max's call.
- **Max's call:** "I would be more comfortable to stay with what dropbox and others are doing in this space" → **bounded-watcher proposal rejected**; Linux keeps the conventional one-watch-per-directory model, no budget/partial-watch/config knob, no registry refactor. Surviving items: L3 (no rebuild on inotify), L4a (name `fs.inotify.max_user_watches` + value + sysctl remedy when the latch trips), L4b (overflow wakes the loop). decisions.md "Linux file watching stays conventional" records the findings, the rejected proposal, the accepted trade, and the closed options (fanotify, recursive `Add`, cgo backend, per-file watches) so they aren't re-derived; plan.md W7-L4 rewritten as L4a/L4b; status.md updated.

### 2026-07-25 — W7-L6: config-dir + credentials.json permission hardening
> "let's solve the open points in an iteractive way"
- Working W7 in the agreed order, one item per iteration. **L6 done.** `config.tightenDir` (new) chmods an existing config dir back to `0700` on every `Dir()` — `MkdirAll`'s mode applies only when it *creates* the dir, and MANUAL §5 tells users to create it by hand for `credentials.json`, so a hand-made dir kept the umask (observed here: `0775` dir, `0664` creds = world-readable client secret). Best-effort: an un-makeable chmod warns, never fails a command; no-op on Windows.
- `auth.loosePerms` + a `slog.Warn` in `resolveClient`: a group/world-readable `credentials.json` is **used with a warning** (path, mode, `chmod 600 <path>`), never refused — it's the user's own file and refusing would block onboarding; deliberate contrast with `token.json`, which `LoadToken` still refuses outright (we write that one).
- Scope addition (small, user-visible): **`info` reports the `credentials.json` mode** beside `token.json`'s and flags a readable file; `--json` gains `credentials_mode`/`credentials_loose`.
- **Red-first verified**: with `tightenDir` disabled the dir stayed `0755` (test failed); with the warn disabled the captured log was empty (test failed). Restored, both green.
- Tests: `TestDirTightensExistingConfigDir`, `TestDirCreatesPrivateConfigDir`, `TestLoosePerms`, `TestResolveClientWarnsButAcceptsLoosePerms`, `TestResolveClientQuietOnTightPerms`, `TestPrintInfoCredentialsPerms`, `TestPrintInfoJSONCredentialsPerms`. Docs same commit: spec §9 (file-permissions rule) + §13, MANUAL §5 (`chmod 600`) + §3 (`info` row), testing.md new "W7 — Linux port" section with the W7-L6 row, plan.md L6 → done. `go build && go vet && go test ./... && make audit` all green. Checked README/MANUAL (rule): MANUAL updated; README unaffected (no build/layout/target change).

### 2026-07-25 — field incident on the Linux rollout: a Drive-bin delete came back as a re-upload
> "[activity wall of `error upload …: no such file or directory`] I added a directory … deleted the directory on Drive (moved to bin). Nothing synced … Then i deleted manually the directory on this machine. Still nothing happened on the mac machine." / "[mac logs: `drive→local quarantine kenney_…`] I wonder if the quarantine folder should be deleted on the client if it is moved to thrash on drive … I removed the folder from drive, and then since i did not see it removed on filesystem on linux i deleted it also on linux by hand." / "let's note in documentation that there might be some open bug on linux and add the bug description. About the quarantine i don't like it because it confuses it."
- **Investigated read-only on the machine** (no state mutated): daemon pid 91741 up since 18:46:48; journal cycles `18:47:05 actions=1` → `18:54:32 actions=1145 executed=890 failed=255` → `18:54:34 discarding stale pending ops count=255` → `18:54:57 actions=890 executed=890 failed=0`; activity ring = 1 `trash credentials.json` + 256 `error` rows; DB now `items`=1, `remote_nodes`=1, `pending_ops`=0, quarantine empty, cycles idle. **No `mass-delete guard blocked` line anywhere in the journal.**
- **Corrected the user's reading:** the 255 errors are **uploads**, not failed trashes — the engine was pushing the deleted tree *back* to Drive (890 files recreated), not deleting it twice. Trashing an already-trashed Drive file is a no-op.
- **Mac behaved correctly** (`drive→local quarantine kenney_…` 19:05:13), so the deletion path itself is sound; **nothing was lost** (Drive bin + Mac quarantine hold copies).
- **Wrote R26** `TestRemoteFolderTrashPropagatesLocally` (`internal/engine/remotetrash_test.go`): folder trashed in Drive (children untouched — real Drive semantics via `Fake.Trash`) → no upload/mkdir_remote planned, local files removed, second cycle idle. **Passes**, so the plain path is correct and the Linux deviation is machine-state-specific. Suspect: `init --adopt` still running when `service install` first started the daemon (journal `another synckeeper instance is running (lock)` 18:46:37, restart 18:46:48) → part of the tree on disk but never recorded.
- **Docs (this commit):** MANUAL §8 open-bug entry with workaround; **plan.md W12** (F1 resurrection, F2 guard never fired, method F2→F1); testing.md W12 section (R26 passing + F1/F2 open); decisions.md "Field incident" with the full evidence; status.md rewritten to put W12 on top. Probe file `zz_probe_test.go` deleted (tree clean, per the probes-are-throwaway rule).
- **Open with Max:** the quarantine UX — he finds it confusing that a folder deleted in Drive leaves an unmarked copy on disk. Options put to him next; my recommendation is to keep the local copy (Drive's bin expires in 30 days and can't cover un-uploaded local edits) but make it visible, not silent.
- **W13 planned in detail and moved ahead of W12** (Max: "I agree with trash the highest directory whose entire subtree is being deleted. Prepare a plan with this and move this as next item in our plan. This needs to be done for all platforms that we currently supported at least (mac and linux)"). plan.md W13 rewritten as a buildable spec: T1 `internal/trash` OS module (Linux freedesktop spec in pure Go incl. the `O_EXCL` `.trashinfo` name claim and the per-volume `.Trash-$uid` cross-filesystem rule; macOS `NSFileManager trashItemAtURL:` via a second cgo file; `ErrUnavailable` stub elsewhere), T2 the agreed collapse rule **with the mass-delete-guard trap called out** (collapsing 1145 file deletes into one dir action would make `CheckMassDelete` count 0 — A2's mirror image, so the guard runs *before* the collapse and the action carries `SubtreeFiles`), T3 executor + write-gate primitive with the quarantine as fallback (never a hard delete), T4 `DeleteItemsUnder`, T5 one activity entry per subtree, T6 docs + 7 testing rows. Scope confined to the `QuarantineLocal` action (`plan.go:317`/`:586` — both remote-initiated); the executor's rescue-copy uses of the quarantine dir are untouched. status.md records the order W13 → W12 → W7 and that the agent's W12-first recommendation was overridden by Max.
- **W12-F2 investigated → not a defect; incident record corrected.** Two probes at incident scale (60 of 61 tracked files, `DeferMassDelete: true`), both directions: the guard fires correctly (`GuardBlocked`, executed=0, local files untouched, reason recorded). Kept as `TestMassDeleteGuardBlocksWholeTreeDeletion`. **Consequence:** the incident's cycles held no delete-class actions, and since `activityKind` leaves only `Record`/`Forget` unrecorded while those cycles executed 890+890 actions with **zero** activity rows, the executed actions were `Forget`s (correct — content gone on both sides). **Nothing was re-uploaded to Drive**; the only uploads were the 255 that all failed at the write gate. Corrected in MANUAL §8, plan.md W12, testing.md W12-F2, status.md, decisions.md — my earlier "890 files were recreated in Drive" was wrong.
- **F1 narrowed** to: (a) 4 minutes of cycles planning *nothing* while 1145 tracked files had a trashed remote (should have been 1145 quarantines + a guard block), and (b) 255 files judged untracked-new. Next step: reproduce an interrupted `init --adopt`, then a Drive-bin delete against that state.
- **Re-planned W12 ahead of W13** (Max: "if you reccomend so let's replane W12 over w13"); W13's plan unchanged.
- **F1 narrowed again.** `TestRemoteTrashDuringPartialLocalDeletePlansNoUpload` reproduces the incident's exact race (Drive trash + `rm -rf` in flight during the scan) and the engine handles it **correctly**: 40 `forget` + 20 `quarantine_local`, zero uploads, guard blocking the quarantines. Since the field's equivalent 255 came out as **uploads**, those files provably had **no baseline rows** while their 890 siblings did. Engine behaviour is correct given that state — the open question is now only *how 255 files came to be on disk untracked*. The journal proves no daemon cycle touched the tree before 18:54:32, so it was the foreground instance (lock holder at 18:46:37) that put it there; its output went to Max's terminal, not journald. Asked Max what that command was.
- **W12 parked at Max's call** ("Let's ignore this. I don't want to discuss this bug anymore. We will discuss it eventually if it come back. As for now record the note about it in the documentation"). Documentation was already in place and stays: MANUAL §8 user-facing entry (corrected — no re-upload reached Drive), plan.md W12 (now `parked`, with the unanswered question and the two experiments to run if it recurs), testing.md rows (R26, W12-mixed, W12-F2 all passing; F1 open), decisions.md two entries. He confirmed the command was `init --adopt`; the terminal scrollback that would have closed it was not available. **W13 is now the active workstream.**

### 2026-07-25 — W13-T1: the `trash` OS module
> "Let's ignore this [W12] … let's move over please"
- **`internal/trash` built.** `MoveToTrash`/`Available`/`Describe` + `ErrUnavailable`. **Linux** (`trash_linux.go`, pure Go, freedesktop spec): `<trash>/files/<name>` + `info/<name>.trashinfo`, the name claimed by creating the info file `O_CREATE|O_EXCL` (the spec's own race protocol), collision numbering inserted **before the extension** to match the desktop's own trasher (`notes.2.txt` — observed in `gio trash --list`), `Path=` percent-encoded via `url.URL.EscapedPath`, `DeletionDate` in the spec's format; cross-filesystem items use `<mount>/.Trash-$uid` (or sticky `<mount>/.Trash/$uid`) with `Path=` relative to the volume root, mount point found by walking up until the device id changes (`deviceOf` is a test seam). **macOS** (`trash_darwin.go` + `trash_darwin.m`): `NSFileManager trashItemAtURL:` behind `-framework Foundation` — Finder's own API, so "Put Back" and per-volume `.Trashes` are the OS's job. **Fallback** (`trash_other.go`): `ErrUnavailable`, so pure-Go darwin and Windows compile and fall back to the quarantine.
- **Verified on real hardware:** trashed a file through the module into the actual desktop trash; `gio trash --list` shows it with the right restore path, and the `.trashinfo` reads correctly. 7 Linux tests + 3 darwin tests; `CGO_ENABLED=0 GOOS=darwin` and `GOOS=windows` builds green. **The darwin path is written but not compiled anywhere yet — it needs one `go test ./internal/trash/` on the Mac.**
- Noticed while checking the desktop trash: **Max's local `kenney_ui-pack-space-expansion` is in his Linux desktop trash** (`trash:///kenney_ui-pack-space-expansion → /home/max/Synckeeper/…`), i.e. his "deleted by hand" was a file-manager trash, and that copy is still restorable.
- testing.md W13 section (T13.1/2/3/8/9 passing, T13.10 unverified-on-Mac); plan.md T1 done. Next: T2 (collapse pass) + T3 (executor wiring).

### 2026-07-26 — status check: what's next
> "what is the next step in the plan?"
- Read status.md + plan.md W13. Answer: **W13-T2** (collapse pass — trash the highest fully-deleted directory, guard runs *before* the collapse, action carries `SubtreeFiles`), then T3 (executor/write-gate `trashLocal` with the quarantine as fallback). Outstanding side item from T1: the darwin trash path is written but never compiled — needs one `go test ./internal/trash/` on the Mac.
- No code, spec, or plan change this prompt.

### 2026-07-26 — W13-T2…T6: the collapse, the bin, and everything that guards them
> "let's proceed with the plan as per documentation"
- **T2 — collapse pass.** `reconcile.CollapseDirDeletes` (pure, `internal/reconcile/collapse.go`): a directory delete absorbs its descendant local deletes when **every** planned action strictly under it is delete-class (`trash_remote`/`quarantine_local`/`forget`); any upload/download/record/move (either end) cancels it; highest qualifying directory wins; siblings collapse independently; a lone directory delete is untouched. The action carries `Subtree []SubtreeEntry{RelPath,IsDir,FileID,Size,MtimeNS}` + `SubtreeFiles`. Called by the engine **after** `guards.CheckMassDelete` and only when `trasher().Available()` — no bin means today's proven per-item quarantine, not a second new road.
- **T3 — executor + gate.** `quarantineLocal` → `trashLocal`. New gate primitives in `localwrite.go`: `guardedTrashPath` (per file, expectation enforced first — a drift refusal never falls through to the quarantine), `guardedTrashDir` + `verifySubtree` (walk the folder, every visible entry must be covered *and* match the scan's stat; ignored/temp skipped whole and ride along inside the folder — R20 via the bin). Ladder: verify → move folder to bin → on `errSubtreeUnexpected` **or** `errTrashUnusable`, `removeDirPerItem` (each covered file bin-then-quarantine, then dirs emptied deepest-first). Never a hard delete. `gate_test.go` extended: `MoveToTrash` outside `localwrite.go` now fails the AST gate.
- **T4 — rows.** `statedb.DeleteItemsByID` (batched 400), **not** the planned `DeleteItemsUnder(relPath)`: a delete's rel_path can be a post-move local path while its rows are keyed at the baseline path, so a prefix delete would miss them (or reach uncovered rows). decisions.md records it.
- **T5 — visibility.** `activityKind(QuarantineLocal)` → `trash` (direction `drive→local` distinguishes it from a remote trash), detail `(N files)` for a collapsed action, and `quarantine` when `Result.TrashAvailable` is false; `info` prints `system bin  <trash.Describe()>` and `paths.system_bin` in `--json`.
- **Beyond the plan (both recorded):** `guards.CheckMassDelete` now also counts a directory delete's `SubtreeFiles`, so the "guard before collapse" ordering can never silently become load-bearing again (uncollapsed dirs still count zero — R10 intact); and the execution-time re-check is exact (carried stats) rather than a file count, which is what preserves edit-beats-delete for every absorbed file.
- **Red-first:** disabling `verifySubtree` fails `TestCollapsedDirRefusesUnexpectedSurvivor`, `TestCollapsedDirRefusesEditedFile` and `TestR20QuarantinedDirStillRefusesUnexpectedSurvivor`; restored, all green.
- **The suite must never touch the developer's own bin.** An early run did — four items landed in Max's real Linux trash (`docs`, `f.txt`, `f.2.txt`, `f.3.txt`, all with `Path=/tmp/Test…`); removed them, his own entries untouched. Now `trash.Fake` (new, `internal/trash/fake.go`) is pinned as the package default in `TestMain` for both `internal/engine` and `internal/executor`, and the engine harness gives each machine its own bin.
- **Tests:** 6 collapse unit tests (reconcile), 5 executor tests + an R20 fallback test, 3 engine tests (T13.1 one-entry / T13.5 guard / T13.7 crash at `CPTrashBeforeCommit`), 2 activity tests (watch), 1 guards test, info assertions. Updated for the new destination: S5, S7, R13, R20, R21 (engine+executor), and W12's two rows (now count `SubtreeFiles` via a `plannedFileDeletes` helper).
- **Docs same commit:** spec §3 invariant 3 (rewritten) + §4.2 (collapse rule + table row) + §10 (`trash` module row); MANUAL §3/§6/§7 + intro + date; README layout (`internal/trash`) + pitch line; testing.md T13.4–T13.15 and R13/R20/R21 restated; plan.md T2–T6 done + an as-built note and the two open verifications; decisions.md "W13-T2/T3"; status.md rewritten.
- **Gates:** `go build ./... && go vet ./... && go test ./... && make audit` green; `-race` green on reconcile/executor/guards/engine; `CGO_ENABLED=0 GOOS=darwin` and `GOOS=windows` builds green.
- **Found, not fixed, not mine:** `internal/watch`'s `TestControlPauseSuppressesSyncThenResume` is flaky **on pristine master** (~1 of 3 uncached full-package runs; verified by stashing everything): a cycle already in flight when `pause` lands finishes and uploads. Correct daemon behaviour, racy test. Recorded in status.md as a loose end rather than folded into W13's commit.
- **Open for W13:** the darwin trash path still has never been compiled (T13.10 — one `go test` on the Mac), and the real-hardware acceptance is Max's.

### 2026-07-26 — the field's "deleted folder won't go away": the guard, and an activity report that lied
> "Ok i uploaded a folder called \"TEST kenney_ui-pack-space-expansion (Copy 1)\". then i checked that the folder is synced in all platforms. It is. Then i moved the folder into the bin on gdrive. The folder is still in Synckeeper on this linux machien and on the other macos. I pulled the lates code on both machines and rebuild and reinstalled the software on bot..."
- **Investigated read-only on `pop-os`.** `status`: daemon running, `guard: BLOCKED — plan deletes 1117 of 1118 tracked files (over the 25% mass-delete threshold)`; journal from 13:01:10 onward: every cycle `actions=1 executed=0 failed=0` + the guard WARN with its reason. **Not W12-F1 recurring, and not a propagation failure:** the remote trash IS seen, the deletion IS planned, and W13's collapse turned 1117 file deletes into **1** action. The mass-delete guard is holding it, exactly as spec §6/§8.1 say it should — the sync dir holds one other file (a PDF), so deleting that folder is deleting ~all tracked content. Resolution for the user: `synckeeper sync --confirm-deletes`, **on each machine**.
- **Real defect found and fixed (red-first): `activity` claimed deletions that never happened.** `recordActivity` iterates `res.Plan`, which deliberately keeps the deferred deletes so they stay visible — so a guard-blocked cycle recorded `drive→local trash TEST kenney… (1117 files)` **once every 45 s** while the folder sat on disk. Pre-existing (before W13 it would have been up to 1117 `quarantine` lines per cycle); W13's collapse made it legible enough to spot. Fix: skip delete-class actions when `res.GuardBlocked`, mirroring the engine's `withoutDeletes`. Test `TestActivityNeverClaimsGuardBlockedDeletes` (verified red first: it recorded both the collapsed `trash` and a `trash_remote`); testing.md T13.16.
- **MANUAL §6 mass-delete bullet extended** (same commit): deleting one big folder trips the guard whenever it holds most of your files; the block is the guard working and `status` names the count and reason; confirm **on each machine**; held-back deletions are not listed by `activity`.
- Suite green (`go build && go vet && go test ./...`).

### 2026-07-26 — W14 planned: the mass-delete guard becomes a recoverability guard
> "I don't like this rule. I think we should remove it and keep it only in case the BIN is not available so the deletion is permanent and not just a move into the bin. In that case this needs to be notified if it is not. Prepare an implementation plan for this feature and put it as next step in the plan"
- **Stated the cost before writing the plan** (it amends spec §3 invariant 4): the guard is the only thing between a deletion the user *never asked for* — engine bug, interrupted `init --adopt`, corrupt baseline, subtree lost to an unmounted volume (W12's shape) — and its execution everywhere. After W14 such a plan runs, and recovery is bounded by someone else's clock (Drive bin ~30 days; GNOME/macOS can auto-empty at 30 days) where the guard's window was unlimited. Recorded as accepted by Max, with two agent-proposed compensations he can drop.
- **plan.md W14 written as a buildable spec, marked NEXT** — M1 guard fires on destination not volume (`TrashRemote` never guarded; `QuarantineLocal` guarded only when `trash.Available()` is false, keeping the ratio rule + `--confirm-deletes` for that case, `SubtreeFiles` still counted); M2 an absent bin becomes a standing visible state (`status` line, `doctor` check, one startup WARN, an activity entry on the first per-item fallback); **M3** report a large deletion loudly instead of blocking (WARN + activity + a `status` field, so it is noticed inside the bin's retention window); **M4** collapse remote trashes the mirror-image way (a locally-deleted folder → **one** `trash_remote`, one Drive-bin entry, one API call instead of 1145; verified against the remote mirror, not extra API calls); M5 compatibility (`mass_delete_threshold` stays a config key — unknown keys are rejected — with narrowed meaning; `--confirm-deletes` stays; **no CLI surface change, DOC1 manifest untouched**); M6 docs (spec §3 invariant 4 *amended*, its sync-dir sentence verbatim; §6, §8.1; MANUAL §6/§7/§4); M7 tests (G1/G3/R10/T13.5 and W12-F2's row are **re-targeted, not deleted**, plus M14.1–M14.6; G2/F5 must keep passing untouched — the proof `CheckSyncDir` survived).
- **`CheckSyncDir` explicitly out of scope** in plan, decision and spec-amendment instructions: an unmounted or empty sync dir is never a deletion, guard or no guard.
- decisions.md entry (context = today's field report, decision + accepted cost + what stays unchanged); status.md header + Next rewritten (order: **W14 → W13 close-out → W7 → W6 → W10**); plan.md gained a current-order line.
- No code changed this prompt.

### 2026-07-26 — W14 implemented: the guard fires on recoverability, not volume
> "Let's go with the implementation"
- **M1** `guards.CheckMassDelete(plan, trackedFiles, threshold, confirmed, binAvailable)`: counts only `QuarantineLocal` (files; `SubtreeFiles` for a collapsed folder) and only when `binAvailable` is false. `TrashRemote` never counted — Drive's bin holds it. Everything downstream (one-shot abort, daemon defer + `status` block, `--confirm-deletes`) unchanged for the case that remains. The engine computes `res.TrashAvailable` **before** the guard and passes it, so plan-time and execution-time share one fact.
- **M2** absent bin = standing state: shared `systemBinLine()` used by `status` and `doctor` (names the quarantine fallback + the flag), `Report.SystemBin`/`SystemBinOK`, one WARN at daemon startup, and a per-cycle `activity` line counting items the bin *refused* (`Summary.QuarantineFallbacks` → `Result.QuarantineFell`) — invisible to a capability probe, so it is reported from execution.
- **M3** large deletions reported, not blocked: `Result.DeletedLocal`/`DeletedRemote`/`LargeDeletion` (files, threshold + >10 floor, skipped when actions failed), an engine WARN, and a `deleted` activity entry naming the count and where to restore from.
- **M4** remote collapse: `CollapseDirDeletes(plan, remote)` grew the `TrashRemote` arm — a locally-deleted folder becomes **one** Drive trash call (asserted via new `Fake.TrashCount()`), one restorable bin entry, `SubtreeFiles` carried; refuses when the remote mirror holds an item under the folder the plan never planned to delete; the two directions never absorb each other. `trashRemote` retires the covered rows via `subtreeIDs`.
- **M5** no CLI/config surface change (key + flag stay, meanings narrowed) → DOC1 manifest untouched.
- **M7** the guard rows that changed meaning were **re-targeted, not deleted**: G1, G3, W12-F2's `TestMassDeleteGuardBlocksWholeTreeDeletion` (now `no system bin` / `with a system bin` subtests) and T13.5 all run against `bin.Unavailable`; new engine tests M14.1/M14.2/M14.3/M14.5, guards tests for both directions and both bin states, watch tests for the reports, `TestSystemBinLine` in cmd. `CheckSyncDir`'s rows (G2/F5) untouched and passing — the proof the other guard survived.
- **Flaky test fixed in its own commit** (found while gating W13, pre-existing on master): `TestControlPauseSuppressesSyncThenResume` now waits for the startup cycle before pausing — a cycle already in flight when `pause` arrives legitimately finishes and uploads. `-count 8` + 3 uncached full-package runs green.
- Docs same commit: spec §3 invariant 4 (amended, sync-dir sentence verbatim) + §6; MANUAL §3/§4/§6/§7 (guard bullet rewritten again — the version written this morning described the old rule); testing.md W14 section M14.1–M14.8 + G1/G3 rows re-targeted; plan.md W14 done + as-built notes; decisions.md "W14 as built"; status.md. README checked: unaffected (no layout/build/target change).
- Gates: `go build ./... && go vet ./... && go test ./... && make audit` green.

### 2026-07-26 — the folder went to the bin; the `activity` noise explained
> "I check and indeed the folder was moved into the bin. Why i have so much noise in the activities: [20 repeated `drive→local trash TEST kenney… (1117 files)` lines]"
- **W14 verified on real hardware**: journal shows the old daemon (pid 410879) blocked every 45 s until 17:44:21, then the new build (pid 711154) started 17:44:34 and its first cycle was `actions=1 executed=1 failed=0` — the folder moved to the desktop bin as one entry, no confirmation asked. Max confirms it is in the bin.
- **The noise is frozen history, not a live bug**: every line from 16:20 to 17:43:36 was written by the pre-`6faad6b` binary, one per guard-blocked cycle, claiming a trash that never ran. The fix stops new ones; the existing rows stay until they age out of the 500-entry ring. Verified nothing has been appended since 17:44:34.
- **Two offers, both declined by Max** (recorded so they are not re-proposed): (a) targeted delete of the stale rows from his live `activity` table — *leave them*, they age out; (b) folding the M3 `deleted` summary into the collapsed `trash` line when a large deletion is already one entry — *keep both*: the trash line says what happened, the summary says where to restore from.
- No code change this prompt.
