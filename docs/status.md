# Status — session-to-session state pointer

The single answer to "where are we?". Read this first; **rewrite it before ending any work session** (sections replaced in place — history lives in `git log` and [decisions.md](decisions.md), not here).

**Updated:** 2026-08-01 — **W18 items C, A, D, B, E and G are done; H, F, I remain.** Two items landed today. **E** ("the init merge names the conflict winner by last edit") turned up **two pre-existing defects, both fixed with it**: the executor learned Drive folder ids too late for anything inside a folder being adopted, and pass 3 pinned a download to an occupant the same plan was about to move away. **G** (an unreadable folder no longer wedges the cycle) shipped with its mandatory half — the baseline rows under it held harmless — and that half was verified to be the load-bearing one: without it the reproduction bins a whole subtree on Drive. A third defect was **found and deliberately not fixed** — a fuzzer run at crash-probability 1.0 (a configuration this project has never used; the variable is a probability, not a boolean) wedges 2 of 40 seeds on a §4.5 upload/download overlap after a folder rename, identically at W17's close, so it is older than W18 and awaits Max's triage. plan.md W18-E/W18-G, decisions.md (three entries), testing.md W18.7–W18.13, spec §4.2/§5/§11/§13, MANUAL §1/§6/§8.

**Previously (2026-07-31):** W16, W17 and W15 all closed. W16 (field-found): the executor's remote-side commits write the Drive mirror in the **same transaction** as the baseline row, so a cycle running before Google's change feed reports our own write no longer reads "absent from the mirror" as "deleted on Drive". W17 (found *by* W16's new fuzzer capability): a crash between Drive storing a **create** and the DB commit used to make the replan create a **second same-name item**, which "first by id wins" then shadowed — recovery now **seeds the mirror** and lets §4.2 decide; `doctor` reports same-name Drive siblings read-only. W15-U6's closing reconcile pass then traced every dashboard doc claim to the code that emits it and found **three drifts, one a real rendering bug** — the degraded header read `polling-only (polling only)`. plan.md W15/W16/W17 (all done), decisions.md, testing.md E1.1–E5.2 / W17.1–W17.5 / U6.1–U6.3, MANUAL §3/§8/§10, spec §4.1/§4.2/§4.6/§12.

## Last completed

**W18-G — an unreadable folder no longer wedges the cycle, and never looks like a deletion (2026-08-01).** Review finding F4: one unreadable subdirectory failed `scanner.Scan` every cycle, permanently, visible only in the log. `Scan` now reports it as a `Skip{Unreadable: true}` and walks on — with the folder itself kept in the snapshot as a directory, so its own row is never read as deleted. **The mandatory half is the one that mattered:** `engine.expandUnreadable` drags every baseline row beneath it into `reconcile.Input.UnreadableLocal` (the local mirror of `ShadowedRemote`), and passes 1 and 4 report those rows instead of deciding them. **Verified red-first on both halves by observation** — without the scanner change the cycle hard-errors; with the tolerance in but the hold removed it plans **2 delete-class actions**, one a collapsed `trash_remote locked/deep` carrying its subtree, i.e. one lost read permission bins a whole subtree on Drive. Scope calls recorded: only *directories* are tolerated (an unreadable root is already a hard error in `EnsureSyncDir`; a file that cannot be statted inside a readable directory means the tree is moving under us and should replan — the W3 soak's 3,488 racing `lstat`s converged that way); and a *new remote* file under an unreadable folder still fails its download each cycle, since we genuinely cannot write there. **One addition the item did not list:** the daemon logs one WARN per folder per occurrence (re-armed when readable again) — skips reach only the one-shot `sync` output until W10, so a fixed wedge must not become an invisible standstill. MANUAL §8 lost its F4 entry in the same commit. Gates: build, vet, suite, `-race` on engine/reconcile/scanner, `make audit`, 40-seed fuzzer sweep — green.

**W18-E — the init merge names the conflict winner by last edit (2026-08-01).** `modifiedTime` now flows Drive → `driveclient.File` → `remote_nodes.modified_ns` (**schema v5**, one `alter table`, no backfill) → `reconcile.RemoteItem`; `Input.PreferNewer` is set by `init` and by nothing else. **The shape the plan left open is the interesting part: the loser steps aside on Drive.** Remote-wins backs the local file up on disk and uploads it under the conflict name; local-wins is the mirror — `MoveRemote` renames the Drive copy to the conflict name, the winner uploads under the plain name, the loser downloads under the conflict name. That rename is **load-bearing, not cosmetic**: uploading the winner beside an unrenamed sibling puts two items under one name in one Drive folder, which is the W17 shadowing shape. The upload is `ProtectedBy` the rename (invariant 7; `noteFailure` gained a `MoveRemote` arm). Unknown stamp and ties fall back to remote-wins — that, not thrift, is what makes the missing backfill correct. Applied to the fold-equal arm too, or the promise would hold for `notes.txt` vs `notes.txt` and quietly fail for `Notes.txt` vs `notes.txt`. **Steady-state §4.2 is untouched** and stays clock-free, which is why the fuzzer acceptance stays green. **Two pre-existing defects came out with it, both red-first** (testing.md W18.8/W18.9): (1) the executor seeded rel_path → Drive id from the baseline plus already-committed actions, so at an init merge nothing inside an *adopted* folder had a resolvable parent — a deterministic failure for E's moves-stage rename and a live race for any upload into such a folder (a `Record` is a DB write; an upload hashes first, so the upload nearly always won); a parent id is a fact about Drive, not our DB, so `Apply` seeds it from the plan itself. (2) Pass 3 pinned the §7 guard to the occupant the scan saw even when an earlier stage was planned to move it away, so the guard refused the download **every** cycle — minimized with no init merge in it (rename a file on one machine, give a new file its old name). Gates: build, vet, full suite, `-race` on the seven touched packages, `make audit`, and the documented 40-seed fuzzer sweep — all green.

**W15 CLOSED — `status` is the dashboard, and U6's doc pass found a bug of its own (2026-07-31).** U1–U5 shipped 2026-07-27/28 and were accepted on hardware; U6 was the closing reconcile pass over MANUAL §3/§10, spec §8.2/§8.3/§15 and the testing ledger, run under the project rule that **doc claims are claims about code, not intent** — every statement traced to the code that produces it rather than to the spec. **Spec was found accurate** (heartbeat 10 s, the `stat`-off-the-loop split, the flag surface). **Three drifts fixed.** (1) **A real rendering bug:** a daemon that has fallen back to polling rendered `polling-only (polling only)` — `ds.Mode` and `backendLabel()` naming the same thing twice, in exactly the state a user most needs to read cleanly. `backendLabel` is now empty when there is no backend to name; `TestPollingOnlyIsNamedHonestly` was **strengthened rather than relaxed** (its old assertion — "the view contains `polling only`" — was satisfied *by* the redundancy; it now requires the header to name polling exactly once and never say "watching"), plus a new `overview_polling` golden. (2) MANUAL §10 claimed the activity view shows the **500**-entry ring; the dashboard pulls **200** per refresh (`dashboardActivityRows`, bounded to keep the query cheap) — plan.md's U2 table carried the same wrong number and was corrected. (3) MANUAL §3's `account` row still described an "embedded default" credential, removed 2026-07-25 and **contradicted by §5 of the same file**. **The ledger was checked mechanically:** all 214 `TestX` names testing.md claims resolve to a `func TestX` in the tree. testing.md U6.1–U6.3. U7 (bytes/sec, per-file progress) stays deferred out of the workstream — it would instrument the transfer path for a cosmetic gain.

**W17 CLOSED — a crashed create no longer mints a duplicate on Drive (2026-07-31).** Found *by* W16's own new capability: once `driveclient.Fake.HoldChanges` could model Drive's eventual consistency, a 40-seed fuzzer sweep failed seeds 23 and 33 on the **convergence** oracle, and it minimized to this — a crash between Drive storing a create and the DB commit rolls **both** rows back (correct; invariant 6), so the replan has no record of it and, with the feed still lagging, creates a **second item under the same name**. Drive permits same-name siblings; §5's "first by id wins" then shadows one, and seed 33 had all three machines converged on `content-2` while the newest `content-21` sat on the shadowed node. Not loss — a silent content revert. **Scope is exactly the two non-idempotent Drive calls**, `Upload` and `Mkdir` (`Move`/`Trash` replayed twice are harmless); the **mkdir arm was reproduced before being fixed** and is the worse one — a shadowed duplicate *folder* makes everything uploaded into it invisible elsewhere. **Decided with Max in two rounds, and the shape is the interesting part: recovery decides nothing.** `executor.seedOrphanCreates` lists each stale create op's parent once and seeds any same-name child into the mirror; §4.2 then resolves it — identical content adopts, different content conflicts. Same fix shape as W16, no second decision site, and **correct when the same-name item is not ours at all**: a file made in the Drive web UI meanwhile gets the conflict it deserves (pinned). It also turned out spec §4.6 **already promised** this adopt — true only once the feed had caught up — so the code caught up to the spec rather than the spec changing. New `CPMkdirBeforeCommit` checkpoint (mkdirRemote had none, so its crash shape was unreachable) joins the fuzzer's list. **`doctor` now names a Drive folder holding one name twice** — reported, never repaired, so §12's "`--repair` only ever adds" stands. Red-first on all three arms. **The sweep that found it is green:** `SYNCKEEPER_FUZZ_RUNS=40` with crashes on. Suite + `-race` + `make audit` green. *Not taken, recorded:* stamping the new Drive id into `pending_ops` to make recovery an exact `Get(id)`.

**W16 CLOSED — the remote mirror follows our own writes (2026-07-30).** `remote_nodes` had exactly one writer, `internal/remotedelta`, fed by `changes.list`; `commitUpload` wrote the baseline row alone. Between the two, reconcile saw a baseline row naming a Drive id the mirror did not know and took the "deleted on Drive" row — `QuarantineLocal`, which since W13 means the user's system bin. **Fix:** `statedb.UpsertRemoteNodeTx`/`DeleteRemoteNodesTx` (tx-scoped) plus one `Executor.mirror` call at four commit sites — `commitUpload` (covering `Upload` **and** `UpdateRemote`), `mkdirRemote`, `moveRemote`, `trashRemote` — always inside the transaction that commits the baseline row, so a crash can never leave one without the other (invariant 6). The conversion is `remotedelta.NodeFromFile` (`toNode` exported), so our row and the feed's later row come from **one** function that cannot drift. **E1 made the bug testable at all:** `driveclient.Fake.HoldChanges(bool)` freezes the feed at its current log offset while mutations queue behind it — the fake's instant visibility was itself the gap that let this ship. **Every arm red-first, observed not assumed:** upload → `quarantine_local` + the id absent from `remote_nodes`; `UpdateRemote` → a download of *stale* content refused by the md5 guard, a failed action every cycle; `MkdirRemote` → `quarantine_local docs`, the whole new folder; `MoveRemote` → the rename undone, the file back at its old name; `TrashRemote` → the deleted file downloaded back. **E4** asserts the invariant (every baseline id present in the mirror) inside the engine harness's `sync()`, so all ~57 tests in the package are witnesses — 57 fail with the fix disabled. **E5** adds `syncWithheldFeed` to the fuzzer **plus a recorded negative result**: the convergence oracle alone *cannot* see this class, because trashing your own upload and downloading it back converges perfectly — the invariant is the oracle. Docs same commit: spec §4.1/§4.2/§12, MANUAL §8 entry removed, testing.md E1.1–E5.2. `go build ./... && go vet ./... && go test ./... && make audit` green; `-race` on engine/executor/statedb/driveclient; 40-seed fuzzer sweep green (with crashes on too, once W17 — which that sweep found — was fixed).

**W13 CLOSED — deletions from Drive land in your own bin, verified on both machines (2026-07-27).** The two verifications the code was waiting on are in. **T13.10 (2026-07-26, agent, on the Mac):** `trash_darwin.go` + `trash_darwin.m` (cgo, `NSFileManager trashItemAtURL:resultingItemURL:error:`, `-framework Foundation`) had never been compiled anywhere — only cross-*build*-checked with `CGO_ENABLED=0 GOOS=darwin`, which drops the file. First compilation was **clean**, no cgo/header/linker change; `TestMoveToTrashDarwinMissing` + `TestAvailableDarwin` pass and the opt-in `SYNCKEEPER_TRASH_TEST=1 TestMoveToTrashDarwin` moved a real file into `~/.Trash` with the source gone (verified via `-run 'Darwin|Available'` that these are genuinely compiled in, not constraint-dropped). Whole-machine gates green on the Mac for the first time with the W13/W14 code: `go build ./... && go vet ./... && go test ./...` 19/19 packages plus `make audit` — `internal/watch` included, the pause flake did not reproduce. **T13.17 (on-hardware acceptance, both platforms):** Linux 2026-07-26 — a 1117-file folder trashed in the Drive web UI removed in one cycle (`actions=1 executed=1 failed=0`), one restorable desktop-bin entry; macOS 2026-07-27 — **Max: *"I tested the linux and mac machine and can confirm that all works as expected"***. Commit `2ccdef8` carries T13.10 + the doc updates; no production code changed for either verification. Windows owes the same acceptance with W8's recycle-bin backend.

**W14 — the mass-delete guard fires on recoverability, not volume (2026-07-26).** **M1:** `guards.CheckMassDelete` takes `binAvailable` and counts only `QuarantineLocal` (files, `SubtreeFiles` for a collapsed folder); `TrashRemote` is never counted (Drive's bin holds it), and with a system bin present nothing is held at all. The old rule — >10 absolute **and** > `mass_delete_threshold`, one-shot aborts / daemon defers and surfaces the block — survives unchanged for the no-bin case. **M2:** an absent bin is a standing state: a `system bin:` line in `status` and `doctor` (shared `systemBinLine`, naming the fallback and `--confirm-deletes`), one WARN at daemon startup, and a per-cycle `activity` entry counting items the bin actually refused (`Summary.QuarantineFallbacks` → `Result.QuarantineFell`). **M3:** a large deletion that ran is reported, not blocked — engine WARN + a `deleted` activity line with the count and destination, from `Result.DeletedLocal`/`DeletedRemote`/`LargeDeletion`. **M4:** `CollapseDirDeletes` gained the remote arm — a locally-deleted folder is **one** `TrashRemote` (asserted: `fake.TrashCount()` == 1 for a 30-file folder), one restorable Drive-bin entry, and the other machine still converges; a Drive item the plan never accounted for refuses the collapse, and the two directions never absorb each other. **M5:** no config or CLI change (the `mass_delete_threshold` key and `--confirm-deletes` both stay, narrowed in meaning) — DOC1 manifest untouched. **M6/M7:** spec §3 invariant 4 amended (its sync-dir sentence verbatim) + §6; MANUAL §3/§4/§6/§7; testing.md M14.1–M14.8 with G1/G3/W12-F2/T13.5 **re-targeted** to the no-bin case rather than deleted. `go build && go vet && go test ./... && make audit` green. Two as-built calls in decisions.md (the report lives in the activity ring instead of a new schema column; the guard is *told* the destination instead of probing it).

**W13-T2…T6 — a folder deleted in Drive is one entry in your bin (2026-07-26).** **T2 (collapse):** `reconcile.CollapseDirDeletes` (pure) absorbs a directory delete's descendants when *every* planned action under it is delete-class — any upload/download/record/move beneath it cancels it — highest directory wins, siblings collapse independently. The action carries `Subtree []SubtreeEntry` (rel_path, is_dir, file id, and the scan's size/mtime per covered file) + `SubtreeFiles`. The engine calls it **after** `guards.CheckMassDelete` and only when a bin exists. **T3 (executor + gate):** `quarantineLocal` → `trashLocal`; new gate primitives `guardedTrashPath` / `guardedTrashDir` / `verifySubtree` in `localwrite.go` (spec §7 — the AST gate test now also bans `MoveToTrash` outside it). Ladder: verify the subtree → move the folder to the bin → on *anything* unexpected (stranger, type change, drift) or a trash failure, delete item by item (bin first, quarantine second), never a hard delete. **T4:** `statedb.DeleteItemsByID` (batched) — by **id**, not rel_path prefix, because a delete's path can be a post-move local path while its rows are keyed at the baseline path (decisions.md). **T5:** one `trash` activity line per subtree with `(N files)`, `quarantine` when there is no bin, and `info` shows the bin destination via `Describe()`. **T6:** spec §3 invariant 3 / §4.2 / §10 `trash` row, MANUAL §3/§6/§7, README layout, testing.md T13.4–T13.15 + R13/R20/R21 restated. **Guard hardening beyond the plan:** `CheckMassDelete` counts `SubtreeFiles` too, so the "guard before collapse" ordering can never silently become load-bearing again (R10 intact — an uncollapsed directory still counts zero). **Red-first verified** on the two safety tests (disabling `verifySubtree` fails both). **No test can reach the developer's own bin:** `trash.Fake` is pinned as the default in both suites' `TestMain` after an early run put four items in Max's real trash (removed). Suite + `-race` + `make audit` green; darwin/windows cross-builds green. **Open:** T13.10 (compile/run the darwin path on the Mac) and the real-hardware acceptance.

**Credential model reversed — no embedded creds, `credentials.json` required, history scrubbed (2026-07-25).** GitHub push protection blocked the first `git push`: the embedded client id/secret were in `internal/auth/credentials.go` (HEAD) and two history commits (`d7b485f`, `49c5f7f`, incl. a committed `client_secret_*.json`). Reversed the 2026-07-24 S1 "accept as publishable" decision. **Code:** `ClientID`/`ClientSecret` now empty `var`s (no secret in source; `-ldflags` still injects for a private build); `resolveClient` → `ErrNoCredentials` without a `credentials.json`; audit allowlist drops `credentials.go`; tests `TestResolveClientRequiresCredentials`/`…EmbeddedViaLdflags`. **History:** id/secret + the `.json` scrubbed from every commit via `git filter-repo` (metadata preserved, hashes change) — **Max force-pushes**. **Docs:** spec §9, MANUAL §1/§5/§9, CLAUDE.md credential rule (reversed), README, plan.md W5.5-S1, testing.md S1-rev, decisions.md. Optional: rotate the client in the Google console (push was rejected → never reached GitHub). Suite green.

**W11 — `info` command (2026-07-25).** `synckeeper info [--json]` in `cmd/synckeeper/info.go`: a read-only, offline, no-lock static snapshot — version; every config-file path (config dir, `config.toml`, `state.db`, `token.json` + mode, `credentials.json`, `control.sock`, quarantine, log); sync dir; Drive folder + root id; machine name + id; OAuth client (source + id, never the secret); token status; effective config; local state (tracked/pending/quarantine + one-line daemon state). Works **before init** (config defaults + hint, no empty `(id )`) and after; `--json` mirrors `status`. Pure render funcs unit-tested (`TestPrintInfoHuman`/`…NotInitialized`/`…JSON`); adding `info` to `newRootCmd` tripped the DOC1 guard until the manifest + MANUAL §3 + spec §15 were updated together (guard working as designed). decisions.md "W11 done"; testing.md W11. Suite green. Also this session: documented cobra's built-in `help`/`completion`/`--version` (were missing from MANUAL §3) and closed the DOC1 guard's blind spot that let them slip.

**W5.5 — Security & privacy hardening, CLOSED (2026-07-24).** The pre-W6 security gate, all four items done. **S1** — embedded OAuth secret reviewed (distribution: embedded `var` + BYO `credentials.json` + `-ldflags`; loading: single `resolveClient`→`oauthConfig`→Google over HTTPS; never logged/printed/persisted) and accepted as publishable (debug creds); no code change. **S2** — no folder-scoped Drive scope exists; decided **keep full `drive`**, logged as a MANUAL §9 known limitation (`drive.file` on record as the future option, gated on a scope-downgrade retroactivity test + a §4/§9 amendment). **S3** — the daemon chmods its launchd log (`0644`→`0600`, owner-only) at `watch` startup since it records synced file names (`service.RestrictLogToOwner`; tests W5.5-S3). **S4** — a self-contained pre-publication secret-scan gate (`internal/audit` + `make audit`): fails on any tracked secret (the one intentional embedded client secret allowlisted), any tracked `token.json`/`credentials.json`/`client_secret_*.json`/`*.db`, or a `.gitignore` that drops those patterns; the detector is proven to fire (`TestScanBytesDetects`), and it runs inside `go test ./...` for continuous enforcement. Commits `dc9c7b9` → `48d0b55` (+ the S4 commit). `go build && go vet && go test ./...` green. **Next actionable non-hardware item: W10.**

**Security review + W5.5 gate created (2026-07-24).** Pre-W6 security pass over the implementation for the two threats Max named — leaking a key/secret, exposing personal files. **The "share publicly" threat is structurally absent:** no Drive `permissions`/sharing API call exists anywhere, so uploads stay owner-private and nothing can be made public; only egress is `googleapis` over HTTPS; only listener is the loopback OAuth server (`127.0.0.1`, CSRF `state` + S256 PKCE, 5-min timeout). Secret handling sound: `token.json` `0600` atomic + refused if group/world-readable, control socket `0600` unix (no TCP), config dir `0700`, `.gitignore` covers `token.json`/`credentials.json`/`client_secret_*.json`/`*.db`, and no token/secret/credential/content in any of the 28 `slog` sites or any `fmt.Print` (`account` shows only presence/expiry). Four residual items, all *decisions* not bugs: (S1) the embedded OAuth secret is committed (spec §9, by design; safe while private, but publication exposes it and git history keeps it past a source edit → rotate+revoke in the console before publishing); (S2) full `drive` scope = leaked-token blast radius is all of Drive; (S3) launchd log records file paths (never contents), maybe `0644` on a shared Mac; (S4) no enforced pre-publication secret scan. Slotted as **plan.md W5.5**, runs before W6; decisions.md "Security review". The four calls are Max's, to make within W5.5. Review + planning only — no code/spec/behavior change.

**Doc audit — MANUAL/README reconciled to code + a doc-code drift guard (2026-07-24).** Adversarial pass over MANUAL.md/README.md against spec + code: the surface is overwhelmingly accurate (all 13 commands+flags, config defaults, hot/restart split, mass-delete semantics, conflict-copy naming, paths, credential model, known-bugs/limitations all verified true). Found **one real inaccuracy** — MANUAL §6 claimed skipped files are "shown in `status`", but the daemon never surfaces skips (no field in `DaemonStatus`/`CycleSummary`; skips print only via one-shot `sync`/`init --adopt`) — now corrected to "reported in `sync` output, not `status`". Plus three staleness nits fixed: README `Use` still listed `service install` as a step (pre-W5 onboarding), README `Status` said "correctness fixes first" (all closed), MANUAL §3 `config` "where it came from". **Guard added** so this drift is caught in future: (a) a `CLAUDE.md` Hard rule — *doc claims are claims about code, not intent* (verify where/what/default claims against the code, not the spec); (b) an automated golden guard, `cmd/synckeeper/surface_test.go` (`TestCLISurfaceMatchesManifest`) — `main()` now shares `newRootCmd()` with the test, which pins the command set + per-command flags + global `-v`, failing the build if the CLI surface changes without the manifest + MANUAL §3/spec §15 being updated together. No spec change and no product-behavior change (the spec was found accurate). The one real behavior gap D1 exposed — the daemon never surfaces skips — is **scheduled as plan.md W10** (Max's call, 2026-07-24: per-skip `activity` entries; capped-ring noise flagged as a build-time detail), not built here. decisions.md "Doc audit"; testing.md DOC1; plan.md W10. `go build && go vet && go test ./...` green.

**W5 — daemon-first polish, closed (2026-07-24).** Three items, all landing with tests. **(1) `init` offers the login service.** After a successful `init`/`init --adopt`, an interactive Y/n prompt (`Start Synckeeper automatically at login?`) installs it; a non-interactive run prints a hint; `--service`/`--no-service` force the choice (`--no-service` wins if both). The offer **never fails init** — a decline, a piped run, or an install error only prints the manual hint. Logic is a pure injected helper (`offerServiceInstall`/`promptYesNo`/`isTerminal`, `cmd/synckeeper/serviceoffer.go`) so it's unit-tested without the OS service manager; the daemon it starts grabs the instance lock once init exits (launchd/systemd restart it). **(2) `account` shows the Google account email.** `driveclient` gained `About(ctx) (About{Email,DisplayName}, error)` — real backend calls `about.get` with the required `user(...)` fields mask, fake returns a settable `AboutInfo`; `account` fetches under a 15 s timeout and prints it via `printAccountIdentity`, **best-effort** (no token/offline/API error → a short "unavailable" note, never a command failure). Retires the old "phase-6 follow-up" stub. **(3) Pause kept in-memory** (Max's decision, no code): a restart is rare and explicit, and resuming automatic syncing is the safe default. Tests W5.1 (`TestOfferServiceInstall` 7 paths + soft-failure + `TestPromptYesNo`) and W5.2 (`TestPrintAccountIdentity` + name-without-email guard). spec §8.3/§8.4/§15 dated; MANUAL §1/§2/§3; decisions.md 2026-07-24 "W5". `go build && go vet && go test -race ./...` all green.

**W3 follow-up — FSEvents detects a volume remount and recovers (2026-07-24, W3-adv-3).** The one low finding the adversarial check deferred to Max is fixed: a per-volume FSEvents stream dies when its volume unmounts+remounts (external-drive sync dir), and `needsRebuild=false` left it dead for the daemon's life while `status` still said "watching". `refresh` — formerly a pure no-op — now re-stats the root and returns 1 (unwatchable) on a device-id change (remount) or a vanished root (unmount race); the R15-tested failure latch degrades to polling (honest `status`) and the recovery path recreates the stream on the current volume. Parity with fsnotify's per-cycle re-walk; reuses the fd-pressure resilience path rather than adding a second. Zero change on an unchanged volume (every internal-drive sync dir → `refresh` returns 0). Red-first: `TestFSEventsRefreshDetectsRemount` via the `fseventsRootDev` seam. Suite + `-race` green (cgo on) and `CGO_ENABLED=0` green. spec §10 dated; decisions.md 2026-07-24 "W3 follow-up". No MANUAL change (MANUAL §9 already promised auto-restore + mode display).

**W3 (item 5) — the 2 h FSEvents soak gate PASSED, W3 closed (2026-07-24).** `SYNCKEEPER_SOAK_SECONDS=7200 go test ./internal/watch/ -run TestSoak -timeout 10h -v`, code `aefe4fe`, two live FSEvents streams under 2 h of create/edit/rename/delete chaos on both sides: `--- PASS: TestSoak (7216.68s)`, **converged on 16,344 files** (byte-identical trees), clean `doctor` on both machines, zero divergence/panic; the 3,488 transient `lstat … no such file` retries (chaos racing the scanner) converged as designed. Ran continuously the full 2 h (09:39→11:39 CEST). Had to be launched **detached** (nohup → launchd, PPID 1) so the machine's 1-min idle-sleep couldn't tear it down like it killed three prior harness-task attempts (all clean to the sleep instant — not failures). testing.md soak row passing; spec §16.9 + plan.md W3.5 dated done; decisions.md 2026-07-24. **W3 is closed; execution order W1.8 → W1.9 → W4 → W3 all done, next W5.**

**W3 adversarial check — two fixes, red-first (2026-07-23), before the gate.** (1) The soak harness's watcher stop only cancelled the daemon's context; the settle phase then ran `engine.Sync` concurrently with the daemon's in-flight last cycle on the same engine (cycle-serialization violation → could mint a duplicate-name pair on the fake Drive → a spurious red after 2 h). Fix: `startWatcher`'s stop now `cancel(); <-done`. Test `TestStopWaitsForDaemonExit` (W3-adv-1). (2) FSEvents `shouldWake` filtered on the basename only, so churn under an ignored directory (`node_modules`/`.git`/`build` — MANUAL §4 name-only hot globs) woke the daemon into a full no-op rescan per write, while fsnotify (which never watches inside ignored dirs) stayed quiet. Fix: component-wise `ignoredPath` relative to the symlink-resolved stream root. Test extended `TestFSEventsShouldWakeFiltersIgnored` (W3-adv-2). Plus F4 (rebuild_test goroutine-leak cleanup) and nits (CLAUDE.md soak `-v`, status.go comment). One low finding left as Max's open call and recorded (decisions.md "fix 1"): a mid-life dead FSEvents stream (external-volume remount) is never rebuilt (`needsRebuild=false` for life) — poll covers correctness, `status` overstates "watching". Suite + `-race` green (both cgo modes); pre-gate battery incl. a 60 s `-race` soak green.

**W3.5 — the soak runs against the production FSEvents backend (2026-07-23).** `TestMain` captures the platform default (`productionBackend = newBackend`, FSEvents on darwin+cgo) before pinning the suite to fsnotify for the R14/R15 seams; `TestSoak` restores it (`newBackend = productionBackend`) so the soak validates what the production build actually runs, with no build-constraint reference to the FSEvents symbol leaking into the cross-platform soak file. Smoke runs at 30 s and 90 s converge with clean `doctor` on both machines against FSEvents. spec §16.9 dated; decisions.md "W3.5".

**W3.4 — the periodic watcher rebuild is per-backend (2026-07-23).** `fsWatcher.needsRebuild()` — fsnotify true (kqueue's per-file fd leak is why the ~6 h rebuild exists), FSEvents false (a directory-tree stream holds no per-file descriptors, so tearing it down and recreating it was pure waste). The loop's rebuild branch is gated `cycle%cadence == 0 && fw.needsRebuild()`; when false it falls through to the normal `refresh` (a no-op for FSEvents) and the stream keeps running. The rlimit raise and polling latch stay for every backend. Tests: `TestRebuildIsPerBackend` (fake backend via `newBackend`: `needsRebuild=false` → created once, never recreated; `true` → recreated), R15 still drives the real fsnotify rebuild→degrade→recover path, the FSEvents wake test asserts the real backend reports false. Suite + `-race` green (both cgo modes). spec §10 dated; decisions.md "W3.4".

**W3.3 — the W1-scale acceptance, at 50k (2026-07-23).** `internal/watch/scale_test.go` (`TestScale`) + `TestFSEventsScaleNoFDExhaustion`, gated by `SYNCKEEPER_SCALE_FILES` (acceptance 50000; mirrors the soak gate). Measured at 50k on macOS: **FSEvents 0/500 dirs unwatchable** (a directory-tree stream holds no per-file descriptors — the crisp "no fd exhaustion" result); **fsnotify 403/500 unwatchable** at the 10240 fd soft limit → the failure latch trips → polling (graceful, not fatal — the full kill→poll→recover loop is R15, scale-independent); a full engine sync of **50 500 actions** converges and the second cycle is idle. Sized the carried-over self-inflicted-events question: after a cycle writes files, their events fire one bounded, self-terminating extra cycle (one full rescan — ~19 s at 50k, sub-second at personal scale), never a loop; suppression is an optimization, deferred (decisions.md "W3.3"). Spec §16.8 verified; `SYNCKEEPER_SCALE_FILES` command in CLAUDE.md.

**W3.2 — the FSEvents backend, the repo's first cgo (2026-07-23).** `internal/watch/fsevents_darwin.go` (`//go:build darwin && cgo`): one recursive **file-level** FSEvents stream on a private serial dispatch queue, callbacks routed to Go by a handle registry (a Go pointer can't ride in the C stream context); `refresh` is a no-op returning 0 (the tree stream auto-covers new dirs, zero per-file fds); `shouldWake` filters ignored paths before waking so Finder's `.DS_Store` churn doesn't spin the daemon. `close()` stops+invalidates+releases (Invalidate blocks in-flight callbacks) then drops the handle; not idempotent by design (the loop closes then recreates). Selected on darwin+cgo by an `init` override of `newBackend`; the pure-Go fsnotify backend stays the fallback when cgo is off (build constraint drops the file) and everywhere else. Makefile dropped its global `CGO_ENABLED=0` — `build`/`test`/`vet` native (cgo on → FSEvents), `build-all` pure-Go cross-compile (fsnotify fallback). Existing watch tests pinned to fsnotify via `TestMain` (R14/R15 stay meaningful); a darwin+cgo test covers the FSEvents wake + the filter. Verified: native build+test+`-race` green; `CGO_ENABLED=0` build/watch-tests green; all-platform pure-Go cross-compile green. Spec §10 + build policy dated; decisions.md "W3.2"; testing.md W3.2 row.

**W3.1 — the fswatch backend interface, extracted (2026-07-23).** The sync loop no longer holds a concrete `*fsnotify.Watcher`: file watching lives behind an unexported `fsWatcher` interface (`internal/watch/fswatch.go`, `refresh(root) int` + `close`), with the pump, per-directory `Add`, new-dir registration, and event filtering moved into `fsnotifyBackend`. Wake-ups and ignore globs are injected (`wake func()`, `ignore func() []string`), so the loop keeps sole ownership of the debounce timer and config (R14) while the backend owns its pump. `newBackend`/`newNotifyWatcher` are the seams (R15 preserved); the `failureLatch`, rebuild cadence, and polling fallback are untouched. Pure refactor — the existing watch tests (R14 reload race + event storm, R15 rebuild/creation failure, wake-up, rebuild cadence) are the net and pass under `-race`. Spec §10 dated; decisions.md "W3.1". The interface is shaped for a whole-tree backend (FSEvents `refresh` once → 0 failed).

**W4 — randomized convergence fuzzer + R25 (2026-07-23).** `internal/engine/fuzz_test.go` (`TestFuzzConvergence`): N machines against the shared fake, random op streams, interleaved syncs, one-shot crashes at executor checkpoints, driven to a quiescent fixed point. Oracle (all exact/non-flaky): `ValidateTransferStage` on every plan; eventual convergence + idempotence; no silent divergence (byte-identical machine + Drive trees); identity stability scoped to the clean single-writer rename (quiet-rename probe). Deterministic replay via a pinned `nowFunc` clock + monotonic mtimes; the one-shot crash latch is atomic (`-race`-clean). Default bounded (8 seeds × 70 steps, ~10s; `-short` shrinks it); `SYNCKEEPER_FUZZ_{RUNS,STEPS,SEED,MACHINES,CRASH}` widen it. **First run found R25**: with a baseline file whose remote moved to a new path and the local file already there (the same rename made locally, or a crashed MoveLocal leaving disk+Drive ahead of the baseline), reconcile planned `download Q` + `upload Q` on one rel_path → §4.5 refused the whole plan every cycle → permanent wedge (reproduced with *and* without crashes). Minimized red-first at the reconcile level (R25), fixed in `plan.go` pass 1 (record the file in place at its new path when it isn't a baseline path — the guard that keeps it from firing mid-swap, R6), spec §4.6 amended. Two recorded oracle scope calls: type clashes (C5) and the deferred directory-arm gaps are excluded from the convergence oracle by design, each covered by its own test (decisions.md "W4"). Passing rows: FZ1, R25; verified `-race`-clean across a 6-seed deep sweep and 60-seed non-race sweeps.

**W1.9.6 — a shadowed folder's tracked subtree is held harmless (2026-07-21).** An adversarial review of docs/code/tests found that W1.9.1's "never a quarantine" guarantee held only for the directly-colliding row: `remotedelta.Snapshot` skips a folder that lost "first by id" without walking its subtree, so the engine's shadowed set missed the folder's tracked descendants and reconcile quarantined them off disk (reproduced end to end, R24 — content recoverable, but the stated bound was wrong, and untested). Fix is engine-side and small: `expandShadowed` marks every baseline row under a shadowed skip's rel_path, not just the directly-skipped id. Not fold-specific (the exact-duplicate-name folder case shadows identically on any FS). R24 + `TestExpandShadowedCoversSubtree` passing; suite + `-race` green. The full directory arm stays deferred; this only closes the descendant-quarantine hole in its safety envelope. The same review also corrected MANUAL.md's stale "Last updated" stamp (2026-07-18 → 2026-07-21) and otherwise found docs/code/tests sound (build/vet/test/`-race` all green; every testing.md row's named test verified to exist and, on spot-check, to assert its claim). A second review pass (2026-07-22) swept the truth-producing components (`scanner`, `remotedelta` Refresh/prune/consume, `statedb.UpsertItem`) and found them correct; its one finding was doc-only — MANUAL §8 overclaimed that files with case-colliding names are fully handled, while a *tracked* file renamed case-only on Drive error-loops (a recorded W1.9.1 gap). MANUAL §8 now lists that loop with a workaround; the code fix stays the deferred dir-arm follow-up (Max's call, not folded in). No code, spec, plan, or testing change.

**W1.9.5 — PKCE on the OAuth loopback flow, and W1.9 closes (2026-07-19).** `GenerateVerifier` + `S256ChallengeOption` on the URL, `VerifierOption` on the exchange. R23 runs the whole loopback flow in-process against a fake token endpoint (two seams: endpoint + browser-opener; the test plays the browser) and proves S256(verifier) equals the URL's promised challenge — a URL-only unit test was rejected as it wouldn't catch a verifier that never reaches the exchange (decisions.md "W1.9.5"). Row R23 passing.

**The 2026-07-19 run, in one paragraph** (eleven commits, `ac71dd4` → `f63b202`; each fix has a full commit message, a decisions.md entry for its non-obvious choices, and a passing testing.md row): W1.8 items 5–9 — §4.5 enforcement (`ValidateTransferStage`, R12, plus a follow-up commit closing two review-found doc nits), the reload race (atomic ignore-glob snapshot, R14), watcher-failure survival at rebuild *and* launch (R15), the crashed-dir-move replay + `Snapshot` visited set (R16/R17, where the red test corrected the round-2 analysis — the crash shape had been destructive, not benign) and N3's named case — then W1.9 items 1–5 — cross-tree fold conflicts for files (R19; **dir arm deferred, recorded**), the quarantine leftover sweep (R20), rescue-copy uniquification (R21), type-clash skips in every direction (R22), PKCE (R23). Four MANUAL Known-bugs entries retired, one narrowed. Standing follow-up from the run: **the gate's directory arm** (decisions.md "W1.9.1"/"W1.8.8").

The 2026-07-18 session (gate + A1 collapse + guard fix + rounds 2–3 analysis + W2 close + MANUAL creation) is fully recorded in decisions.md's entries of that date and the git history; the round-3 defect list (C1–C7) lives in decisions.md 2026-07-18 "W1.9".

## In progress

**W18 — C, A, D, B, E, G done; H, F, I remain (2026-08-01).** Order was C → A → D → B → E → G → H → F → I. Next up is **H** (journal recovery classifies its `List` failure — permanent 404/permission skips the op and continues, transient keeps failing the cycle; reuses `driveclient.IsNotFound`, added in A), then **F** (`doctor` unregistered — DOC1 guard), then **I** (the doc sweep). The planning context below is the original entry, kept because G/H/F/I still point into it.

**Awaiting Max's triage — found 2026-08-01, not fixed:** a fuzzer run at crash-probability 1.0 wedges 2 of 40 seeds (2 and 19) on a §4.5 `upload`+`download` overlap on one rel_path after a local folder rename — the R25 family. Reproduced identically at `433d41f` (W17's close), so it predates W18; it passes at the default crash probability, which is why no recorded sweep saw it. Nothing is lost (the plan is refused), but the machine stops syncing until something external changes. Recorded in decisions.md and MANUAL §8; where it lands in the order is Max's call, with the W16/W17 precedent that a reproduced correctness defect outranks features and ports.

**W18 — the original planning entry (2026-07-31).** An adversarial review of code, plan and docs reproduced five defects; Max triaged each and specified the target behaviour rather than a patch. plan.md W18 carries the ordered items and decisions.md the two entries (findings, then decisions). **Nothing is built yet — the plan is awaiting Max's read.** The headline: root resolution goes **by name**, so a Drive-folder rename repoints `root_folder_id` at a new empty folder and the next cycle bins the whole local tree (measured 3 files → 0, guard silent because W14 turned it off wherever a system bin exists). The decided shape is id-first resolution, an idempotent flagless `init`, and "a missing root is never a deletion" in both directions — the last of which needs no new guard, because spec §11 already proves an empty baseline *structurally cannot produce a delete*: reset the baseline whenever root identity changes and both catastrophes disappear together. `doctor` is unregistered (its recovery job is now `init`'s, with a stronger guarantee). Amends spec §3 invariant 4; trips the DOC1 guard twice; needs schema v4→v5. **Items D and E were re-opened before implementation and settled 2026-07-31** (decisions.md "W18 items D and E settled"): E stays — the migration is one `alter table` line with no backfill, since E fires only at `init`, which force-walks the mirror that feeds it. **D shrank.** Max's rule — *"if the files are on the DB and they are gone in local then deletion propagates"* — is spec §4.2 verbatim, already built and already what Dropbox does (merge-base diff, not event observation; §4.1 "identical to Nucleus"). So a **missing** sync dir is recreated + re-downloaded, an **emptied** one propagates its deletions, and the guard's empty arm is *deleted* rather than rewritten. A marker file, a device-id check and a "did we detect it" gate were all explored and dropped. Mounting only the sync folder from a separate volume is **unsupported by decision** (MANUAL §9, with the code).

## Next

**Order: ~~W14~~ → ~~W13 close-out~~ → ~~W16~~ → ~~W17~~ → ~~W15-U6~~ → **W18** → W7 → W6 → W10; W12 and W9 parked.**

0. ~~**W14**~~, ~~**W13 close-out**~~, ~~**W16**~~, ~~**W17**~~, ~~**W15**~~ — all done (see Last completed).
1. **W18 — root identity + idempotent `init`** ([plan.md](plan.md) W18): ~~C~~ → ~~A~~ → ~~D~~ → ~~B~~ → ~~E~~ → ~~G~~ → **H → F → I**. Ahead of W7 for the reason W16 jumped the queue: a reproduced correctness defect outranks features and ports.
2. **W7 — Linux port** ([plan.md](plan.md) W7): items **L1–L5, L7, L8** (L6 done). Order inside it: **L1 → L2 → L5 → L3 → L4 → L8 (gates) → L7 (docs)** — L1/L2/L5 change what `init` asks and what `service install` writes, so they land before the service is (re)installed; docs come last because doc claims are claims about *validated* code. Nothing in L1–L5 blocks Max's rollout; L8's soak wants the box unattended (run **detached** + `systemd-inhibit --what=sleep`, decisions.md 2026-07-24). **W18 lands first** — it rewrites what `init` does, which L1/L2 also touch.
2. **W6 — Real multi-machine rollout** ([plan.md](plan.md) W6): **Max performs it himself** (binary → creds → machine name → `login` → `init --adopt` → **`service install` last**; then a day under `watch`, clean `doctor` on both, reboot check). No agent task under W6.
3. **W10 — daemon surfaces skips in `activity`** ([plan.md](plan.md) W10): still open, needs no hardware; below the ports in importance. Note the W15 interaction: the dashboard renders whatever activity kinds exist, so W10's new `skip` kind needs its glyph/label there too — U3 leaves a default case so an unknown kind renders rather than vanishes. **W8 (Windows)**: not started — it also owes W13 a recycle-bin backend.

**W9 (tray/menu-bar app) is parked (Max, 2026-07-27)** — W15 delivers the interactive front end (*"like the gui application but just from command line"*), so the tray is ergonomics rather than capability; reopen if the menu-bar presence is missed. Reviving it stays cheap because W15-U5's `stat` payload is specified as the shared client read model. decisions.md "W9 parked".

**W12 stays parked at Max's call** — F2 resolved as "not a defect", the blast radius proved nil (nothing reached Drive), and all three reproductions of the incident's shape came out correct; the MANUAL §8 entry stays and plan.md W12 records the two experiments to run **if it recurs**.

**Closed loose end:** the flaky `TestControlPauseSuppressesSyncThenResume` (~1 in 3 uncached full-package runs) is fixed in its own commit — the test now waits for the startup cycle to record itself before pausing, since a cycle already in flight legitimately runs to completion. Verified with `-count 8` and three uncached full-package runs.

Unscheduled, on the table from the W1.8/W1.9 close-outs: the gate's **directory arm** (case-only dir renames + dir fold-adopts + R7-for-dirs + tracked case-only renames — one family, decisions.md "W1.9.1"/"W1.8.8"); W4's convergence oracle deliberately excludes it, so it won't surface there until built.

**Discipline reminder** (unchanged): a defect found while building gets a **minimized red regression test, then the fix, then the testing.md row**, suite + `-race` green at every commit, and a MANUAL.md Known-bugs entry if it ships unfixed. If the plan turns out wrong when it meets the code, that is a decisions.md entry, not a silent deviation (W1.6/W1.8.8/W4-R25 precedents).

## Blockers / parked

- **W6** — no longer blocked (2026-07-25): the Linux box is the second machine. Not parked, just **Max's to run** (see Next). *(Soak lesson for it and the ports: run any long soak **detached** and defeat sleep — `systemd-inhibit --what=sleep` on Linux, AC + launchd reparenting on macOS — an aggressive idle-sleep tears down harness-tracked background tasks. decisions.md 2026-07-24.)*
- **Rotate the OAuth client before publication — RESOLVED (2026-07-24), no longer parked:** Max runs on debug credentials and accepts them as safe to publish, so no rotation/revocation is required (W5.5-S1, `done`; decisions.md "W5.5 item calls").
- **Deferred to publication:** the donation note (W2.4d; lands in MANUAL.md §5 since the README/MANUAL consolidation) — repo is still private.
- **Sized, not a defect (W3.3):** every cycle that writes files fires its own watch events, re-triggering the loop — a bounded, self-terminating one-extra-cycle rescan, never a loop (measured ~19 s at 50k, sub-second at personal scale). Suppression is an optimization, deferred.
